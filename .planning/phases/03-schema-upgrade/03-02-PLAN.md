---
phase: 03-schema-upgrade
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/src-tauri/src/archive/upgrade.rs
  - app/src-tauri/src/archive/mod.rs
  - app/src-tauri/src/archive/manifest.rs
  - app/src-tauri/src/error.rs
  - app/src-tauri/tests/schema_upgrade_tests.rs
  - app/src-tauri/tests/manifest_tests.rs
  - app/src-tauri/tests/archive_validity_tests.rs
  - app/src-tauri/tests/error_tests.rs
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "Opening a v12/v13/v14/v15/v16 fixture archive succeeds and the session reports schema_version 16"
    - "Opening a v11 fixture fails with a typed too-old error naming the version; a v17 fixture fails with a distinct too-new error"
    - "A failed upgrade surfaces as a typed error and leaves the working-copy DB at its ORIGINAL version — never half-migrated, never silently swallowed"
    - "The user's source file on disk is byte-identical after an upgrade-on-open"
    - "After upgrade, both PRAGMA user_version and session.manifest.schema_version read 16, and a saved upgraded archive's manifest schemaVersion is 16"
    - "Existing non-null Specialty/Edition values on a v<16 Location survive the upgrade unchanged (never overwritten with NULL)"
    - "After upgrade, the working DB is validated against a v16 contract (required tables, all v16 Location columns, IX_Location_Media unique index, user_version==16); any gap fails LOUD with a typed error rather than being accepted"
    - "The manifest.rs schema gate and the archive/mod.rs gate share one source of truth for the 12-16 range and can never disagree"
  artifacts:
    - path: "app/src-tauri/src/archive/upgrade.rs"
      provides: "upgrade_to_v16 transactional DDL port + column_exists guard + post-upgrade v16 contract validator"
      exports: ["upgrade_to_v16", "validate_v16_contract"]
      contains: "fn upgrade_to_v16"
    - path: "app/src-tauri/src/archive/mod.rs"
      provides: "range gate (accept 12-16, reject <=11 / >=17) + upgrade call on open + post-upgrade contract validation"
      contains: "upgrade_to_v16"
    - path: "app/src-tauri/src/archive/manifest.rs"
      provides: "widened schema gate (accept 12-16, reject <=11 SchemaTooOld / >=17 SchemaTooNew) sharing the mod.rs range constants"
      contains: "SchemaTooOld"
    - path: "app/src-tauri/tests/schema_upgrade_tests.rs"
      provides: "full version + reject + rollback + idempotency + edge-case + Specialty/Edition-preservation + representative-Location-type + FK-off + contract-validator + manifest/PRAGMA-mismatch + source-unchanged + reopen-round-trip matrix"
  key_links:
    - from: "app/src-tauri/src/archive/mod.rs"
      to: "app/src-tauri/src/archive/upgrade.rs"
      via: "upgrade::upgrade_to_v16(&mut conn) then upgrade::validate_v16_contract(&conn) after PRAGMA check when version < 16"
      pattern: "upgrade_to_v16"
    - from: "app/src-tauri/src/archive/upgrade.rs"
      to: "SQLite Location table"
      via: "transactional CREATE Location_new / INSERT..SELECT (conditional Specialty/Edition source cols) / DROP / RENAME + PRAGMA user_version = 16"
      pattern: "PRAGMA user_version"
    - from: "app/src-tauri/src/archive/manifest.rs"
      to: "app/src-tauri/src/archive/mod.rs"
      via: "shared MIN/MAX/WORKING schema-version constants (one const/module, no drift)"
      pattern: "SchemaTooOld"
---

<objective>
The vertical slice: a real-user v14 archive that Phase 1 rejected now opens, is upgraded to v16 in the working copy, and displays its Notes — while a v11 archive is cleanly refused. This ports JWLManager.py:1016-1070's DDL to a transactional Rust `upgrade_to_v16`, widens BOTH open-time gates (archive/mod.rs AND manifest.rs) from v16-only to the 12-16 range with distinct out-of-range rejects, adds a post-upgrade v16-contract validator so any unknown v12/v13 gap fails LOUD instead of being mislabeled v16, preserves any pre-existing Specialty/Edition data, and proves the whole matrix (anti-`except:pass` failure test, transactional rollback, source-byte-identity, representative Location types, and manifest-reads-16) with a new test file plus updates to the existing manifest/validity/error suites.

Purpose: This is the reason the phase jumped the queue — 19 of the owner's 32 archives are v14 and were locked out. The core safety point (SAFE-05 / D3-02) is that a failed upgrade must fail LOUD, never leave a half-upgraded DB that later gets saved. The cross-AI review (03-REVIEWS.md, codex HIGH) surfaced two real corruption vectors folded in here: silent Specialty/Edition data loss on the D3-04 path, and a missing post-upgrade contract that could stamp an incomplete v12/v13 DB as v16.
Output: New `archive/upgrade.rs` (upgrade + validator), widened `archive/mod.rs` and `archive/manifest.rs` gates sharing one range const, new `tests/schema_upgrade_tests.rs`, and updated `manifest_tests.rs` / `archive_validity_tests.rs` / `error_tests.rs` (v14 now ACCEPTED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-schema-upgrade/03-CONTEXT.md
@.planning/phases/03-schema-upgrade/03-RESEARCH.md
@.planning/phases/03-schema-upgrade/03-VALIDATION.md
@.planning/phases/03-schema-upgrade/03-REVIEWS.md

<interfaces>
From app/src-tauri/src/archive/mod.rs (widen, do not rewrite wholesale):
  const SUPPORTED_SCHEMA_VERSION: i64 = 16;   // → replace with MIN=12 / MAX=16 / WORKING=16
  pub fn open_and_validate(path: &Path, resources_db_path: &Path)
      -> Result<(ArchiveSession, Vec<NotesRow>), ArchiveError>
  // current flow: extract → manifest schema_version check → open conn →
  //   PRAGMA user_version check (both == 16) → ResourceCatalog → query_notes →
  //   ArchiveSession { manifest: ManifestMeta { schema_version } }
  // conn is currently `let conn` (immutable); upgrade needs `let mut conn`.

From app/src-tauri/src/archive/manifest.rs (SECOND, independent gate — finding 3):
  const SUPPORTED_SCHEMA_VERSION: i64 = 16;      // line 28 — v16-ONLY, must widen
  impl Manifest { pub fn check_schema_gate(&self) -> Result<(), ArchiveError> }  // line ~109
  pub fn check_validity(manifest_bytes: &[u8]) -> Result<Manifest, ArchiveError>
  // This gate is SEPARATE from archive/mod.rs and currently emits UnsupportedSchema.
  // It MUST be widened in lockstep so the two gates cannot disagree.

From app/src-tauri/src/error.rs (added in 03-01):
  ArchiveError::SchemaTooOld { version: i64 }
  ArchiveError::SchemaTooNew { version: i64 }
  ArchiveError::SchemaUpgradeFailed { reason: String }
  // this plan MAY add ArchiveError::SchemaContractViolation { reason: String }
  // (post-upgrade validator) OR reuse SchemaUpgradeFailed { reason } — see Task 2.

From app/src-tauri/tests/common/mod.rs (added in 03-01):
  generate_fixture_pre_v16_shape(version: i64) -> (TempDir, PathBuf)   // v11..v15,
    // now emits REPRESENTATIVE Location rows (scripture/publication/media/Type1/
    // Type2-3/NULL-heavy) per finding 5 — the rebuild test asserts they all survive
  // + v17 fixture generator + existing generate_v16_fixture()
  extract_to_tempdir, normalized_table_rows, read_file_bytes

The DDL source of truth (port the DDL, NOT the except:pass):
  JWLManager.py:1016-1070 — upgrade_schema
  NOTE the INSERT..SELECT copies literal NULL,NULL into Specialty/Edition
  (JWLManager.py:1063). That is safe ONLY when the columns were just ADDed
  (empty). On the D3-04 path (columns pre-exist, possibly non-null, user_version<16)
  that NULL,NULL DESTROYS data — finding 1 fixes it with conditional source cols.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — the schema-upgrade test matrix</name>
  <read_first>
    - .planning/phases/03-schema-upgrade/03-VALIDATION.md (Per-Task Verification Map — exact behaviors and the differential row deferred to 03-03)
    - .planning/phases/03-schema-upgrade/03-REVIEWS.md (the 9 accepted findings these tests must encode)
    - app/src-tauri/tests/open_archive_tests.rs (how open_and_validate is driven in integration tests, resources_db_path via dev_resources_db_path)
    - app/src-tauri/tests/common/mod.rs (generators from 03-01: generate_fixture_pre_v16_shape now with representative rows, v17 fixture, normalized_table_rows, read_file_bytes)
    - app/src-tauri/tests/save_tests.rs (how a saved archive's manifest is read back — for the manifest-reads-16 and reopen-round-trip assertions)
    - .planning/phases/03-schema-upgrade/03-CONTEXT.md (D3-02..D3-07 the assertions must encode)
  </read_first>
  <behavior>
    - test_gate_accepts_v12_through_v16: open each of v12,v13,v14,v15,v16 fixtures → Ok, session.manifest.schema_version == 16
    - test_gate_rejects_v11: open v11 fixture → Err(ArchiveError::SchemaTooOld { version: 11 })
    - test_gate_rejects_v17: open v17 fixture → Err(ArchiveError::SchemaTooNew { version: 17 })
    - test_upgrade_v14_to_v16: after open, extracted DB PRAGMA user_version == 16 AND Location has Specialty+Edition columns AND IX_Location_Media exists
    - test_upgrade_noop_on_v16: opening the v16 fixture leaves the DB at 16 with data intact (idempotent, D3-05)
    - test_upgrade_rejects_direct_call_above_v16 (finding 7): calling upgrade_to_v16 on a DB with PRAGMA user_version == 17 returns Err(ArchiveError::SchemaTooNew { version: 17 }); user_version == 16 returns Ok with no mutation (belt-and-suspenders with the gate)
    - test_upgrade_skips_existing_columns: a v14-shaped fixture whose Location already has Specialty/Edition (but user_version 14) upgrades to 16 without a "duplicate column" error (D3-04). CONSTRUCT this state ad hoc: take generate_v16_fixture()'s extracted DB — which already has Specialty/Edition — open it, `conn.pragma_update(None, "user_version", 14)`, then call upgrade_to_v16 and assert Ok + user_version == 16.
    - test_upgrade_preserves_existing_specialty_edition (finding 1, HIGH): take the v16 fixture DB (has Specialty/Edition columns), set two Location rows' Specialty/Edition to KNOWN non-null values, `pragma_update(user_version, 14)`, call upgrade_to_v16, then assert those EXACT Specialty/Edition values survive on those LocationIds (assert the row values, not just success/version). This proves the conditional INSERT source-column list, not NULL,NULL.
    - test_upgrade_preserves_representative_location_types (finding 5): open a v14 fixture (generate_fixture_pre_v16_shape(14), which now seeds scripture Type0/BookNumber+Chapter+KeySymbol, publication/document Type0/DocumentId, media/track, Type1, Type2, Type3, and NULL-heavy rows). After upgrade assert Location row COUNT is unchanged AND each representative row's key fields (LocationId, Type, and its identifying column) survive — the rebuild INSERT..SELECT + CHECK/UNIQUE constraints on Location_new are actually exercised, not a single trivial row.
    - test_foreign_keys_off_around_rebuild (finding 6): assert PRAGMA foreign_keys reads 0 on the connection before upgrade (the default that makes DROP TABLE Location safe); do NOT enable FKs.
    - test_upgrade_failure_is_typed_error: induce a failure (e.g. Location table renamed away / poisoned pre-state) → Err(ArchiveError::SchemaUpgradeFailed { .. }), NEVER Ok (D3-02)
    - test_upgrade_rollback_leaves_original_version: after an induced failure, PRAGMA user_version is unchanged from its pre-call value and Location's row-set is normalized-identical to pre-call (D3-03)
    - test_post_upgrade_contract_rejects_incomplete_db (finding 2, HIGH): construct a deliberately-incomplete "v13-ish" DB (e.g. drop a required column or the IX_Location_Media index after a normal upgrade, or seed a fixture missing a required table) → validate_v16_contract returns Err (SchemaContractViolation or SchemaUpgradeFailed), and open_and_validate on such an archive is REJECTED with that typed error rather than stamped v16-accepted.
    - test_in_range_manifest_pragma_mismatch_normalizes (finding 4): a fixture whose manifest schemaVersion==14 but DB PRAGMA user_version==16 → Ok, ends at 16; and manifest==16 but DB==14 → Ok, upgraded, ends at 16. Both directions normalize to 16 (manifest schemaVersion derived from final PRAGMA on save).
    - test_source_file_unchanged_after_open: read_file_bytes(source) before vs after open_and_validate on a v14 fixture are identical (D3-06)
    - test_saved_upgraded_archive_reopen_round_trip (finding 8, strengthened D3-07): open v14 → save via the Phase 1 save path → RE-OPEN the saved archive and assert: PRAGMA user_version == 16, Location has Specialty/Edition columns AND IX_Location_Media, note count/content identical (normalized) to pre-save, and the saved manifest.json schemaVersion == 16.
  </behavior>
  <action>
    Create app/src-tauri/tests/schema_upgrade_tests.rs with `mod common;` and the behaviors above. These MUST fail/not-compile initially (upgrade_to_v16, validate_v16_contract, and the widened gate do not exist yet) — that is RED. Use `jwlmanager_lib::archive::open_and_validate` and, where a unit-level assertion must be induced, `jwlmanager_lib::archive::upgrade::{upgrade_to_v16, validate_v16_contract}` directly on a rusqlite Connection opened over an extracted fixture DB. Resolve resources via `jwlmanager_lib::db::resources::dev_resources_db_path`. Add `#![allow(clippy::unwrap_used, clippy::expect_used)]` (test-only). Assert error variants by matching the concrete `ArchiveError` variant, not string-matching. Do NOT include the Python differential row here — that is 03-03.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test schema_upgrade_tests 2>&1 | grep -Eq "error\[|test result: FAILED|cannot find" && echo RED-CONFIRMED</automated>
  </verify>
  <done>schema_upgrade_tests.rs exists encoding all behaviors incl. Specialty/Edition preservation, representative Location types, FK-off, post-upgrade contract, in-range mismatch, direct-call >v16, and reopen round-trip; suite is RED — RED-CONFIRMED printed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — transactional upgrade + contract validator + widen BOTH gates</name>
  <read_first>
    - JWLManager.py:1016-1070 (the EXACT DDL to port: two ALTER ADD COLUMN Specialty/Edition, CREATE Location_new with UNIQUE + 3 CHECK constraints, INSERT..SELECT, DROP Location, RENAME, 3 CREATE INDEX incl. UNIQUE IX_Location_Media over COALESCE(Specialty,'')/COALESCE(Edition,''), PRAGMA user_version=16 — lines 1071-1074 are the except:pass defect, DO NOT port; line 1063's NULL,NULL is the data-loss defect finding 1 fixes)
    - .planning/phases/03-schema-upgrade/03-RESEARCH.md (Pattern 1 upgrade_to_v16 + column_exists shape; Pitfall 3 ALTER-on-existing-column guard; Pitfall 4 user_version is transactional; Pitfall 5 foreign_keys stays OFF; Pitfall 6 do NOT set legacy_alter_table)
    - app/src-tauri/src/archive/mod.rs (whole file — the gate to widen and where the upgrade+validate call slots in, between the PRAGMA check and query_notes)
    - app/src-tauri/src/archive/manifest.rs (whole file — the SECOND v16-only gate at line 28/~109, finding 3, widen in lockstep)
    - app/src-tauri/src/error.rs (SchemaTooOld/SchemaTooNew/SchemaUpgradeFailed added in 03-01; add SchemaContractViolation here if chosen)
  </read_first>
  <action>
    Create app/src-tauri/src/archive/upgrade.rs exposing `pub fn upgrade_to_v16(conn: &mut rusqlite::Connection) -> Result<(), ArchiveError>` and `pub fn validate_v16_contract(conn: &rusqlite::Connection) -> Result<(), ArchiveError>`.

    upgrade_to_v16: read PRAGMA user_version; if == 16 return Ok (D3-05 no-op); if > 16 return Err(ArchiveError::SchemaTooNew { version }) (finding 7, belt-and-suspenders). Open `conn.transaction()`. Guard each ADD COLUMN with `column_exists(&tx, "Location", col)` via PRAGMA table_info (Specialty, Edition) as separate `tx.execute` calls so an already-present column is skipped (D3-04). CONDITIONAL SOURCE COLUMNS (finding 1, HIGH): build the INSERT..SELECT source list so Specialty's source is `Specialty` when column_exists(Location, Specialty) else literal `NULL`, and likewise for Edition — do NOT hardcode `NULL, NULL`. When the columns pre-existed with non-null values, those values are copied through; when they were just ADDed, the source is NULL. Then execute the CREATE Location_new (verbatim constraints from the Python DDL) / INSERT..SELECT / DROP / RENAME / 3 CREATE INDEX. Then `tx.pragma_update(None, "user_version", 16)`. `tx.commit()`. On ANY error inside, the Transaction guard rolls back on drop; map the rusqlite error into `ArchiveError::SchemaUpgradeFailed { reason }` (D3-02/D3-03) — never Ok, never a bare `?` surfacing a generic Sqlite error without upgrade context. Do NOT set PRAGMA foreign_keys or legacy_alter_table anywhere (finding 6: the rebuild relies on the default foreign_keys=OFF; assert-and-leave, never enable). Only static DDL (the sole `format!` is the hardcoded literal "Location" in column_exists — not user data, not a SAFE-02 issue). No unwrap/expect (clippy -D warnings). Register `pub mod upgrade;` in archive/mod.rs.

    validate_v16_contract (finding 2, HIGH): after upgrade and BEFORE the session is accepted, verify the working DB against a v16 contract — required tables present (Location + the tables Phase 1 already reads), Location has ALL v16 columns (incl. Specialty AND Edition via PRAGMA table_info), the UNIQUE index IX_Location_Media exists (PRAGMA index_list / sqlite_master), and PRAGMA user_version == 16. On ANY gap return a typed error — EITHER add `ArchiveError::SchemaContractViolation { reason: String }` (with to_dto arm + errors.ts case, mirroring SchemaUpgradeFailed's sanitization: reason stays internal) OR reuse `SchemaUpgradeFailed { reason }` — choose one and be consistent. NEVER silent acceptance. This is what makes an unknown v12/v13 gap fail LOUD instead of being stamped v16.

    Widen archive/mod.rs gate: replace `SUPPORTED_SCHEMA_VERSION` with MIN=12/MAX=16/WORKING=16; manifest schema_version < 12 → SchemaTooOld, > 16 → SchemaTooNew; open the conn as `let mut conn`; re-check PRAGMA user_version against the same bounds. IN-RANGE manifest/PRAGMA mismatch (finding 4): NORMALIZE — if both are in [12,16] but differ, proceed to upgrade the DB (do NOT reject); an out-of-range PRAGMA is its own SchemaTooOld/SchemaTooNew on the PRAGMA value. If pragma < 16 call `upgrade::upgrade_to_v16(&mut conn)?`; then ALWAYS call `upgrade::validate_v16_contract(&conn)?`; re-read PRAGMA user_version and set `ManifestMeta.schema_version` from that final value (must be 16) so a saved manifest reflects 16 regardless of what the manifest originally said (D3-07, finding 4).

    Widen archive/manifest.rs gate IN LOCKSTEP (finding 3): the manifest.rs `SUPPORTED_SCHEMA_VERSION=16` (line 28) and `check_schema_gate` (line ~109) accept 12-16, reject <=11 SchemaTooOld, reject >=17 SchemaTooNew — using the SAME constants as archive/mod.rs. Share ONE const/module (e.g. define MIN/MAX/WORKING once in archive/mod.rs and reference from manifest.rs, or a small shared submodule) so the two gates CANNOT drift. Do NOT leave two gates disagreeing.

    Document in this plan text (recorded, finding 4): the chosen behavior is NORMALIZE — in-range manifest/PRAGMA mismatches upgrade the DB and save derives the manifest schemaVersion from the final PRAGMA (always 16).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test schema_upgrade_tests</automated>
  </verify>
  <done>All schema_upgrade_tests behaviors green; upgrade_to_v16 transactional + idempotent + rejects direct >v16; pre-existing Specialty/Edition preserved (conditional source cols, no NULL,NULL loss); representative Location types survive; foreign_keys asserted 0; validate_v16_contract rejects incomplete DBs with a typed error; both mod.rs and manifest.rs gates share one range const and accept 12-16 / reject <=11 / >=17; in-range mismatch normalizes to 16; failed upgrade returns SchemaUpgradeFailed with DB left at original version; source file byte-identical; reopened saved archive reads 16 with columns/index/notes preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update manifest/validity/error suites (v14 now ACCEPTED) + retire placeholder + full green</name>
  <read_first>
    - app/src-tauri/tests/manifest_tests.rs (the existing test asserting a v14 archive is REJECTED — finding 3: must become "v14 accepted + upgraded", or move reject assertions to <=11 / >=17)
    - app/src-tauri/tests/archive_validity_tests.rs (same v14-rejection assertions to flip to v14-accepted)
    - app/src-tauri/tests/error_tests.rs (asserts UnsupportedSchema for v14 — update to SchemaTooOld/SchemaTooNew for <=11 / >=17; add SchemaContractViolation coverage if that variant was added)
    - app/src-tauri/src/error.rs (the UnsupportedSchema arm and its error.archive.unsupported_schema_phase3 message_key)
    - app/src/lib/errors.ts (the unsupported_schema case copy; add schema_contract_violation case if the variant was added)
    - .planning/phases/03-schema-upgrade/03-RESEARCH.md (retire the unsupported_schema_phase3 placeholder now that the real range gate + typed errors exist)
  </read_first>
  <action>
    Update manifest_tests.rs, archive_validity_tests.rs, and error_tests.rs so the EXISTING assertions that a v14 archive/manifest is REJECTED now assert it is ACCEPTED (and upgraded) — move the reject expectations to <=11 (SchemaTooOld) and >=17 (SchemaTooNew). Do NOT leave any test asserting v12-16 is rejected. If `UnsupportedSchema` is no longer produced by either gate after the widen (the range checks emit SchemaTooOld/SchemaTooNew), remove the variant, its to_dto arm, and the errors.ts `unsupported_schema` case — but ONLY if `grep` confirms no remaining producer in src/. If any caller still constructs it, update its errors.ts copy to the real range message and rename the message_key off the `_phase3` placeholder. If SchemaContractViolation was added in Task 2, add its errors.ts case and a to_dto arm (reason stays internal, never in the DTO). Then run the full suite. Fix any clippy findings on the upgrade path (no unwrap/expect).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && cd ../.. && cd app && npm test</automated>
  </verify>
  <done>manifest_tests/archive_validity_tests/error_tests updated so v14 is accepted+upgraded and only <=11 / >=17 are rejected; no test asserts v12-16 rejection; no `unsupported_schema_phase3` placeholder key remains; SchemaContractViolation (if added) has DTO + errors.ts copy with reason kept internal; full Rust suite + clippy + frontend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .jwlibrary manifest/DB → open gate | crafted schemaVersion / user_version / tampered Location rows cross here |
| upgrade transaction → working-copy DB | all-or-nothing mutation of the extracted temp DB |
| upgraded working DB → session acceptance | post-upgrade v16 contract must hold before Notes are trusted |
| source file on disk → open path | must remain read-only (D3-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | half-migrated DB via silent upgrade failure | mitigate | upgrade wrapped in rusqlite Transaction; any failure → rollback + SchemaUpgradeFailed (test_upgrade_failure_is_typed_error, test_upgrade_rollback_leaves_original_version) — the except:pass defect is explicitly NOT ported |
| T-03-04 | Tampering | crafted manifest schemaVersion vs actual PRAGMA user_version | mitigate | dual independent gate (mod.rs + manifest.rs) now share ONE range const; in-range mismatch normalizes to 16, out-of-range on either → typed reject (finding 3, 4) |
| T-03-05 | Tampering | malformed / representative Location rows violating rebuild CHECK/UNIQUE | mitigate | INSERT..SELECT into Location_new exercised with scripture/publication/media/Type1/Type2-3/NULL-heavy rows; a violation fails the constraints loudly → SchemaUpgradeFailed (finding 5) |
| T-03-06 | Tampering | source file mutated on open | mitigate | upgrade runs on extracted temp copy only; test_source_file_unchanged_after_open asserts byte-identity |
| T-03-07 | Elevation | foreign_keys/legacy_alter_table side effects on the DROP Location rebuild | mitigate | plan explicitly does NOT set either pragma AND asserts PRAGMA foreign_keys == 0 before the rebuild (finding 6); RESEARCH verified both default OFF |
| T-03-10 | Tampering | pre-existing Specialty/Edition silently overwritten with NULL (D3-04 path) | mitigate | conditional INSERT source columns copy existing values; test_upgrade_preserves_existing_specialty_edition asserts exact row values survive (finding 1) |
| T-03-11 | Spoofing | incomplete v12/v13 DB stamped user_version=16 and trusted as valid | mitigate | validate_v16_contract (required tables/columns/IX_Location_Media/user_version==16) rejects with a typed error before session acceptance (finding 2) |
| T-03-SC | Tampering | crate installs | accept | zero new crates (pure rusqlite DDL) |
</threat_model>

<verification>
`cd app/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test` then `cd app && npm test`. The v12/v13 coverage limit (VALIDATION "Known Coverage Limit") is recorded, not hidden: these fixtures prove the code path only; the post-upgrade contract validator (finding 2) is what makes an unknown real v12/v13 gap fail loud. Real-data verification for v14 is 03-03.
</verification>

<success_criteria>
v12-v16 open and normalize to v16; v11/v17 cleanly rejected with distinct typed errors from BOTH gates (mod.rs + manifest.rs sharing one range const); upgrade is transactional (failure → rollback + typed error, DB at original version) and rejects direct calls above v16; pre-existing Specialty/Edition data preserved (no NULL,NULL loss); representative Location types survive the rebuild; foreign_keys asserted OFF; post-upgrade v16 contract validated before acceptance (incomplete DB → typed reject); in-range manifest/PRAGMA mismatch normalizes to 16 both directions; source byte-identical after open; a saved-then-reopened upgraded archive reads user_version 16 with Specialty/Edition + IX_Location_Media and notes preserved; existing manifest/validity/error suites updated so v14 is accepted; clippy/full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-schema-upgrade/03-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
