---
phase: 03-schema-upgrade
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/src-tauri/src/archive/upgrade.rs
  - app/src-tauri/src/archive/mod.rs
  - app/src-tauri/tests/schema_upgrade_tests.rs
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "Opening a v12/v13/v14/v15/v16 fixture archive succeeds and the session reports schema_version 16"
    - "Opening a v11 fixture fails with a typed too-old error naming the version; a v17 fixture fails with a distinct too-new error"
    - "A failed upgrade surfaces as a typed error and leaves the working-copy DB at its ORIGINAL version — never half-migrated, never silently swallowed"
    - "The user's source file on disk is byte-identical after an upgrade-on-open"
    - "After upgrade, both PRAGMA user_version and session.manifest.schema_version read 16, and a saved upgraded archive's manifest schemaVersion is 16"
  artifacts:
    - path: "app/src-tauri/src/archive/upgrade.rs"
      provides: "upgrade_to_v16 transactional DDL port + column_exists guard"
      exports: ["upgrade_to_v16"]
      contains: "fn upgrade_to_v16"
    - path: "app/src-tauri/src/archive/mod.rs"
      provides: "range gate (accept 12-16, reject <=11 / >=17) + upgrade call on open"
      contains: "upgrade_to_v16"
    - path: "app/src-tauri/tests/schema_upgrade_tests.rs"
      provides: "full version + reject + rollback + idempotency + edge-case + source-unchanged + manifest-16 matrix"
  key_links:
    - from: "app/src-tauri/src/archive/mod.rs"
      to: "app/src-tauri/src/archive/upgrade.rs"
      via: "upgrade::upgrade_to_v16(&mut conn) after PRAGMA check when version < 16"
      pattern: "upgrade_to_v16"
    - from: "app/src-tauri/src/archive/upgrade.rs"
      to: "SQLite Location table"
      via: "transactional CREATE Location_new / INSERT..SELECT / DROP / RENAME + PRAGMA user_version = 16"
      pattern: "PRAGMA user_version"
---

<objective>
The vertical slice: a real-user v14 archive that Phase 1 rejected now opens, is upgraded to v16 in the working copy, and displays its Notes — while a v11 archive is cleanly refused. This ports JWLManager.py:1016-1070's DDL to a transactional Rust `upgrade_to_v16`, widens the open-time gate from v16-only to the 12-16 range with distinct out-of-range rejects, and proves the whole matrix (including the anti-`except:pass` failure test, transactional rollback, source-byte-identity, and manifest-reads-16) with a new test file.

Purpose: This is the reason the phase jumped the queue — 19 of the owner's 32 archives are v14 and were locked out. The core safety point (SAFE-05 / D3-02) is that a failed upgrade must fail LOUD, never leave a half-upgraded DB that later gets saved.
Output: New `archive/upgrade.rs`, widened `archive/mod.rs` gate, new `tests/schema_upgrade_tests.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-schema-upgrade/03-CONTEXT.md
@.planning/phases/03-schema-upgrade/03-RESEARCH.md
@.planning/phases/03-schema-upgrade/03-VALIDATION.md

<interfaces>
From app/src-tauri/src/archive/mod.rs (widen, do not rewrite wholesale):
  const SUPPORTED_SCHEMA_VERSION: i64 = 16;   // → replace with MIN=12 / MAX=16 / WORKING=16
  pub fn open_and_validate(path: &Path, resources_db_path: &Path)
      -> Result<(ArchiveSession, Vec<NotesRow>), ArchiveError>
  // current flow: extract → manifest schema_version check → open conn →
  //   PRAGMA user_version check (both == 16) → ResourceCatalog → query_notes →
  //   ArchiveSession { manifest: ManifestMeta { schema_version } }
  // conn is currently `let conn` (immutable); upgrade needs `let mut conn`.

From app/src-tauri/src/error.rs (added in 03-01):
  ArchiveError::SchemaTooOld { version: i64 }
  ArchiveError::SchemaTooNew { version: i64 }
  ArchiveError::SchemaUpgradeFailed { reason: String }

From app/src-tauri/tests/common/mod.rs (added in 03-01):
  generate_fixture_pre_v16_shape(version: i64) -> (TempDir, PathBuf)   // v11..v15
  // + v17 fixture generator + existing generate_v16_fixture()
  extract_to_tempdir, normalized_table_rows, read_file_bytes

The DDL source of truth (port the DDL, NOT the except:pass):
  JWLManager.py:1016-1070 — upgrade_schema
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — the schema-upgrade test matrix</name>
  <read_first>
    - .planning/phases/03-schema-upgrade/03-VALIDATION.md (Per-Task Verification Map — exact behaviors and the differential row deferred to 03-03)
    - app/src-tauri/tests/open_archive_tests.rs (how open_and_validate is driven in integration tests, resources_db_path via dev_resources_db_path)
    - app/src-tauri/tests/common/mod.rs (generators from 03-01: generate_fixture_pre_v16_shape, v17 fixture, normalized_table_rows, read_file_bytes)
    - app/src-tauri/tests/save_tests.rs (how a saved archive's manifest is read back — for the manifest-reads-16 assertion)
    - .planning/phases/03-schema-upgrade/03-CONTEXT.md (D3-02..D3-07 the assertions must encode)
  </read_first>
  <behavior>
    - test_gate_accepts_v12_through_v16: open each of v12,v13,v14,v15,v16 fixtures → Ok, session.manifest.schema_version == 16
    - test_gate_rejects_v11: open v11 fixture → Err(ArchiveError::SchemaTooOld { version: 11 })
    - test_gate_rejects_v17: open v17 fixture → Err(ArchiveError::SchemaTooNew { version: 17 })
    - test_upgrade_v14_to_v16: after open, extracted DB PRAGMA user_version == 16 AND Location has Specialty+Edition columns AND IX_Location_Media exists
    - test_upgrade_noop_on_v16: opening the v16 fixture leaves the DB at 16 with data intact (idempotent, D3-05)
    - test_upgrade_skips_existing_columns: a v14-shaped fixture whose Location already has Specialty/Edition (but user_version 14) upgrades to 16 without a "duplicate column" error (D3-04). CONSTRUCT this state ad hoc inside the test (no named generator produces it, since generate_fixture_pre_v16_shape drops the columns for v<16): take `generate_v16_fixture()`'s extracted DB — which already has Specialty/Edition — open it and issue a raw `PRAGMA user_version = 14` via `conn.pragma_update(None, "user_version", 14)`, then call `upgrade_to_v16` and assert it succeeds and PRAGMA user_version == 16. This exercises the table_info ADD-COLUMN guard directly.
    - test_upgrade_failure_is_typed_error: induce a failure (e.g. call upgrade_to_v16 on a connection whose Location table has been renamed away / a poisoned pre-state) → Err(ArchiveError::SchemaUpgradeFailed { .. }), NEVER Ok (D3-02)
    - test_upgrade_rollback_leaves_original_version: after an induced failure, PRAGMA user_version is unchanged from its pre-call value and Location's row-set is byte-for-byte (normalized) identical to pre-call (D3-03)
    - test_source_file_unchanged_after_open: read_file_bytes(source) before vs after open_and_validate on a v14 fixture are identical (D3-06)
    - test_saved_upgraded_archive_manifest_is_16: open v14 → save via the Phase 1 save path → re-read the saved manifest.json → schemaVersion == 16 (D3-07)
  </behavior>
  <action>
    Create app/src-tauri/tests/schema_upgrade_tests.rs with `mod common;` and the tests above. These MUST fail/not-compile initially (upgrade_to_v16 and the widened gate do not exist yet) — that is the RED state. Use `jwlmanager_lib::archive::open_and_validate` and, where a unit-level failure must be induced, `jwlmanager_lib::archive::upgrade::upgrade_to_v16` directly on a rusqlite Connection opened over an extracted fixture DB. Resolve resources via `jwlmanager_lib::db::resources::dev_resources_db_path`. Add `#![allow(clippy::unwrap_used, clippy::expect_used)]` (test-only). Assert error variants by matching the concrete `ArchiveError` variant, not string-matching. Do NOT include the Python differential row here — that is 03-03.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test schema_upgrade_tests 2>&1 | grep -Eq "error\[|test result: FAILED|cannot find" && echo RED-CONFIRMED</automated>
  </verify>
  <done>schema_upgrade_tests.rs exists encoding all ten behaviors; suite is RED (upgrade module/widened gate absent) — RED-CONFIRMED printed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — port the transactional upgrade + widen the gate</name>
  <read_first>
    - JWLManager.py:1016-1070 (the EXACT DDL to port: two ALTER ADD COLUMN Specialty/Edition, CREATE Location_new with UNIQUE + 3 CHECK constraints, INSERT..SELECT copying NULL into the two new cols, DROP Location, RENAME, 3 CREATE INDEX incl. UNIQUE IX_Location_Media over COALESCE(Specialty,'')/COALESCE(Edition,''), PRAGMA user_version=16 — lines 1071-1074 are the except:pass defect, DO NOT port)
    - .planning/phases/03-schema-upgrade/03-RESEARCH.md (Pattern 1 upgrade_to_v16 + column_exists shape; Pitfall 3 ALTER-on-existing-column guard; Pitfall 4 user_version is transactional; Pitfall 5 foreign_keys stays OFF — do NOT set it ON; Pitfall 6 do NOT set legacy_alter_table)
    - app/src-tauri/src/archive/mod.rs (whole file — the gate to widen and where the upgrade call slots in, between the PRAGMA check and query_notes)
    - app/src-tauri/src/error.rs (SchemaTooOld/SchemaTooNew/SchemaUpgradeFailed added in 03-01)
  </read_first>
  <action>
    Create app/src-tauri/src/archive/upgrade.rs exposing `pub fn upgrade_to_v16(conn: &mut rusqlite::Connection) -> Result<(), ArchiveError>`. Read PRAGMA user_version; if >= 16 return Ok (D3-05 no-op). Open `conn.transaction()`. Guard each ADD COLUMN with a `column_exists(&tx, "Location", col)` check via PRAGMA table_info (Specialty, Edition) issued as separate `tx.execute` calls so an already-present column is skipped (D3-04). Then `tx.execute_batch(...)` for the CREATE Location_new (verbatim constraints from the Python DDL) / INSERT..SELECT / DROP / RENAME / 3 CREATE INDEX. Then `tx.pragma_update(None, "user_version", 16)`. `tx.commit()`. On ANY error inside, the Transaction guard rolls back on drop; map the rusqlite error into `ArchiveError::SchemaUpgradeFailed { reason }` (D3-02/D3-03) — never Ok, never a bare `?` that would surface as a generic Sqlite error without the upgrade context. Do NOT set PRAGMA foreign_keys or legacy_alter_table anywhere. All DDL is static (the only `format!` is the hardcoded literal "Location" in column_exists — not user data, not a SAFE-02 issue). No unwrap/expect on this path (clippy -D warnings). Register the module: add `pub mod upgrade;` in archive/mod.rs. Widen the gate in open_and_validate: replace `SUPPORTED_SCHEMA_VERSION` with MIN=12/MAX=16/WORKING=16; manifest schema_version < 12 → SchemaTooOld, > 16 → SchemaTooNew; open the conn as `let mut conn`; re-check PRAGMA user_version against the same bounds (a manifest/PRAGMA disagreement outside the range is its own reject, per RESEARCH Open Question 2 — treat as SchemaTooOld/SchemaTooNew on the PRAGMA value); if pragma < 16 call `upgrade::upgrade_to_v16(&mut conn)?`; re-read PRAGMA user_version and set `ManifestMeta.schema_version` from that final value (must be 16) so a saved manifest reflects 16 (D3-07).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test schema_upgrade_tests</automated>
  </verify>
  <done>All ten schema_upgrade_tests behaviors green; upgrade_to_v16 is transactional and idempotent; gate accepts 12-16 and rejects <=11/>=17 with distinct typed errors; failed upgrade returns SchemaUpgradeFailed with the DB left at its original version; source file byte-identical after open; saved manifest reads 16.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the placeholder key + full-suite green</name>
  <read_first>
    - app/src-tauri/src/error.rs (the UnsupportedSchema arm and its error.archive.unsupported_schema_phase3 message_key)
    - app/src/lib/errors.ts (the unsupported_schema case copy to update now that the real range gate exists)
    - .planning/phases/03-schema-upgrade/03-RESEARCH.md (retire the unsupported_schema_phase3 placeholder now that too-old/too-new exist)
  </read_first>
  <action>
    If `UnsupportedSchema` is no longer produced by open_and_validate after the gate rewrite (the range checks now emit SchemaTooOld/SchemaTooNew instead), remove the variant and its to_dto arm and the errors.ts `unsupported_schema` case — but ONLY if `grep` confirms no remaining producer in src/. If any caller still constructs it (e.g. a manifest/PRAGMA mismatch path chose to reuse it), instead update its errors.ts copy to the real range message and rename the message_key off the `_phase3` placeholder. Then run the full suite. Fix any clippy findings on the upgrade path (no unwrap/expect).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && cd ../.. && cd app && npm test</automated>
  </verify>
  <done>No `unsupported_schema_phase3` placeholder key remains; full Rust suite + clippy + frontend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .jwlibrary manifest/DB → open gate | crafted schemaVersion / user_version / tampered Location rows cross here |
| upgrade transaction → working-copy DB | all-or-nothing mutation of the extracted temp DB |
| source file on disk → open path | must remain read-only (D3-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | half-migrated DB via silent upgrade failure | mitigate | upgrade wrapped in rusqlite Transaction; any failure → rollback + SchemaUpgradeFailed (test_upgrade_failure_is_typed_error, test_upgrade_rollback_leaves_original_version) — the except:pass defect is explicitly NOT ported |
| T-03-04 | Tampering | crafted manifest schemaVersion vs actual PRAGMA user_version | mitigate | dual independent check kept from Phase 1, now range-bounded; out-of-range on either → typed reject |
| T-03-05 | Tampering | malformed Location rows violating rebuild CHECK constraints | mitigate | INSERT..SELECT into Location_new fails the CHECK constraints loudly → SchemaUpgradeFailed (reject, do not sanitize) |
| T-03-06 | Tampering | source file mutated on open | mitigate | upgrade runs on extracted temp copy only; test_source_file_unchanged_after_open asserts byte-identity |
| T-03-07 | Elevation | foreign_keys/legacy_alter_table side effects on the DROP Location rebuild | accept | plan explicitly does NOT set either pragma; RESEARCH verified both default OFF and the rename shape is not exposed to the legacy hazard |
| T-03-SC | Tampering | crate installs | accept | zero new crates (pure rusqlite DDL) |
</threat_model>

<verification>
`cd app/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test` then `cd app && npm test`. The v12/v13 coverage limit (VALIDATION "Known Coverage Limit") is recorded, not hidden: these fixtures prove the code path only; real-data verification for v14 is 03-03.
</verification>

<success_criteria>
v12-v16 open and normalize to v16; v11/v17 cleanly rejected with distinct typed errors; upgrade is transactional (failure → rollback + typed error, DB at original version); source byte-identical after open; PRAGMA + manifest both read 16 post-upgrade and a saved upgraded archive's manifest schemaVersion is 16; clippy/full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-schema-upgrade/03-02-SUMMARY.md` when done.
</output>
