---
phase: 03-schema-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src-tauri/tests/common/mod.rs
  - app/src-tauri/tests/fixtures.rs
  - app/src-tauri/src/error.rs
  - app/src/lib/errors.ts
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "A versioned pre-v16 fixture (v11/v12/v13/v14/v15) and a v17 out-of-range fixture can be generated on demand, seeded from res/blank, never committed"
    - "The typed error surface distinguishes too-old (<=11), too-new (>=17), and upgrade-failed as three separate, actionable user messages"
  artifacts:
    - path: "app/src-tauri/tests/common/mod.rs"
      provides: "generate_fixture_pre_v16_shape(version) + v17 reject fixture generator"
      contains: "generate_fixture_pre_v16_shape"
    - path: "app/src-tauri/src/error.rs"
      provides: "SchemaTooOld / SchemaTooNew / SchemaUpgradeFailed variants + to_dto arms"
      contains: "SchemaUpgradeFailed"
    - path: "app/src/lib/errors.ts"
      provides: "actionable copy for schema_too_old / schema_too_new / schema_upgrade_failed codes"
      contains: "schema_too_old"
    - path: "app/src-tauri/tests/fixtures.rs"
      provides: "existence tests for the new versioned fixtures"
  key_links:
    - from: "app/src-tauri/src/error.rs"
      to: "app/src/lib/errors.ts"
      via: "stable snake_case code strings from to_dto"
      pattern: "schema_too_(old|new)"
---

<objective>
Lay the two shared foundations the rest of Phase 3 builds on: (1) extend the res/blank-seeded fixture generator to emit older-schema archives (v11-v15) plus a v17 out-of-range fixture, and (2) add the three new typed error variants (too-old, too-new, upgrade-failed) with their sanitized DTO codes and frontend copy. This plan touches no upgrade logic and no open path — it only makes the pieces the version tests and the widened gate will consume.

Purpose: The version test matrix in 03-02 cannot exist without versioned fixtures, and the widened gate cannot reject/report without the error variants. Isolating both here keeps 03-02's file ownership clean and gives it a green foundation to build against.
Output: Extended `tests/common/mod.rs`, new fixture existence tests, three `ArchiveError` variants + `to_dto` arms, three frontend error messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-schema-upgrade/03-CONTEXT.md
@.planning/phases/03-schema-upgrade/03-RESEARCH.md
@.planning/phases/03-schema-upgrade/03-VALIDATION.md

<interfaces>
From app/src-tauri/tests/common/mod.rs (extend, do not rewrite):
  fn seed_from_res_blank(dest_dir: &Path)                  // copies v16 userData.db + default_thumbnail.png
  fn insert_synthetic_notes(db_path: &Path)                // inserts Location (WITH Specialty/Edition), UserMark, 2 Notes, Tag, TagMap
  fn synthetic_manifest_json() -> String                   // HARDCODED schemaVersion 16 — must be parameterized for versioned fixtures
  pub fn generate_v16_fixture() -> (TempDir, PathBuf)      // existing; keep working unchanged

From app/src-tauri/src/error.rs:
  pub enum ArchiveError { NotAZip, MissingManifest, MissingUserDataBackup,
      UnsupportedSchema { version: i64 }, ZipSlipRejected, StatePoisoned,
      MissingResourcesLanguage, MissingResourcesDb, Io(..), Sqlite(..), Zip(..), Json(..) }
  impl ArchiveError { pub fn to_dto(&self, operation: &str, file: Option<&Path>) -> ErrorDto }
  // to_dto matches EVERY variant; adding variants REQUIRES new match arms or it will not compile.

From app/src/lib/errors.ts:
  export function describeError(err: ErrorDto): string      // switch on err.code, default arm exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the fixture generator for older-schema shapes</name>
  <read_first>
    - app/src-tauri/tests/common/mod.rs (whole file — the seed/insert/manifest/zip helpers being extended)
    - app/src-tauri/tests/fixtures.rs (existing generator assertions to mirror)
    - .planning/phases/03-schema-upgrade/03-RESEARCH.md (Pitfall 2 — name/doc the fixtures as "reverse-mutated v16 shape", NOT faithful v12/v13; the v14<->v16 delta is Specialty/Edition columns + IX_Location_Media index)
    - .planning/phases/03-schema-upgrade/03-CONTEXT.md (D3-10 synthetic-only, GDPR bright line; code_context note that res/blank is v16 so older shapes are produced by reverse-adjusting)
  </read_first>
  <action>
    Add `generate_fixture_pre_v16_shape(version: i64) -> (TempDir, PathBuf)` to tests/common/mod.rs, plus a thin wrapper set the tests can call by name. Reuse `seed_from_res_blank` and `insert_synthetic_notes` (which insert the v16-shaped Location), then REVERSE-MUTATE the seeded userData.db to the older shape: drop index IX_Location_Media, rebuild Location WITHOUT the Specialty and Edition columns (CREATE Location_old with the pre-v16 column set, INSERT..SELECT the non-Specialty/Edition columns, DROP + RENAME), then `PRAGMA user_version = <version>`. Parameterize `synthetic_manifest_json` (or add `synthetic_manifest_json_for(version)`) so the manifest's schemaVersion field equals `version` — a fixture whose manifest says 14 must carry a DB at user_version 14 (the gate cross-checks both). Also add a v17 (>16) fixture that keeps the v16 shape but sets user_version and manifest schemaVersion to 17 (for the too-new reject path). Doc-comment every new fn per RESEARCH Pitfall 2: these exercise the upgrade CODE PATH with the known delta reversed; they are NOT independently-verified v12/v13 archives. Keep `generate_v16_fixture` untouched. Test-only code — the existing `#![allow(clippy::unwrap_used, clippy::expect_used)]` at the top of the module covers these helpers.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test fixtures</automated>
  </verify>
  <done>generate_fixture_pre_v16_shape(v) produces an archive whose extracted userData.db has PRAGMA user_version == v, no Specialty/Edition columns on Location for v<16, and no IX_Location_Media; manifest schemaVersion == v; a v17 fixture generator also exists. res/blank and generate_v16_fixture unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture existence + shape tests</name>
  <read_first>
    - app/src-tauri/tests/fixtures.rs (whole file — mirror its assertion style and the git-tracking GDPR guard test)
    - app/src-tauri/tests/common/mod.rs (the generators added in Task 1)
  </read_first>
  <action>
    Add tests to fixtures.rs asserting each versioned fixture (v11, v12, v13, v14, v15) generates, extracts, and reports the expected PRAGMA user_version and older-schema shape (Location lacks Specialty/Edition for v<16; IX_Location_Media absent), and that the v17 fixture reports user_version 17. Confirm the existing `test_no_real_archive_is_tracked_in_git` GDPR guard still passes (do not weaken it). No new fixture files land in the repo — all under TempDir.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test fixtures</automated>
  </verify>
  <done>Fixture existence/shape tests green for v11-v15 + v17; GDPR git-tracking guard still green.</done>
</task>

<task type="auto">
  <name>Task 3: Typed error variants + DTO codes + frontend copy</name>
  <read_first>
    - app/src-tauri/src/error.rs (whole file — the enum, the to_dto match that covers every variant, the module docs on ErrorDto sanitization)
    - app/src/lib/errors.ts (whole file — the describeError switch and copywriting pattern: what happened + why + what to do)
    - .planning/phases/03-schema-upgrade/03-RESEARCH.md (Typed error additions section: variant names, distinct message_keys, retire the error.archive.unsupported_schema_phase3 placeholder)
    - .planning/phases/03-schema-upgrade/03-CONTEXT.md (D3-08 name the version found; D3-09 distinct "newer version" message)
  </read_first>
  <action>
    Add three variants to `ArchiveError`: `SchemaTooOld { version: i64 }`, `SchemaTooNew { version: i64 }`, `SchemaUpgradeFailed { reason: String }`, each with a `#[error(...)]` message. Add their `to_dto` match arms with distinct stable codes `schema_too_old` / `schema_too_new` / `schema_upgrade_failed` and message_keys `error.archive.schema_too_old` / `error.archive.schema_too_new` / `error.archive.schema_upgrade_failed`. Keep the existing `UnsupportedSchema` variant (still referenced by archive/mod.rs until 03-02 rewrites the gate) — do NOT remove it in this plan, but its `error.archive.unsupported_schema_phase3` key stays only until 03-02 retires it. `SchemaUpgradeFailed`'s `reason` is an internal detail and MUST NOT leak into the DTO — the DTO carries only the code + message_key, never the reason string (matches error.rs module docs: no wrapped Display in the DTO). In app/src/lib/errors.ts add three `case` arms with the actionable copy: too-old names the version and states the minimum supported (12) and that it can't be opened; too-new says the archive was made by a newer JW Library than this app supports; upgrade-failed says the upgrade could not be completed and the original file is unchanged. Update the existing `unsupported_schema` copy is NOT required here (left for 03-02's retirement).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo build && cargo test --test error_tests</automated>
  </verify>
  <done>error.rs compiles with the three new variants and exhaustive to_dto; codes schema_too_old/schema_too_new/schema_upgrade_failed emitted; errors.ts has matching cases; SchemaUpgradeFailed.reason never appears in ErrorDto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .jwlibrary manifest → gate | attacker-crafted schemaVersion crosses here (consumed by 03-02) |
| Rust core → Tauri IPC (ErrorDto) | sanitized error copy crosses to frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | SchemaUpgradeFailed.reason | mitigate | reason stays internal; to_dto emits only code + message_key, never the reason string (asserted in 03-02's error test) |
| T-03-02 | Repudiation | synthetic fixtures | mitigate | fixtures are TempDir-only; GDPR git-tracking guard test keeps any real/scrubbed .jwlibrary out of the repo |
| T-03-SC | Tampering | crate installs | accept | this plan adds zero new crates (RESEARCH: no new dependencies) |
</threat_model>

<verification>
`cd app/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test --test fixtures && cargo test --test error_tests && cargo build`
</verification>

<success_criteria>
Versioned pre-v16 fixtures (v11-v15) + v17 fixture generate correctly with matching manifest/PRAGMA versions; three new typed error variants exist with distinct sanitized codes and actionable frontend copy; no real archive tracked; clippy clean.
</success_criteria>

<output>
Create `.planning/phases/03-schema-upgrade/03-01-SUMMARY.md` when done.
</output>
