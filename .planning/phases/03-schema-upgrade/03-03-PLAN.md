---
phase: 03-schema-upgrade
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - app/src-tauri/tests/differential.rs
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "A synthetic v14 fixture, upgraded on open and saved through the Phase 1 save path, is accepted by the Python app's check_validity (ARCH-02 oracle extended)"
    - "The owner's real v14 archive (JWLM_REAL_ARCHIVE) opens, upgrades to v16, saves, and is accepted by check_validity — as a recorded, env-gated manual gate, never a silent CI skip"
  artifacts:
    - path: "app/src-tauri/tests/differential.rs"
      provides: "v14-upgrade differential oracle + env-gated real-v14 acceptance, both recorded manual gates"
      contains: "check_validity"
  key_links:
    - from: "app/src-tauri/tests/differential.rs"
      to: "JWLManager.py check_validity"
      via: "python3 subprocess against a Tauri-saved, upgraded archive"
      pattern: "ORACLE_RESULT:PASS"
---

<objective>
Close the phase against the reason it exists: prove an upgraded v14 archive is not just Rust-readable but accepted by the Python app / JW Library ecosystem. Extend the verified ARCH-02 differential oracle so an upgraded-on-open v14 fixture passes `check_validity`, and add/confirm the env-gated real-v14 path so the owner's actual iPad backups are the acceptance test — both as recorded manual gates (`#[ignore]`, run locally), never silently skipped in CI.

Purpose: Synthetic fixtures prove the code path; the owner's 19 real v14 files prove it against data JW Library actually produced (D3-11). v12/v13 remain synthetic-only by explicit, recorded limitation — this plan must NOT overclaim them.
Output: Extended `tests/differential.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-schema-upgrade/03-CONTEXT.md
@.planning/phases/03-schema-upgrade/03-VALIDATION.md

<interfaces>
From app/src-tauri/tests/differential.rs (extend, do not rewrite):
  fn repo_root() -> PathBuf
  #[test] #[ignore] fn python_app_opens_tauri_saved_archive()   // v16 fixture → save → python check_validity == PASS (VERIFIED PASSING 2026-07-20)
  #[test] fn <real-archive round-trip using JWLM_REAL_ARCHIVE>   // existing owner real-archive path, skipped (not failed) when env unset

  jwlmanager_lib::archive::open_and_validate
  jwlmanager_lib::archive::save::save_archive
  jwlmanager_lib::archive::new::save_as
  jwlmanager_lib::db::resources::dev_resources_db_path

From app/src-tauri/tests/common/mod.rs (03-01):
  generate_fixture_pre_v16_shape(version: i64) -> (TempDir, PathBuf)   // use version 14
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v14-upgrade differential oracle</name>
  <read_first>
    - app/src-tauri/tests/differential.rs (whole file — the python_app_opens_tauri_saved_archive pattern: generate fixture → open_and_validate → save_archive → shell python3 check_validity, the PATH/DLL staging comments, the exact #[ignore] reason + VERIFIED PASSING doc convention)
    - app/src-tauri/tests/common/mod.rs (generate_fixture_pre_v16_shape from 03-01)
    - .planning/phases/03-schema-upgrade/03-VALIDATION.md (the differential row: "Upgraded archive still accepted by the Python app's check_validity", command `cargo test --test differential -- --ignored`)
  </read_first>
  <action>
    Add a new `#[test] #[ignore = "..."]` fn (e.g. `python_app_opens_upgraded_v14_archive`) mirroring `python_app_opens_tauri_saved_archive`, but seeding from `generate_fixture_pre_v16_shape(14)` so the archive is upgraded from v14→v16 during `open_and_validate` before the save + Python `check_validity` handoff. Assert `session.manifest.schema_version == 16` before saving (proves the upgrade ran) and assert `ORACLE_RESULT:PASS`. Reuse the existing repo_root/PATH-staging/DLL comments verbatim in intent — do not duplicate the CRC helper or re-implement the python invocation from scratch; factor the shared python-oracle invocation into a small helper if it reduces duplication, otherwise copy the proven block. Keep the `#[ignore]` with a reason string stating this is a RECORDED MANUAL GATE (CI is Rust-only, no PySide6) and referencing `cargo test --test differential -- --ignored`. Do NOT mark it VERIFIED PASSING until a human actually runs it — leave a STATUS line noting it is pending the owner's local run.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test differential -- --list 2>&1 | grep -q python_app_opens_upgraded_v14 && echo REGISTERED</automated>
  </verify>
  <done>differential.rs has an ignored v14-upgrade oracle test asserting schema_version 16 pre-save and ORACLE_RESULT:PASS; recorded manual gate reason present; test compiles and is registered (does not run in the default suite).</done>
</task>

<task type="auto">
  <name>Task 2: Env-gated real-v14 acceptance path (recorded manual gate)</name>
  <read_first>
    - app/src-tauri/tests/differential.rs (the existing JWLM_REAL_ARCHIVE round-trip test — extend or add a sibling that also runs the Python check_validity assertion on the real upgraded output)
    - app/src-tauri/examples/roundtrip.rs (the owner-facing helper the manual gate uses; note it already opens read-only, upgrades on open once 03-02 lands, and writes a NEW file)
    - .planning/phases/03-schema-upgrade/03-CONTEXT.md (D3-11 real v14 is the acceptance test; never CI, never committed)
    - .planning/phases/03-schema-upgrade/03-VALIDATION.md (Manual-Only Verifications table — the exact roundtrip + check_validity commands)
  </read_first>
  <action>
    Ensure a real-v14 acceptance path exists as a recorded, env-gated manual gate: when `JWLM_REAL_ARCHIVE` is set, open_and_validate the real file (which now upgrades v14→v16 via 03-02), save it, and shell to python3 `check_validity` asserting acceptance — SKIPPED (not failed, `println!` + early return) when the env var is unset, so CI stays green without ever silently claiming the gate passed. If the existing real-archive test already saves but does not run check_validity, add the Python acceptance assertion behind the same env gate (guarded additionally by python3 availability, matching Task 1's tolerance). Add a doc comment recording this as the D3-11 acceptance gate and its exact run command from VALIDATION's Manual-Only table. Record in the SUMMARY (not asserted in code) the VALIDATION "Known Coverage Limit": v14-v16 are verified; v12/v13 are same-code-path synthetic-only.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test differential && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>Real-v14 env-gated acceptance path present (skips cleanly when JWLM_REAL_ARCHIVE unset, runs open→upgrade→save→check_validity when set); doc-comment records it as the D3-11 manual gate with its command; default `cargo test --test differential` passes (real + ignored oracle skipped, not failed); clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust-saved upgraded archive → Python check_validity | cross-ecosystem acceptance of what we wrote |
| owner's real v14 archive (irreplaceable PII) → test harness | must never be committed or fixture-generated (GDPR Art. 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Repudiation | oracle silently reported passing | mitigate | v14-upgrade oracle is #[ignore]d with an explicit RECORDED MANUAL GATE reason; not marked VERIFIED PASSING until a human runs it; real-v14 path SKIPS visibly (println) when env unset, never a false green |
| T-03-09 | Information Disclosure | real v14 archive leaking into repo/CI | mitigate | env-gated only; GDPR git-tracking guard (fixtures.rs) keeps any .jwlibrary out of git; never committed |
| T-03-SC | Tampering | crate installs | accept | zero new crates |
</threat_model>

<verification>
`cd app/src-tauri && cargo test --test differential && cargo clippy --all-targets -- -D warnings`. Recorded manual gates (owner-run): `cargo test --test differential -- --ignored` (with PySide6 + root-staged jwlCore/sqlite3 DLLs) and `JWLM_REAL_ARCHIVE=<real v14> cargo run --example roundtrip -- <in> <out>` then Python check_validity. v12/v13 remain synthetic-only per the recorded coverage limit — not overclaimed.
</verification>

<success_criteria>
An ignored v14-upgrade differential oracle asserts schema_version 16 + Python check_validity PASS; an env-gated real-v14 acceptance path runs open→upgrade→save→check_validity locally and skips visibly in CI; both are recorded manual gates, not silent skips; clippy/default differential suite green.
</success_criteria>

<output>
Create `.planning/phases/03-schema-upgrade/03-03-SUMMARY.md` when done.
</output>
