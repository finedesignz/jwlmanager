---
phase: 05-two-archive-merge
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - app/src/components/CommandBar.tsx
  - app/src/components/CommandBar.test.tsx
  - app/src/App.tsx
  - app/src-tauri/tests/differential.rs
  - app/src-tauri/tests/common/mod.rs
autonomous: true
requirements: [MERGE-02, MERGE-04]
must_haves:
  truths:
    - "A user can pick a second .jwlibrary and see a dry-run preview (add/overwrite/delete) before merging"
    - "Cancel commits nothing; Confirm calls merge_commit and refreshes"
    - "A busy/spinner state shows while the blocking merge is in flight; a MergeUnavailable error shows a clear actionable message"
    - "Rust-FFI merge of two synthetic fixtures matches the Python app's merge_databases on the same inputs by NORMALIZED table state"
  artifacts:
    - path: "app/src/components/CommandBar.tsx"
      provides: "Merge Archive action: pick source -> merge_dry_run -> reuse DeletePreviewDialog -> merge_commit"
      contains: "merge_dry_run"
    - path: "app/src-tauri/tests/differential.rs"
      provides: "Rust-FFI-vs-Python normalized-state parity leg (#[ignore]d) + arm64/missing actionable-error test"
      contains: "merge_databases"
  key_links:
    - from: "app/src/components/CommandBar.tsx"
      to: "merge_commit"
      via: "invoke on dialog Confirm"
      pattern: "invoke.*merge_commit"
    - from: "app/src-tauri/tests/differential.rs"
      to: "JWLManager.py merge_databases"
      via: "python3 shell-out on same fixtures, normalized comparison"
      pattern: "merge_databases"
---

<objective>
Wire the merge into the UI (pick a second archive → dry-run preview reusing the existing
`DeletePreviewDialog` → confirm/cancel → `merge_commit`, with a busy state and an actionable
MergeUnavailable message), and extend the differential oracle to PROVE parity: merge two
synthetic fixtures via the Rust FFI path AND via the Python app's `merge_databases`, comparing
NORMALIZED table state (never byte-diff). Add the arm64/missing-lib actionable-error test.

Purpose: deliver the full user-facing merge slice + the semantic parity proof (criterion 3) and
the "clear error not a crash" proof (criterion 4).
Output: merge action in CommandBar, its tests, the extended differential parity + unavailable leg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-two-archive-merge/05-CONTEXT.md
@.planning/phases/05-two-archive-merge/05-RESEARCH.md
@.planning/phases/05-two-archive-merge/05-02-SUMMARY.md

@app/src/components/CommandBar.tsx
@app/src/components/DeletePreviewDialog.tsx
@app/src/App.tsx
@app/src-tauri/tests/differential.rs
@app/src-tauri/tests/common/mod.rs

<interfaces>
Tauri commands (Wave 2):
  merge_dry_run({ sourcePath: string }) -> DryRunReport   // reuse the add/overwrite/delete report
  merge_commit({ sourcePath: string })  -> void
  // MergeUnavailable → ErrorDto { code: "merge_unavailable", message_key: "error.merge.unavailable" }
  // MergeFailed      → ErrorDto { code: "merge_failed",      message_key: "error.merge.failed" }

Reusable dialog (Phase 2/4) — app/src/components/DeletePreviewDialog.tsx:
  props: report, onConfirm: ()=>Promise<void>, onCancel, title, ariaLabel, summary?, confirmLabel, confirmPendingLabel
  // already used by the v14 preview flow in CommandBar — follow that exact pattern (V14Preview state)

Python oracle harness — app/src-tauri/tests/differential.rs:
  repo_root(); run_python_check_validity(...); #[ignore] convention + RECORDED MANUAL GATE.
  jwlcore.py:74-75 wrapper: merge_databases(path1_dest, path2_src, downgrade=False).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Archive action in CommandBar (pick -> preview -> confirm/cancel)</name>
  <files>app/src/components/CommandBar.tsx, app/src/components/CommandBar.test.tsx</files>
  <action>
  Add a "Merge Archive…" toolbar button (disabled when `!archiveOpen || anyPending`), following
  the EXACT `handleSaveV14` preview-then-write pattern already in this file:
    - `runAction("merge", ...)`: `open()` a `.jwlibrary` (FILTERS), dismiss = onCancelled();
      then `invoke<DryRunReport>("merge_dry_run", { sourcePath })`; on success stash a
      `MergePreview { report, sourcePath }` state; on error `onError(err as ErrorDto)` — a
      `merge_unavailable` DTO renders through the existing ErrorBanner message-key mapping
      (add/confirm the `error.merge.unavailable` + `error.merge.failed` copy in the frontend
      message map / errors.ts).
    - Render `<DeletePreviewDialog>` when `mergePreview` is set: report=preview.report,
      title="Merge this archive?", ariaLabel="Confirm merge", confirmLabel="Merge",
      confirmPendingLabel="Merging…", and a `summary` describing added/overwritten counts
      (e.g. "N records added, N updated from <file>"). onConfirm → `invoke("merge_commit",
      { sourcePath })` then `onSaved()` (refresh) in a finally that clears the preview; onCancel
      → clear preview (commits nothing). The dialog's own busy-ref gives the in-flight spinner
      state for the blocking call (D5-05); no live %.
    - Add a new ActionName "merge".
  Extend CommandBar.test.tsx: button disabled with no archive; open→dry-run→dialog shows counts;
  Confirm invokes merge_commit; Cancel invokes nothing; a merge_unavailable error surfaces the
  actionable banner. Mock `invoke`/`open` as the existing tests do.
  </action>
  <verify>
    <automated>cd app && npm test -- CommandBar 2>&1 | tail -20</automated>
  </verify>
  <done>Merge button present + gated; dry-run preview reuses DeletePreviewDialog; Confirm→merge_commit→refresh; Cancel commits nothing; unavailable/failed errors render actionable copy; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx wiring + merge action smoke</name>
  <files>app/src/App.tsx</files>
  <action>
  Wire whatever refresh callback the merge confirm needs (reuse the existing `onSaved`/reload
  path so the merged data re-renders — mirror how v14/save refresh the Notes list). Keep changes
  minimal: the merge action lives in CommandBar; App only provides the post-merge refresh hook if
  not already covered by `onSaved`. If `onSaved` already reloads, this task is a no-op verification
  that merge confirm triggers a re-render.
  </action>
  <verify>
    <automated>cd app && npm test -- App 2>&1 | tail -15</automated>
  </verify>
  <done>Post-merge refresh re-renders the archive view; App tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Differential parity oracle (Rust FFI vs Python) + arm64/missing test</name>
  <files>app/src-tauri/tests/differential.rs, app/src-tauri/tests/common/mod.rs</files>
  <action>
  Add the parity leg to differential.rs (MERGE-04), following the existing harness conventions
  (repo-root python3 shell-out, root-staged DLL prerequisites for the PYTHON leg only,
  `#[ignore]` with a RECORDED MANUAL GATE reason string matching the existing tests):
    `#[test] #[ignore = "..."] fn rust_ffi_merge_matches_python_merge()`:
      1. Build TWO synthetic v16 fixtures (reuse generate_merge_pair) — call them A (dest) + B
         (source), identical seed for both legs.
      2. RUST leg: materialize A into dest_root, B into dest_root/merge, run
         run_merge_with_lib_path(host_dll, dest_root, dest_root/merge, false); collect
         normalized_table_rows for every MERGE_SNAPSHOT_TABLES table from the merged DB.
      3. PYTHON leg: on FRESH copies of A + B laid out the same way, shell to python3 importing
         `jwlcore.merge_databases(dest_dir, dest_dir/merge, False)` (root-staged
         jwlCore-amd64.dll + sqlite3_64.dll next to jwlcore.py, per the existing v16 oracle
         prereqs); collect the same normalized rows from the Python-merged DB.
      4. ASSERT normalized table state EQUAL across the two legs — sorted rows / semantic keys,
         NEVER a byte-diff (VACUUM + jwlCore ID-densify diverge bytes legitimately, D5-10).
    Add a helper in common/mod.rs if needed to run `merge_databases` via a tiny python snippet
    (mirror run_python_check_validity's Command shape).

  Add a NON-ignored unit test (criterion 4) `merge_unavailable_is_actionable_not_a_crash`:
    simulate the no-binary host path — assert `merge_availability` for an unsupported (OS,ARCH)
    returns `ArchiveError::MergeUnavailable` whose `to_dto` yields code `merge_unavailable` and
    message_key `error.merge.unavailable`, and that NO panic/process-exit occurs. (Test the
    error mapping directly; do not require the DLL.)
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test differential merge 2>&1 | tail -15</automated>
  </verify>
  <done>Parity leg exists, #[ignore]d with the manual-gate reason, and (run manually with the Python env) proves Rust-FFI and Python merges yield equal NORMALIZED table state; the unavailable actionable-error test passes by default with no crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend file picker → merge_dry_run/merge_commit | user-chosen source path crosses IPC |
| test harness → python3 subprocess | dev-only parity oracle, gated behind #[ignore] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Spoofing/Info | MergeFailed reason leaking to UI | mitigate | frontend renders only message_key copy; reason never crosses IPC (Wave 1 to_dto) |
| T-05-10 | DoS | arm64/missing lib crashing the app | mitigate | merge_unavailable → actionable banner; unit test proves no panic (criterion 4) |
| T-05-11 | Tampering | false-green parity (byte-diff hides semantic drift) | mitigate | normalized-table comparison only, never byte-diff; Python leg #[ignore]d + RECORDED MANUAL GATE |
| T-05-SC | Tampering | package installs | accept | no new packages (frontend + tests use existing deps) |
</threat_model>

<verification>
- `npm test -- CommandBar` and `npm test -- App` green.
- `cargo test --test differential merge` — unavailable test green by default; parity leg #[ignore]d.
- RECORDED MANUAL GATE: `cargo test --test differential -- --ignored` on a machine with
  `res/requirements.txt` + root-staged DLLs proves Rust-FFI == Python normalized state.
- Phase gate: full `cargo test` + `npm test` green.
</verification>

<success_criteria>
User picks a second archive, sees an accurate dry-run preview, cancels or confirms; a merge on arm64/missing-lib shows a clear actionable error, not a crash; Rust-FFI merge matches the Python app's merge on the same fixtures by semantic round-trip.
</success_criteria>

<output>
Create `.planning/phases/05-two-archive-merge/05-03-SUMMARY.md` when done
</output>
