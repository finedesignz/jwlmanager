---
phase: 02-safe-delete
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/src-tauri/src/db/delete.rs
  - app/src-tauri/src/db/mod.rs
  - app/src-tauri/src/error.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/delete_tests.rs
  - app/src-tauri/tests/dryrun_tests.rs
  - app/src-tauri/tests/delete_roundtrip_tests.rs
autonomous: true
requirements: [EDIT-01, SAFE-01, SAFE-02, SAFE-03, SAFE-04, QA-02]
must_haves:
  truths:
    - "Deleting a selection removes exactly the targeted Note rows + their directly-owned links; orphans are then swept by trim on save (criterion 5, EDIT-01)"
    - "A dry-run returns per-table deleted counts (incl. orphan cascade) and leaves the working-copy DB byte-identical (criterion 1, SAFE-01)"
    - "An empty selection is rejected at IPC deserialization before any DB access — impossible by construction (criterion 4, SAFE-03)"
    - "A failure mid-delete leaves the archive unchanged via transaction rollback (criterion 3, SAFE-04)"
    - "A multi-table-orphan round-trip: delete → save(trim) → reopen equals the expected normalized table state, never byte equality (QA-02)"
  artifacts:
    - path: "app/src-tauri/src/db/delete.rs"
      provides: "NonEmptyNoteIds newtype, DryRunReport, delete_notes(tx,ids), dry_run_delete_notes(conn,ids)"
      contains: "struct NonEmptyNoteIds"
    - path: "app/src-tauri/tests/delete_tests.rs"
      provides: "EDIT-01/SAFE-02/SAFE-03/SAFE-04 coverage"
      exports: ["test_delete_notes_removes_selected_rows", "test_empty_selection_fails_deserialization", "test_delete_rollback_on_forced_failure"]
  key_links:
    - from: "app/src-tauri/src/lib.rs"
      to: "db::delete"
      via: "delete_notes_dry_run + delete_notes_apply Tauri commands"
      pattern: "delete_notes_dry_run|delete_notes_apply"
    - from: "app/src-tauri/src/db/delete.rs"
      to: "db::trim::trim_sweep"
      via: "dry-run + apply reuse the same sweep for accurate orphan counts"
      pattern: "trim_sweep"
---

<objective>
Build the delete backend: a `NonEmptyNoteIds` newtype that rejects `[]` at the IPC boundary (SAFE-03), a general `DryRunReport` computed inside a rolled-back transaction (SAFE-01), parameterized deletes (SAFE-02), transaction rollback (SAFE-04), and the two Tauri commands the frontend will drive. Delete Notes only; orphans are swept by Plan-01's trim on save (D2-05).

Purpose: EDIT-01 + the reusable safety net (dry-run / rollback) every later destructive phase inherits.
Output: `db/delete.rs`, two registered commands, and delete/dryrun/roundtrip test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-safe-delete/02-CONTEXT.md
@.planning/phases/02-safe-delete/02-RESEARCH.md
@.planning/phases/02-safe-delete/02-VALIDATION.md

<read_first>
- .planning/phases/02-safe-delete/02-01-SUMMARY.md — Plan-01 outputs: `db::trim::trim_sweep` (DML-only, per-table BTreeMap counts, reuse in dry-run + apply) and `trim_db`.
- app/src-tauri/src/db/trim.rs — `trim_sweep(tx)` signature/return, to reuse for accurate orphan counts.
- app/src-tauri/src/archive/upgrade.rs — transactional / FK-off / rollback-on-drop template.
- app/src-tauri/src/error.rs — add `DeleteFailed { reason }` (+ `to_dto`). Note `EmptySelection` is NOT needed as an ArchiveError: SAFE-03 is enforced by `NonEmptyNoteIds` failing serde deserialization, surfacing as a Tauri invoke rejection before the command body.
- app/src-tauri/src/lib.rs — command registration pattern (`open_archive`/`save_archive`), `SessionState = Mutex<Option<ArchiveSession>>`, `session.db_path`, `session.dirty`, `to_dto(operation, file)` mapping.
- app/src-tauri/src/db/notes.rs — Note table shape; delete targets `Note.NoteId` and the Note's own `UserMarkId`/`BlockRange` links.
- app/src-tauri/tests/common/mod.rs — `generate_trim_fixture` (added in Plan 01), `normalized_table_rows`, `extract_to_tempdir`, `read_file_bytes`.
- RESEARCH.md Code Examples — `NonEmptyNoteIds` `#[serde(try_from = "Vec<i64>")]` pattern; `params_from_iter` parameterized IN-clause (SAFE-02).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NonEmptyNoteIds + DryRunReport + delete/dry-run core in db/delete.rs</name>
  <files>app/src-tauri/src/db/delete.rs, app/src-tauri/src/db/mod.rs, app/src-tauri/src/error.rs</files>
  <behavior>
    - `NonEmptyNoteIds` deserializes a non-empty `Vec<i64>` successfully; deserializing `[]` returns a serde error (not an Ok wrapping an empty vec).
    - `dry_run_delete_notes` on `generate_trim_fixture` returns a `DryRunReport` whose `deleted` per-table map includes Note plus the swept orphans (UserMark, BlockRange, TagMap, Tag, Location) with `added`/`overwritten` = 0, and leaves the DB byte-identical (read the file bytes before/after — equal).
    - `delete_notes` inside a transaction removes exactly the selected Note rows + their directly-owned UserMark/BlockRange links, parameterized; an id list shaped like SQL injection (`"1); DROP TABLE Note;--"` is impossible since ids are i64, but a huge/odd i64 list binds safely and deletes nothing spurious).
  </behavior>
  <action>Create `db/delete.rs`, register `pub mod delete;` in `db/mod.rs`. Define `NonEmptyNoteIds(Vec<i64>)` with `#[serde(try_from = "Vec<i64>")]` + `ts_rs::TS` export, `TryFrom<Vec<i64>>` erroring on empty (RESEARCH Code Examples), and `iter()`/`len()`/`is_empty()` accessors. Define `DryRunReport { added: BTreeMap<String,usize>, overwritten: BTreeMap<String,usize>, deleted: BTreeMap<String,usize>, total_deleted: usize }` with `Serialize`+`ts_rs::TS` export to `../../src/bindings/DryRunReport.ts` — GENERAL shape (added/overwritten/deleted) so Phase 4/5 reuse it; for delete, added/overwritten stay empty. Implement `pub fn delete_notes(tx: &rusqlite::Transaction, ids: &NonEmptyNoteIds) -> Result<(), ArchiveError>`: build `?,?,..` placeholders from `ids.len()` (only the placeholder COUNT is dynamic — never the ids) and `tx.execute(sql, rusqlite::params_from_iter(ids.iter()))` to delete the Note's own BlockRange (via its UserMark), the Note's UserMark link, then the Note rows — all parameterized (SAFE-02). Implement `pub fn dry_run_delete_notes(conn: &mut rusqlite::Connection, ids: &NonEmptyNoteIds) -> Result<DryRunReport, ArchiveError>`: `PRAGMA foreign_keys = OFF` on conn, open a `Transaction`, call `delete_notes(&tx, ids)` capturing the Note-delete count, then `crate::db::trim::trim_sweep(&tx)?` to get accurate per-table orphan counts, assemble `DryRunReport`, and DROP the transaction WITHOUT `.commit()` (automatic ROLLBACK — RESEARCH Pattern 2). NEVER call VACUUM in the dry-run path (Pitfall 2). Add `ArchiveError::DeleteFailed { reason }` + `to_dto` arm (code `"delete_failed"`, key `"error.archive.delete_failed"`, generic copy).</action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test dryrun_tests && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>Types export TS bindings; `dry_run_delete_notes` yields a per-table `DryRunReport` and leaves the DB file byte-identical; `delete_notes` deletes exactly the selected rows + owned links, all parameterized; compiles under the unwrap/expect deny gate.</done>
</task>

<task type="auto">
  <name>Task 2: delete_notes_dry_run + delete_notes_apply Tauri commands</name>
  <files>app/src-tauri/src/lib.rs, app/src-tauri/tests/delete_tests.rs</files>
  <action>Add two `#[tauri::command]`s in `lib.rs`, registered in `generate_handler!`. `delete_notes_dry_run(ids: NonEmptyNoteIds, state) -> Result<DryRunReport, ErrorDto>`: lock session, open `conn` on `session.db_path`, call `db::delete::dry_run_delete_notes`, map errors via `to_dto("delete_notes_dry_run", Some(target))`. `delete_notes_apply(ids: NonEmptyNoteIds, state) -> Result<DryRunReport, ErrorDto>`: lock session (mut), `PRAGMA foreign_keys = OFF`, open a `Transaction`, `delete_notes(&tx, &ids)`, `tx.commit()`, set `session.dirty = true`; return the same-shaped report of what was directly deleted (orphan sweep happens on save, so apply's report reflects the direct delete — document this; the dry-run already showed the user the full effect). Because both command signatures take `NonEmptyNoteIds`, an `[]` payload fails deserialization at the IPC boundary before either body runs (SAFE-03). Add `delete_tests.rs`: `test_delete_notes_removes_selected_rows` (EDIT-01, via the core fn against an extracted fixture DB), `test_empty_selection_fails_deserialization` (SAFE-03 — assert `serde_json::from_str::<NonEmptyNoteIds>("[]")` is `Err`, and a non-empty array is `Ok`), `test_delete_sql_is_parameterized` (SAFE-02 — a grep-style source assertion + a behavioral test binding a large id list), and `test_delete_rollback_on_forced_failure` (SAFE-04 — induce a failure after the first delete inside a transaction, e.g. poison a table, assert `normalized_table_rows` unchanged, mirroring `test_upgrade_rollback_leaves_original_version`).</action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test delete_tests</automated>
  </verify>
  <done>Both commands registered and callable; empty selection rejected at deserialization; delete removes exactly the targeted rows; a forced mid-delete failure leaves the DB normalized-identical (rollback). SAFE-02/03/04 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: QA-02 multi-table-orphan round-trip semantic-equivalence</name>
  <files>app/src-tauri/tests/delete_roundtrip_tests.rs</files>
  <action>Add `test_delete_round_trip_semantic_equivalence`: open `generate_trim_fixture` into a session, apply `delete_notes` for the fixture's Note (NoteId 900), save (Plan-01 trim runs), reopen the saved archive, and assert the normalized state of every affected table (`Note`, `UserMark`, `BlockRange`, `TagMap`, `Tag`, `Location`) via `normalized_table_rows` equals a hand-computed expected post-state: NoteId 900 and its owned UserMark 900 / BlockRange gone, TagMapId 900 gone, TagId 900 removed (Type>0, now unused), Location 900 gone (referenced only by the deleted Note), TagId 901's positions re-densified to contiguous 0-based, and unrelated rows preserved. Assert NEVER by byte equality (trim+VACUUM make bytes non-deterministic — use the normalized harness only). Include an `#[ignore]`d differential leg asserting the saved archive is Python `check_validity`-accepted.</action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test delete_roundtrip_tests test_delete_round_trip_semantic_equivalence</automated>
  </verify>
  <done>The round-trip test passes on normalized table state across all multi-table orphans; no byte-diff assertion anywhere; the differential leg (when run) confirms Python acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frontend → Tauri command | untrusted `ids` payload crosses IPC into a DB-mutating command |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | SQL injection via IN-clause id list | mitigate | `params_from_iter` with `?` placeholders; only the placeholder COUNT is dynamic; ids are typed `i64`, never string-interpolated (SAFE-02) |
| T-02-05 | Tampering | empty/degenerate selection reaching the delete path | mitigate | `NonEmptyNoteIds` `#[serde(try_from)]` rejects `[]` at IPC deserialization before the command body (SAFE-03), not a disabled button alone |
| T-02-06 | Denial of integrity | partial delete committed on mid-op failure | mitigate | delete runs inside one `Transaction` that rolls back on `Err` (drop-default Rollback); `DeleteFailed` typed error (SAFE-04) |
| T-02-07 | Info disclosure | internal SQL/paths leaking in error DTO | mitigate | `DeleteFailed { reason }` never serialized into `ErrorDto`; only stable code + message_key cross IPC |
| T-02-SC | Tampering | npm/cargo installs | accept | zero new dependencies this phase |
</threat_model>

<verification>
- `cargo test --test dryrun_tests` — dry-run per-table report + byte-identical-after-dry-run (SAFE-01).
- `cargo test --test delete_tests` — EDIT-01, SAFE-02, SAFE-03, SAFE-04.
- `cargo test --test delete_roundtrip_tests` — QA-02 semantic round-trip.
- `cargo clippy --all-targets -- -D warnings` clean.
</verification>

<success_criteria>
Delete removes exactly the targeted Notes (criterion 5/EDIT-01); dry-run previews per-table deletes without mutating (criterion 1); empty selection impossible by construction (criterion 4); mid-op failure rolls back (criterion 3); parameterized SQL + semantic round-trip test exist (criterion 5/QA-02).
</success_criteria>

<output>
Create `.planning/phases/02-safe-delete/02-02-SUMMARY.md` when done.
</output>
