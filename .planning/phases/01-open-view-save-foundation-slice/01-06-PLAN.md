---
phase: 01-open-view-save-foundation-slice
plan: 06
type: execute
wave: 5
depends_on: ["01-01", "01-03", "01-04", "01-05", "01-07"]
files_modified:
  - app/src/App.tsx
  - app/src/components/CommandBar.tsx
  - app/src/components/ErrorBanner.tsx
  - app/src/components/JwlCoreNotice.tsx
  - app/src/lib/errors.ts
  - app/src/components/CommandBar.test.tsx
  - app/src/components/ErrorBanner.test.tsx
autonomous: true
# SPLIT NOTE (plan-checker BLOCKER 1): added 01-07 dep — the ErrorDto this plan maps to copy now
# originates in 01-07's error.rs (not the old 01-01). Wave stays 5 (deps top out at 01-05 wave 4).
# REVIEWS NOTE (codex): (a) the frontend maps the sanitized `ErrorDto { code, operation,
# safe_file_name, message_key }` — the ONLY error shape crossing IPC — not raw Rust errors
# (finding 6); (b) JwlCoreNotice consumes the unified `JwlCoreStatus { loaded, arch, version,
# reason }` from 01-03, showing the notice only when `loaded === false` on arm64-windows
# (finding 12); (c) the command bar has explicit pending state, cancel, and double-click guarding
# for Open/Save/Save-As (finding 15).
requirements: [SAFE-05, DATA-01]
must_haves:
  truths:
    - "User can trigger Open, New, Save, and Save As from a command bar wired to the IPC commands"
    - "Open/Save/Save-As have explicit pending state, are cancel-able, and are guarded against double-click (no duplicate concurrent invokes)"
    - "Every ErrorDto renders as a specific actionable sentence keyed by code/message_key, never a bare code or raw Display string"
    - "The zip-slip rejection shows a distinct security message, not a generic open failure"
    - "On arm64 Windows, an informational (non-red, dismissible) jwlCore capability notice appears when JwlCoreStatus.loaded === false; nowhere else"
  artifacts:
    - path: "app/src/components/CommandBar.tsx"
      provides: "Open/New/Save/Save As actions wired to IPC with pending/cancel/double-click guarding"
    - path: "app/src/components/ErrorBanner.tsx"
      provides: "ErrorDto -> sentence mapping, sticky for real errors"
    - path: "app/src/components/JwlCoreNotice.tsx"
      provides: "informational arm64 capability notice driven by JwlCoreStatus.loaded"
    - path: "app/src/lib/errors.ts"
      provides: "ErrorDto (code/message_key) -> copy mapping per UI-SPEC"
  key_links:
    - from: "app/src/components/CommandBar.tsx"
      to: "save_archive / new_archive / save_as / open_archive"
      via: "invoke"
      pattern: "invoke"
    - from: "app/src/components/ErrorBanner.tsx"
      to: "app/src/lib/errors.ts"
      via: "ErrorDto code lookup"
      pattern: "errors"
---

<objective>
Complete the UI surface: a command bar (Open/New/Save/Save As) wired to the IPC commands with explicit pending/cancel/double-click handling, an error surface that maps every sanitized `ErrorDto` to the specific actionable sentence from the UI-SPEC copywriting contract, and the informational jwlCore capability notice shown only when the unified `JwlCoreStatus.loaded` is false on arm64 Windows.

Purpose: SAFE-05 is only satisfied when errors actually reach the user with actionable context — this plan renders the sanitized ErrorDto the backend produces. Closes the Walking Skeleton into a usable, non-races open/view/save app.
Output: `CommandBar.tsx`, `ErrorBanner.tsx`, `JwlCoreNotice.tsx`, `lib/errors.ts`, component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md
@.planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Command bar (Open/New/Save/Save As) wired to IPC with pending/cancel/double-click guarding</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Component Inventory command bar — icon+label buttons, 44x44 hit area; Copywriting primary CTA "Open Archive"; Color/Typography tokens; pending/disabled state styling)
    - ~/.claude/design-preferences.md (dark-first tokens, spacing multiples of 4)
    - .planning/phases/01-open-view-save-foundation-slice/01-05-PLAN.md (command names: open_archive, save_archive, save_as, new_archive)
  </read_first>
  <action>
    Build `CommandBar.tsx` (shadcn Button + Lucide icons) with New / Open / Save / Save As actions at 44x44 hit areas, invoking the corresponding Tauri commands (`new_archive`, `open_archive`, `save_archive`, `save_as`) via the native file dialog for path selection where needed. Add explicit interaction discipline (finding 15): a `pending` state per action that disables ALL file actions while any invoke is in flight and shows a spinner/disabled affordance; DOUBLE-CLICK GUARD so a second click while pending is a no-op (no duplicate concurrent `open_archive`/`save_archive` invokes); and CANCEL for the native open/save dialog — if the user dismisses the OS file picker, treat it as a clean cancel (no error banner, return to idle). Wire it into `App.tsx` above the Notes list; disable Save/Save As when no archive is open. Keep the empty-state CTAs consistent with the command bar. Add `CommandBar.test.tsx` (vitest) asserting each button invokes the expected command name, that a rapid double-click fires the invoke only once (guard), and that a cancelled dialog produces no error banner.
  </action>
  <verify>
    <automated>bash -c 'cd app && npm test -- CommandBar 2>&1 | grep -qiE "pass|✓|Test Files"'</automated>
    <human-check>Run `npm run tauri dev`; use each toolbar action; double-click Open rapidly (confirm one open); cancel the file dialog (confirm no error); confirm open renders notes, save/save-as write files, new clears to an empty archive.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- CommandBar` passes: each action invokes the correct command name; double-click fires once; cancelled dialog -> no error.
    - Actions show a pending state and are disabled while an invoke is in flight; Save/Save As disabled with no archive open; button copy/hit-area matches UI-SPEC.
  </acceptance_criteria>
  <done>All four file actions are usable from the command bar, with pending state, cancel, and double-click guarding, wired to the backend.</done>
</task>

<task type="auto">
  <name>Task 2: ErrorDto surface + jwlCore capability notice (JwlCoreStatus-driven)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Copywriting Contract — error state pattern, zip-slip distinct message, jwlCore notice muted/dismissible/non-red; "errors are sticky" rule)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-14 typed errors serialized to frontend; D-13a arm64 capability is expected/known, not a fault)
    - .planning/phases/01-open-view-save-foundation-slice/01-07-PLAN.md (ErrorDto { code, operation, safe_file_name, message_key } — the ONLY error shape over IPC; ArchiveError stays internal)
    - .planning/phases/01-open-view-save-foundation-slice/01-03-PLAN.md (unified JwlCoreStatus { loaded, arch, version, reason } — the shape this notice consumes)
  </read_first>
  <action>
    Create `lib/errors.ts` mapping every `ErrorDto` `code`/`message_key` (NotAZip, MissingManifest, MissingUserDataBackup, UnsupportedSchema, ZipSlipRejected, Io, Sqlite; plus jwlCore-related keys) to the exact actionable sentence pattern from the UI-SPEC (what happened + why + what to do next) — keyed off the sanitized ErrorDto fields, never a raw Rust Display string (finding 6). The `UnsupportedSchema` copy references Phase-3 multi-version support (matches 01-07's v16-only gate). Build `ErrorBanner.tsx` (shadcn Alert) that is sticky/non-auto-dismiss for real open/save/zip-slip errors, with the zip-slip case rendering the distinct security-rejection copy (not the generic failure copy). Build `JwlCoreNotice.tsx` that calls `check_jwlcore` after mount and, ONLY when the returned `JwlCoreStatus.loaded === false` with the arm64 no-binary reason, renders the muted, dismissible, non-destructive capability notice ("Merge isn't available on this device...") — never shown when `loaded === true` (finding 12). Add `ErrorBanner.test.tsx` asserting the ErrorDto mapping (including zip-slip -> distinct copy), that no code renders a raw Rust Display string or bare code, and that the notice shows only for `loaded: false`.
  </action>
  <verify>
    <automated>bash -c 'cd app && npm test -- ErrorBanner 2>&1 | grep -qiE "pass|✓|Test Files"'</automated>
    <human-check>Trigger an open failure and the zip-slip fixture; confirm distinct sticky messages. On arm64 Windows confirm the muted jwlCore notice (loaded=false); on x64 confirm it never appears.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- ErrorBanner` passes: every ErrorDto code maps to a specific sentence; zip-slip copy is distinct; no raw code/Display string surfaces.
    - jwlCore notice appears only when `JwlCoreStatus.loaded === false` (arm64-windows no-binary), styled muted/dismissible (not red).
    - Real errors are sticky (not auto-dismissed).
  </acceptance_criteria>
  <done>Errors surface with actionable context from the sanitized ErrorDto (SAFE-05); the arm64 jwlCore gap is communicated as expected capability via the unified status, not a fault.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC ErrorDto payload -> webview | Sanitized typed errors (no raw paths/source internals) cross into the UI for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | ErrorBanner.tsx | mitigate | Renders only curated sentences keyed off ErrorDto code/message_key; the backend already stripped raw paths/SQL (01-07 ErrorDto); a test asserts no raw Display surfaces |
| T-06-02 | Repudiation | silent error swallow | mitigate | Every ErrorDto code has a mapped sticky message; SAFE-05 forbids silent failure — a test asserts full coverage |
| T-06-03 | Denial of Service | double-click / racing invokes | mitigate | Pending-state + double-click guard prevent duplicate concurrent save/open invokes that could race on the session/target file |
</threat_model>

<verification>
- `bash -c 'cd app && npm test'` green (CommandBar + ErrorBanner).
- `npm run tauri dev`: all actions usable with pending/cancel/double-click discipline; errors sticky + distinct; arm64 notice only when loaded=false.
</verification>

<success_criteria>
The app has a working command bar (with pending/cancel/double-click discipline) and an error surface where every sanitized ErrorDto is an actionable sentence; the arm64 jwlCore capability is communicated informationally via the unified JwlCoreStatus.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
