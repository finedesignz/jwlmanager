---
phase: 01-open-view-save-foundation-slice
plan: 06
type: execute
wave: 5
depends_on: ["01-01", "01-03", "01-04", "01-05", "01-07"]
files_modified:
  - app/src/App.tsx
  - app/src/components/CommandBar.tsx
  - app/src/components/ErrorBanner.tsx
  - app/src/components/JwlCoreNotice.tsx
  - app/src/lib/errors.ts
  - app/src/components/CommandBar.test.tsx
  - app/src/components/ErrorBanner.test.tsx
autonomous: true
# SPLIT NOTE (plan-checker BLOCKER 1): added 01-07 dep — the ArchiveError variants this plan maps
# to copy now originate in 01-07's error.rs (not the old 01-01). Wave stays 5 (deps top out at 01-05 wave 4).
requirements: [SAFE-05, DATA-01]
must_haves:
  truths:
    - "User can trigger Open, New, Save, and Save As from a command bar wired to the IPC commands"
    - "Every typed Rust error renders as a specific actionable sentence, never a bare code or raw Display string"
    - "The zip-slip rejection shows a distinct security message, not a generic open failure"
    - "On arm64 Windows, an informational (non-red, dismissible) jwlCore capability notice appears; nowhere else"
  artifacts:
    - path: "app/src/components/CommandBar.tsx"
      provides: "Open/New/Save/Save As actions wired to IPC"
    - path: "app/src/components/ErrorBanner.tsx"
      provides: "typed-error -> sentence mapping, sticky for real errors"
    - path: "app/src/components/JwlCoreNotice.tsx"
      provides: "informational arm64 capability notice"
    - path: "app/src/lib/errors.ts"
      provides: "discriminated-union error -> copy mapping per UI-SPEC"
  key_links:
    - from: "app/src/components/CommandBar.tsx"
      to: "save_archive / new_archive / save_as / open_archive"
      via: "invoke"
      pattern: "invoke"
    - from: "app/src/components/ErrorBanner.tsx"
      to: "app/src/lib/errors.ts"
      via: "error variant lookup"
      pattern: "errors"
---

<objective>
Complete the UI surface: a command bar (Open/New/Save/Save As) wired to the IPC commands, an error surface that maps every typed Rust error to the specific actionable sentence from the UI-SPEC copywriting contract, and the informational jwlCore capability notice shown only on arm64 Windows.

Purpose: SAFE-05 is only satisfied when errors actually reach the user with actionable context — this plan renders the discriminated-union errors the backend produces. Closes the Walking Skeleton into a usable open/view/save app.
Output: `CommandBar.tsx`, `ErrorBanner.tsx`, `JwlCoreNotice.tsx`, `lib/errors.ts`, component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md
@.planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Command bar (Open/New/Save/Save As) wired to IPC</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Component Inventory command bar — icon+label buttons, 44x44 hit area; Copywriting primary CTA "Open Archive"; Color/Typography tokens)
    - ~/.claude/design-preferences.md (dark-first tokens, spacing multiples of 4)
    - .planning/phases/01-open-view-save-foundation-slice/01-05-PLAN.md (command names: open_archive, save_archive, save_as, new_archive)
  </read_first>
  <action>
    Build `CommandBar.tsx` (shadcn Button + Lucide icons) with New / Open / Save / Save As actions at 44x44 hit areas, invoking the corresponding Tauri commands (`new_archive`, `open_archive`, `save_archive`, `save_as`) via the native file dialog for path selection where needed. Wire it into `App.tsx` above the Notes list; disable Save/Save As when no archive is open. Keep the empty-state CTAs consistent with the command bar. Add `CommandBar.test.tsx` (vitest) asserting each button invokes the expected command name.
  </action>
  <verify>
    <automated>cd app && npm test -- CommandBar 2>&1 | grep -qiE "pass|✓"</automated>
    <human-check>Run `npm run tauri dev`; use each toolbar action; confirm open renders notes, save/save-as write files, new clears to an empty archive.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- CommandBar` passes: each action invokes the correct command name.
    - Save/Save As disabled with no archive open; button copy/hit-area matches UI-SPEC.
  </acceptance_criteria>
  <done>All four file actions are usable from the command bar and wired to the backend.</done>
</task>

<task type="auto">
  <name>Task 2: Typed-error surface + jwlCore capability notice</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Copywriting Contract — error state pattern, zip-slip distinct message, jwlCore notice muted/dismissible/non-red; "errors are sticky" rule)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-14 typed errors serialized to frontend; D-13a arm64 capability is expected/known, not a fault)
    - .planning/phases/01-open-view-save-foundation-slice/01-07-PLAN.md + 01-03-PLAN.md (ArchiveError / JwlCoreError variant names — ArchiveError now lives in 01-07's error.rs)
  </read_first>
  <action>
    Create `lib/errors.ts` mapping every serialized error variant (NotAZip, MissingManifest, MissingUserDataBackup, SchemaTooOld, ZipSlipRejected, Io, Sqlite; JwlCoreError::NoArm64WindowsBinary, UnsupportedPlatform) to the exact actionable sentence pattern from the UI-SPEC (what happened + why + what to do next). Build `ErrorBanner.tsx` (shadcn Alert) that is sticky/non-auto-dismiss for real open/save/zip-slip errors, with the zip-slip case rendering the distinct security-rejection copy (not the generic failure copy). Build `JwlCoreNotice.tsx` that calls `check_jwlcore` after mount and, ONLY when it returns `NoArm64WindowsBinary`, renders the muted, dismissible, non-destructive capability notice ("Merge isn't available on this device...") — never shown when jwlCore loads. Add `ErrorBanner.test.tsx` asserting the mapping (including zip-slip -> distinct copy) and that no variant renders a raw Rust Display string or bare code.
  </action>
  <verify>
    <automated>cd app && npm test -- ErrorBanner 2>&1 | grep -qiE "pass|✓"</automated>
    <human-check>Trigger an open failure and the zip-slip fixture; confirm distinct sticky messages. On arm64 Windows confirm the muted jwlCore notice; on x64 confirm it never appears.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- ErrorBanner` passes: every error variant maps to a specific sentence; zip-slip copy is distinct; no raw code/Display string surfaces.
    - jwlCore notice appears only on the arm64-windows no-binary path, styled muted/dismissible (not red).
    - Real errors are sticky (not auto-dismissed).
  </acceptance_criteria>
  <done>Errors surface with actionable context (SAFE-05); arm64 jwlCore gap is communicated as expected capability, not a fault.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC error payload -> webview | Serialized typed errors cross into the UI for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | ErrorBanner.tsx | mitigate | Map to curated sentences; never render raw Rust Display (which could leak internal paths/SQL); asserted by test |
| T-06-02 | Repudiation | silent error swallow | mitigate | Every variant has a mapped sticky message; SAFE-05 forbids silent failure — a test asserts full coverage |
</threat_model>

<verification>
- `cd app && npm test` green (CommandBar + ErrorBanner).
- `npm run tauri dev`: all actions usable; errors sticky + distinct; arm64 notice only on arm64.
</verification>

<success_criteria>
The app has a working command bar and an error surface where every typed error is an actionable sentence; the arm64 jwlCore capability is communicated informationally.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-06-SUMMARY.md` when done.
</output>
</content>
