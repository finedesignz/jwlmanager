---
phase: 01-open-view-save-foundation-slice
plan: 07
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/src-tauri/src/error.rs
  - app/src-tauri/src/category.rs
  - app/src-tauri/src/archive/mod.rs
  - app/src-tauri/src/archive/extract.rs
  - app/src-tauri/src/db/mod.rs
  - app/src-tauri/src/db/notes.rs
  - app/src-tauri/src/main.rs
  - app/src-tauri/src/lib.rs
  - app/src/components/NotesList.tsx
autonomous: true
# SPLIT NOTE: security-sensitive core primitives extracted from the old 01-01 (plan-checker
# BLOCKER 1). Depends on 01-01's scaffold + Wave-0 harness (Cargo.toml deps, tauri.conf,
# bare main/lib, synthetic fixture, RED open_archive test). This plan turns that RED test green.
# manifest PARTITION: the skeleton does a MINIMAL inline validity check (zip has manifest.json +
# userDataBackup, schemaVersion > 11) inside archive/mod.rs. It does NOT create archive/manifest.rs.
# The full byte-compatible Manifest struct + strict check_validity + hash-last lives in 01-02
# (manifest.rs). No file overlap between this plan and 01-02.
requirements: [ARCH-01, ARCH-05, DATA-01, DATA-08, SAFE-05]
must_haves:
  truths:
    - "Opening the synthetic v16 fixture returns a NotesRow list with >= 1 real Note read from userData.db"
    - "A malformed / non-archive file produces a typed error, never a silent failure or panic"
    - "The Category enum round-trips to a ts-rs-generated TS type; no code path branches on a translated display string"
    - "`npm run tauri dev` renders at least one real Note row after opening a fixture (thin, not-yet-virtualized list)"
  artifacts:
    - path: "app/src-tauri/src/error.rs"
      provides: "thiserror ArchiveError enum serialized over IPC"
      contains: "thiserror"
    - path: "app/src-tauri/src/category.rs"
      provides: "Category enum with ts-rs derive"
      contains: "enum Category"
    - path: "app/src-tauri/src/archive/extract.rs"
      provides: "zip>=2.3 enclosed_name-validated extraction to temp dir (zip-slip safe)"
    - path: "app/src-tauri/src/archive/mod.rs"
      provides: "minimal skeleton validity check (manifest.json + userDataBackup + schemaVersion>11)"
    - path: "app/src-tauri/src/db/notes.rs"
      provides: "raw located-Note query over rusqlite (thin; independent-notes union + labels thicken in 01-04)"
    - path: "app/src/components/NotesList.tsx"
      provides: "thin (not-yet-virtualized) render of NotesRow[] from open_archive IPC"
  key_links:
    - from: "app/src/App.tsx"
      to: "open_archive Tauri command"
      via: "invoke('open_archive', {path})"
      pattern: "invoke.*open_archive"
    - from: "app/src-tauri/src/main.rs"
      to: "db::notes"
      via: "open_archive command calls extract then notes query"
      pattern: "notes::"
---

<objective>
Security-sensitive core primitives + the thinnest real end-to-end open path. Build the typed error surface, the category enum (ts-rs), zip-slip-safe extraction, a minimal validity check, a raw Notes query, register the `open_archive` Tauri command, and render at least one real Note row — turning 01-01's RED `open_archive` test green. These primitives every later plan builds on.

Purpose: Prove the riskiest integration (untrusted archive envelope + schema + SQLite + IPC + webview) works end-to-end with typed errors and the unwrap ban, in a plan reviewable in isolation from scaffold boilerplate (plan-checker BLOCKER 1 split).
Output: `error.rs`, `category.rs`, `archive/extract.rs` (+ minimal `archive/mod.rs` validity), `db/notes.rs` (thin), `open_archive` command wired in main.rs/lib.rs, and a thin `NotesList.tsx` render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/SKELETON.md
@.planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core primitives — typed errors, category enum (ts-rs), zip-slip-safe extract, minimal validity, raw Notes query</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Standard Stack table; Security Domain zip-slip row; State of the Art CVE-2025-29787; Package Legitimacy Audit)
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (extract.rs, notes.rs sections; Shared Patterns "Bare-except swallowing -> typed errors", "Temp-dir working copy", "Zip-slip validation via crate"; category.rs anti-pattern)
    - jwlcore.py (full — bridge shape reference; do not port the arch-blind bug here; jwlCore itself is 01-03)
    - JWLManager.py lines 694-767 (get_notes / load_independent — Phase 1 skeleton needs base located query only, no dupes/independent branch yet)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-11 category enum, D-14 typed errors, D-15 unwrap ban)
  </read_first>
  <behavior>
    - Opening the synthetic v16 fixture returns a NotesRow list with >= 1 row (located note present).
    - Opening a non-zip / manifest-less file returns Err(ArchiveError) with an actionable message, not a panic.
    - Category enum round-trips to a ts-rs-generated TS type; no code path branches on a translated display string.
    - A zip-slip entry (../ traversal) is refused by extraction (full crafted-fixture proof lands in 01-02).
  </behavior>
  <action>
    Add `error.rs` with a `thiserror` `ArchiveError` enum (variants: NotAZip, MissingManifest, MissingUserDataBackup, SchemaTooOld, Io, Sqlite, ZipSlipRejected) that derives Serialize so it crosses IPC (D-14). Add `category.rs` with `enum Category { Notes, Bookmarks, Favorites, Highlights, Annotations, Playlists }` deriving `ts-rs::TS` + serde, emitting a generated TS binding (DATA-08/D-11). Confirm the crate-level `#![deny(clippy::unwrap_used, clippy::expect_used)]` scaffolded in 01-01 is active (D-15). Add `archive/extract.rs` using the `zip` >=2.3.0 `ZipArchive::extract` into a `tempfile::TempDir` (enclosed_name-validated, ARCH-05). Add a MINIMAL `archive/mod.rs` validity gate (is-zip -> has manifest.json -> has userDataBackup -> schemaVersion > 11) returning typed rejections — this is the skeleton check; the full byte-compat Manifest struct is 01-02's manifest.rs (do NOT create manifest.rs here). Add `db/notes.rs` with the base located-Note query (Id, raw fields, ColorIndex, GROUP_CONCAT tags, Modified) over `rusqlite` — no resources.db labels and no independent-notes union yet (those thicken in 01-04).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo build && cargo clippy -- -D warnings && cargo test category_enum error_surface extract_rejects_traversal 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test error_surface` passes: a malformed input returns a typed ArchiveError variant with a non-empty message; no `unwrap`/`expect` on archive paths (clippy `-D warnings` green).
    - `cargo test category_enum` passes and a ts-rs generated `.ts` binding for Category exists.
    - `cargo test extract_rejects_traversal` passes: a `../` entry is refused (crafted multi-variant zip-slip proof is 01-02).
    - No fenced-in raw path joins; extraction goes through `zip` `extract`; no manifest.rs created here.
  </acceptance_criteria>
  <done>Typed errors, category enum, zip-slip-safe extract, minimal validity, and a raw Notes query in place under the unwrap ban.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register open_archive command + thin Notes render, turning the RED e2e test green under `tauri dev`</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Component Inventory Notes list row 44px; Color/Typography tokens; empty state already built in 01-01)
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (NotesList.tsx — greenfield, consumes NotesRow[] over IPC; open command shape)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-14 typed errors over IPC)
    - ~/.claude/design-preferences.md (dark-first tokens, spacing multiples of 4)
  </read_first>
  <behavior>
    - `open_archive(path) -> Result<Vec<NotesRow>, ArchiveError>` extracts then queries, returning >= 1 row for the fixture.
    - The RED `open_archive` integration test from 01-01 now passes.
    - Opening a fixture from the UI renders >= 1 real Note row; the empty state shows before opening.
  </behavior>
  <action>
    Register a `open_archive(path) -> Result<Vec<NotesRow>, ArchiveError>` Tauri command in the (bare) `main.rs`/`lib.rs` invoke_handler from 01-01 — extract to temp, run the minimal validity check, then the located-Note query. Wire `app/src/App.tsx`'s existing "Open Archive" CTA to a Tauri native file-open dialog (dialog plugin capability declared in 01-01, not raw JS path strings) that invokes `open_archive`. Build `app/src/components/NotesList.tsx` as a plain (NOT-yet-virtualized) list rendering `NotesRow[]` at fixed 44px rows using dark-first tokens — virtualization + labels + independent-notes are added in 01-04; here just prove real rows render. Turn 01-01's RED `open_archive` test green. Do NOT run a full `tauri build` bundle.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test open_archive 2>&1 | grep -q "test result: ok" && cd ../ && npm run build 2>&1 | grep -qiv "error TS"</automated>
    <human-check>Run `npm run tauri dev`, click Open Archive, select a generated fixture, confirm at least one Note row renders and the empty state shows before opening.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test open_archive` passes (fixture yields >= 1 NotesRow); 01-01's RED test is now green.
    - `npm run build` (frontend) compiles with no TypeScript errors.
    - Path selection goes through the Tauri native file dialog, not arbitrary JS strings.
    - Row height is fixed 44px; no full-bundle `tauri build` in verify.
  </acceptance_criteria>
  <done>Full stack runs: webview -> IPC -> Rust -> SQLite -> rendered Note rows via `tauri dev`; the Walking Skeleton is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file -> Rust core | An opened `.jwlibrary` is untrusted input (may be crafted); crosses into extraction/parse |
| webview (JS) -> Tauri IPC | Frontend command args (paths) cross into the trusted backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | archive/extract.rs | mitigate | `zip` >=2.3.0 `extract` with enclosed_name validation; a `../` entry is refused (full multi-variant zip-slip fixture + rejection test lands in 01-02) |
| T-07-02 | Tampering | archive/mod.rs minimal validity | mitigate | Typed rejections on missing manifest/userDataBackup/old schema; no loose `.get().unwrap_or(0)` reads |
| T-07-03 | Denial of Service | open_archive command | mitigate | Typed errors + no `unwrap`/`expect` (clippy `-D warnings`); malformed input cannot panic the app |
| T-07-04 | Tampering | webview path arg | mitigate | Path selection via Tauri native file dialog, not arbitrary JS strings; narrow tauri.conf capabilities (declared in 01-01) |
</threat_model>

<verification>
- `cd app/src-tauri && cargo fmt --check && cargo clippy -- -D warnings && cargo test` green (category, error, extract, open_archive).
- `cd app && npm run build` green; `npm run tauri dev` boots and shows real Notes from a fixture.
</verification>

<success_criteria>
Walking Skeleton runs end-to-end: a synthetic fixture opens and its real Notes render in the window via `npm run tauri dev`, with typed errors and the unwrap ban enforced; 01-01's RED test is green.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-07-SUMMARY.md` when done.
</output>
</content>
