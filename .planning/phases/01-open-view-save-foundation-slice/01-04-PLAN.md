---
phase: 01-open-view-save-foundation-slice
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - app/src-tauri/src/db/resources.rs
  - app/src-tauri/src/db/notes.rs
  - app/src-tauri/src/db/mod.rs
  - app/src-tauri/tauri.conf.json
  - app/src-tauri/tests/notes_query_tests.rs
  - app/src-tauri/tests/fixtures.rs
  - app/src/components/NotesList.tsx
  - app/src/components/NotesList.test.tsx
autonomous: true
requirements: [DATA-01, ARCH-01]
must_haves:
  truths:
    - "Independent notes (LocationId IS NULL) appear in the list alongside located notes"
    - "Note rows show synthesized human-readable labels (language + publication/bible reference) via resources.db"
    - "The 9,000-row Notes list renders far fewer DOM nodes than rows (virtualized)"
  artifacts:
    - path: "app/src-tauri/src/db/resources.rs"
      provides: "bundled resources.db label lookups (Languages/BibleBooks/Publications)"
    - path: "app/src-tauri/src/db/notes.rs"
      provides: "located + independent-notes UNION query with process_code/process_detail label synthesis"
      contains: "LocationId IS NULL"
    - path: "app/src/components/NotesList.tsx"
      provides: "TanStack Virtual windowed list, 44px rows"
      contains: "useVirtualizer"
  key_links:
    - from: "app/src-tauri/src/db/notes.rs"
      to: "db::resources"
      via: "label synthesis per row"
      pattern: "resources::"
    - from: "app/src/components/NotesList.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer"
      pattern: "useVirtualizer"
---

<objective>
Make the Notes view real and complete: bundle and query `res/resources.db` to synthesize the human-readable labels the Python app shows, add the independent-notes UNION query so standalone notes are not silently lost, port `process_code`/`process_detail` exactly, and virtualize the list with TanStack Virtual so 9,000+ rows stay responsive (DATA-01's one hard perf criterion, verified on Linux WebKitGTK).

Purpose: The skeleton showed raw rows; this delivers the real user-visible Notes list with correct labels, no data loss, and the virtualization the phase's success criterion 1 requires.
Output: `db/resources.rs`, thickened `db/notes.rs`, virtualized `NotesList.tsx` + vitest node-count assertion, 9k-row fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle resources.db + cached label lookups (Languages / BibleBooks / Publications)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (db/resources.rs section — read_resources JWLManager.py:4023-4053; f-string SQL anti-pattern, MUST parameterize; cache in OnceCell not module globals)
    - JWLManager.py lines 4023-4056 (read_resources / load_languages / load_bible_books; publications + extras DataFrames)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Pitfall 3 resources.db not optional; bundle as Tauri resource, 335 KB)
  </read_first>
  <action>
    Copy `res/resources.db` into the app resources dir and declare it in `tauri.conf.json` bundle resources. Create `db/resources.rs` that opens the bundled resources.db read-only via `rusqlite` and loads, once (cached in a `OnceCell`/struct), `lang_name` (Languages: Language->Name, plus Symbol), `bible_books` (BibleBooks for the UI language: Number->Name), and a `publications` map (Publications JOIN Types + Extras JOIN Types: Symbol -> {Short, Full, Year, Type}). All queries use `rusqlite` bound parameters — NEVER f-string/format interpolation (CLAUDE.md constraint; the Python `WHERE Language = {ui_lang}` is the anti-pattern to fix). Expose lookup helpers used by notes.rs.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test resources_lookups 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test resources_lookups` passes: language name, a bible book name, and a publication symbol resolve from the bundled resources.db.
    - All resources.db SQL uses bound parameters (no string interpolation).
    - resources.db is declared as a bundled Tauri resource.
  </acceptance_criteria>
  <done>resources.db bundled and queried; label lookup maps cached and parameterized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Independent-notes UNION query + process_code/process_detail label synthesis</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (db/notes.rs section — two-query union JWLManager.py:696-704,762-766; main query 751-757; process_code/process_detail 578-627 full excerpt; note code_jwb/code_yr regex constants must be grepped from JWLManager.py)
    - JWLManager.py lines 578-627 (process_code, process_detail) and 694-767 (get_notes, load_independent) and grep for `code_jwb =` / `code_yr =`
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Pitfall 2 independent-notes; A5 process_code/detail need direct read)
  </read_first>
  <behavior>
    - A fixture with N located + M independent notes returns N+M rows (independent notes not dropped).
    - process_code returns the correct (code, year) for known tuples (e.g. ws/issue=0 -> 'ws-'; dated symbol -> stripped prefix + 19xx/20xx year).
    - process_detail returns correct (detail1, year, detail2) for a bible reference (book+chapter) vs a publication issue.
  </behavior>
  <action>
    Thicken `db/notes.rs`: keep the located-note base query and ADD the separate independent-notes query (`WHERE n.BlockType = 0 AND LocationId IS NULL`), UNION-ing both result sets (mirror the Python concat order: independent then located). Port `process_code` and `process_detail` as pure Rust functions (grep and port the `code_jwb`/`code_yr` regex constants from JWLManager.py) plus a `process_color` and language-name resolution using `db::resources`. Build the final `NotesRow { id, language, code, color, tags, snippet/modified, year, detail1, detail2 }` exactly as JWLManager.py:756 assembles it (fallbacks: `* OTHER *`, `* NO TAG *`). Wire this into the `open_archive` command output. Add unit tests for process_code/process_detail against known tuples and an integration test proving independent notes appear.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test notes_query_includes_independent process_code process_detail 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test notes_query_includes_independent` passes: independent + located counts both present.
    - `cargo test process_code process_detail` pass against known input tuples.
    - Labels are synthesized (language name + code/detail), not raw IDs.
  </acceptance_criteria>
  <done>All notes (located + independent) render with correct synthesized labels; no silent data loss.</done>
</task>

<task type="auto">
  <name>Task 3: Virtualize the Notes list (TanStack Virtual) + node-count assertion at 9k rows</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Notes list row 44px fixed estimateSize; single flat virtualized list, not a data-table; tag-chip pattern; tokens)
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (NotesList.tsx greenfield — @tanstack/react-virtual v3)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (D-10 virtualization; DATA-01 9k rows; Don't Hand-Roll list virtualization)
  </read_first>
  <action>
    Rebuild `app/src/components/NotesList.tsx` using `@tanstack/react-virtual` `useVirtualizer` with a fixed `estimateSize` of 44 (uniform row height — the perf-relevant constant), rendering resolved label + truncated snippet + tag chips. Add a 9,000-row fixture (extend fixtures.rs or a JS test fixture) and `NotesList.test.tsx` (vitest) asserting that with 9,000 rows the number of rendered row DOM nodes is far below the row count (e.g. < 100). Do not render 9k DOM nodes.
  </action>
  <verify>
    <automated>cd app && npm test -- NotesList 2>&1 | grep -qiE "pass|✓"</automated>
    <human-check>On Linux (WebKitGTK), load the 9k-row fixture and scroll the full list; confirm no visible stutter (manual per VALIDATION — frame timing is not headless-assertable).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- NotesList` passes: rendered row nodes ≪ 9,000 (virtualized).
    - `estimateSize` returns fixed 44px; rows show synthesized labels + tag chips.
    - Manual Linux scroll-smoothness check recorded (manual-only per VALIDATION).
  </acceptance_criteria>
  <done>Notes list is virtualized and responsive at 9k+ rows, verified by DOM-node assertion + manual Linux check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| userData.db -> Rust query | Untrusted archive DB content is read into label synthesis |
| resources.db (bundled) -> Rust | Trusted bundled lookup DB, read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | db/resources.rs SQL | mitigate | `rusqlite` bound parameters only; fixes Python f-string `WHERE Language = {ui_lang}` interpolation |
| T-04-02 | Denial of Service | db/notes.rs on large archive | mitigate | Query streamed into Vec; frontend virtualizes (no 9k DOM nodes); no `unwrap` on row parse |
| T-04-03 | Information Disclosure | NotesRow snippet | accept | Note content is the user's own local data shown back to them; stays in-process, never transmitted |
</threat_model>

<verification>
- `cd app/src-tauri && cargo clippy -- -D warnings && cargo test` green (resources, notes, process_code/detail).
- `cd app && npm test -- NotesList` green; manual Linux scroll check recorded.
</verification>

<success_criteria>
The Notes list shows real synthesized labels for all notes (located + independent) and stays responsive at 9,000+ rows via virtualization.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-04-SUMMARY.md` when done.
</output>
