---
phase: 01-open-view-save-foundation-slice
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02"]
files_modified:
  - app/src-tauri/src/archive/save.rs
  - app/src-tauri/src/archive/new.rs
  - app/src-tauri/src/archive/mod.rs
  - app/src-tauri/src/main.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/save_tests.rs
  - app/src-tauri/tests/differential.rs
autonomous: true
requirements: [ARCH-02, ARCH-06, ARCH-07, QA-01]
must_haves:
  truths:
    - "Saving rebuilds the archive via a sibling temp file + atomic rename; a source archive is never mutated in place"
    - "The Python app (JWLManager.py) opens an archive the Tauri app saved, without error"
    - "Save-as writes to a chosen path and leaves the original file bytes unchanged; the session follows the new path"
    - "A new empty v16 archive can be created and opened by both apps"
  artifacts:
    - path: "app/src-tauri/src/archive/save.rs"
      provides: "atomic save (temp + rename), update_manifest hash-last, zip rebuild"
      contains: "rename"
    - path: "app/src-tauri/src/archive/new.rs"
      provides: "create new empty v16 archive"
    - path: "app/src-tauri/tests/differential.rs"
      provides: "Python-app-opens-Tauri-save oracle test"
      contains: "JWLManager.py"
  key_links:
    - from: "app/src-tauri/src/archive/save.rs"
      to: "archive::manifest::compute_hash"
      via: "hash computed last, before zip"
      pattern: "compute_hash"
    - from: "app/src-tauri/tests/differential.rs"
      to: "python3 JWLManager.py"
      via: "subprocess reopen assertion"
      pattern: "python3"
---

<objective>
Deliver the persistence slice: save (atomic temp-write + rename, manifest hash-last), save-as (chosen path, original untouched, session follows new path), and new empty v16 archive — then prove ARCH-02 by having the Python app reopen what the Tauri app saved (the differential oracle from D-01).

Purpose: Save is where Core Value is won or lost; atomic rename + read-only source guarantee a crash can never corrupt the opened file. The Python-app cross-check is ARCH-02's real proof.
Output: `archive/save.rs`, `archive/new.rs`, save/save-as/new Tauri commands, save round-trip tests + the differential test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/research/FUNCTIONALITY-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic save (sibling temp + rename), update_manifest hash-last, zip rebuild</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (archive/save.rs — save_file/save_as_file JWLManager.py:1121-1150; update_manifest hash-last 1152-1170; Shared Patterns "Manifest hash-last ordering")
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Code Examples atomic save write-temp-then-rename D-04, same-filesystem caveat; Pitfall 1 hash timing)
    - .planning/research/FUNCTIONALITY-SPEC.md (save sequence; LastModified UPDATE before hash; NO trim_db in Phase 1 — ARCH-04 is Phase 2)
  </read_first>
  <behavior>
    - Saving writes to a sibling temp file in the target's directory, then renames over the target atomically.
    - update_manifest touches LastModified, reads schemaVersion via PRAGMA user_version, then computes hash LAST over the final DB bytes.
    - Re-opening a saved archive with the Tauri core yields the same normalized Notes (semantic round-trip, not byte).
  </behavior>
  <action>
    Create `archive/save.rs` implementing `save_archive(target_path)`: run the manifest update sequence in the correct order — write DB mutations (LastModified UPDATE), close/flush the rusqlite handle, read final userData.db bytes, `compute_hash` (sha2) LAST, serialize the compact manifest, rebuild the zip (userData.db + manifest.json + media) into a sibling temp file in the SAME directory as the target, then `fs::rename` over the target (atomic on same filesystem, D-04). Do NOT implement trim_db/VACUUM (ARCH-04 is Phase 2). Add `tests/save_tests.rs` asserting: (a) save produces a re-openable archive with semantically equal Notes; (b) the temp file is gone and only the target remains; (c) hash equals sha256 of the on-disk userData.db after mutation.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test roundtrip_reopens save_atomic_rename hash_last 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test roundtrip_reopens` passes: saved archive reopens with semantically equal normalized Note tables.
    - `cargo test hash_last` passes: manifest hash == sha256 of final on-disk DB bytes.
    - Save uses sibling-temp + `fs::rename`; no in-place write of the source.
  </acceptance_criteria>
  <done>Save is atomic and hash-last; a re-open round-trips semantically.</done>
</task>

<task type="auto">
  <name>Task 2: New empty v16 archive + save-as (original untouched, session follows new path)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (archive/save.rs save_as_file D-05 session-follows-new-path; fixture_gen res/blank shape; new_file JWLManager.py:965-991)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-03 source read-only, D-04 atomic rename, D-05 save-as follows new file, A4 res/blank as v16 seed)
    - res/blank (extract + inspect the userData.db shape before generating a new one)
  </read_first>
  <action>
    Create `archive/new.rs` implementing `new_archive()`: build a fresh empty v16 archive in a session temp dir — a seeded empty v16 `userData.db` (reuse the fixture generator's v16 schema builder) plus a compact `manifest.json` via the `Manifest` struct (name=APP, version 1, type 0, schemaVersion 16, empty hash until first save). Implement `save_as(target_path)`: write to the chosen path via the atomic save from Task 1, leave the original source file's bytes unchanged (source was opened read-only into a temp dir per D-03), and update the session's working identity to the NEW path (D-05). Add tests: new archive opens with zero notes and valid manifest; save-as to a new path leaves an original fixture's bytes byte-identical (assert original file hash unchanged) while the new file opens cleanly.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test new_empty_archive save_as_preserves_original 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test new_empty_archive` passes: a new archive opens with a valid v16 manifest and zero Notes.
    - `cargo test save_as_preserves_original` passes: original file bytes unchanged after save-as; new file opens cleanly; session path updated to new.
  </acceptance_criteria>
  <done>User can create a new empty archive and save-as without mutating the original working copy.</done>
</task>

<task type="auto">
  <name>Task 3: Register save/new/save-as commands + differential test (Python app opens Tauri save)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-VALIDATION.md (ARCH-02 row: differential + manual; Manual-Only Verifications JW Library + owner real archive)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Phase Requirements Test Map ARCH-02 differential.rs shelling to python3 JWLManager.py, assert clean exit / no crash_box)
    - JWLManager.py (load_file / check_validity — what a clean open looks like; the app is the oracle per D-01)
    - CLAUDE.md (repo root — Python app run instructions; res/requirements.txt)
  </read_first>
  <action>
    Register `save_archive`, `save_as`, and `new_archive` as Tauri commands in `main.rs`/`lib.rs` (append to the existing invoke_handler alongside `open_archive` and `check_jwlcore`). Add `tests/differential.rs`: generate a fixture, save it through the Tauri save path, then shell out to `python3 JWLManager.py <saved-path>` (or import its check_validity) asserting it opens without raising / without the crash_box path — the D-01 oracle for ARCH-02. Gate an additional env-var test (`JWLM_REAL_ARCHIVE`, D-07) that round-trips the owner's real archive locally and skips when unset (never in CI). Document the manual JW Library open step (cannot run in CI) in the SUMMARY.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test differential 2>&1 | grep -qE "test result: ok|ignored"</automated>
    <human-check>Save a fixture-derived archive from the Tauri app and open it in JW Library on a real device; confirm no error and data intact (manual per VALIDATION).</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test --test differential` passes (or skips gracefully if python3/PySide6 unavailable, recording the skip reason).
    - save/save-as/new commands registered and callable over IPC.
    - Env-var real-archive test present and skipped when `JWLM_REAL_ARCHIVE` unset.
    - Manual JW Library open step documented.
  </acceptance_criteria>
  <done>Python app reopens Tauri-saved archives (ARCH-02 oracle); save/new/save-as exposed to the frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust core -> target file | Save writes/replaces a user file on disk |
| webview -> IPC | Save-as path selected via native dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | archive/save.rs | mitigate | Sibling-temp + atomic `fs::rename` (same fs) — partial write never replaces a good archive (Core Value) |
| T-05-02 | Tampering | source archive | mitigate | Source opened read-only into temp dir (D-03); save-as asserts original bytes unchanged |
| T-05-03 | Tampering | save-as path arg | mitigate | Native file dialog for path selection; narrow tauri.conf fs capability, not arbitrary JS path strings |
| T-05-04 | Denial of Service | manifest hash timing | mitigate | Hash computed last over final bytes; wrong-order regression caught by `hash_last` test |
</threat_model>

<verification>
- `cd app/src-tauri && cargo clippy -- -D warnings && cargo test` green (save, new, save-as, differential).
- Manual: JW Library opens a Tauri-saved archive; owner real-archive round-trip locally (env-gated).
</verification>

<success_criteria>
Save/save-as/new work atomically without mutating the source; the Python app (and JW Library, manually) reopen what the Tauri app saved.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-05-SUMMARY.md` when done.
</output>
