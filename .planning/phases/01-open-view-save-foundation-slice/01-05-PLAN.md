---
phase: 01-open-view-save-foundation-slice
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-07"]
files_modified:
  - app/src-tauri/src/archive/save.rs
  - app/src-tauri/src/archive/new.rs
  - app/src-tauri/src/archive/mod.rs
  - app/src-tauri/src/main.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/save_tests.rs
  - app/src-tauri/tests/differential.rs
autonomous: true
# WAVE NOTE (plan-checker WARNING 1): stays wave 4 (NOT rewaved to 3). The 01-01 split pushed
# 01-02 (manifest::compute_hash, which this plan needs) to wave 3, and pushed 01-03 to wave 3.
# This plan shares main.rs/lib.rs (invoke_handler) with 01-07 (wave 2) and 01-03 (wave 3), and
# depends on 01-02 (wave 3) for compute_hash — so its earliest correct wave is 4. Real shared
# file (main.rs/lib.rs) + real dep (01-02 hash) = wave 4, per the WARNING-1 exception clause.
# REVIEWS NOTE (codex HIGH): this plan now (a) CONSUMES the durable ArchiveSession from 01-07
# (TempDir + db_path + full zip-entry inventory + target path) rather than re-deriving working
# state (finding 1); (b) uses a same-directory temp + OS-correct ATOMIC REPLACE, with
# delete-then-rename explicitly FORBIDDEN (finding 3); (c) rebuilds the FULL zip from the session
# entry inventory so loose media + unknown entries survive — only userData.db + manifest.json are
# replaced (finding 4); (d) seeds new_archive() from res/blank, the real v16 empty seed (finding 5);
# (e) makes the Python differential oracle a real headless check OR an explicit recorded manual gate,
# never a silent skip that fakes ARCH-02 (finding 10).
requirements: [ARCH-02, ARCH-06, ARCH-07, QA-01]
must_haves:
  truths:
    - "Saving rebuilds the archive from the ArchiveSession's full zip-entry inventory via a same-directory temp file + OS-correct atomic replace; a source archive is never mutated in place; delete-then-rename is never used"
    - "A saved archive preserves every original entry (loose media + unknown/forward-compat keys) byte-identically; only userData.db and manifest.json are replaced"
    - "A save over an existing destination leaves EITHER the old OR the new complete file on disk — never a truncated file (crash/interruption safe)"
    - "The Python app (JWLManager.py) opens an archive the Tauri app saved, without error (real headless check or a recorded manual gate — never a silent CI skip)"
    - "Save-as writes to a chosen path and leaves the original file bytes unchanged; the session's target path follows the new path"
    - "A new empty v16 archive seeded from res/blank can be created and opened by both apps"
  artifacts:
    - path: "app/src-tauri/src/archive/save.rs"
      provides: "atomic save (same-dir temp + OS-correct replace, NO delete-then-rename), full-inventory zip rebuild, update_manifest hash-last"
      contains: "rename"
    - path: "app/src-tauri/src/archive/new.rs"
      provides: "create new empty v16 archive seeded from res/blank"
      contains: "blank"
    - path: "app/src-tauri/tests/save_tests.rs"
      provides: "atomic-replace, media/unknown-entry preservation, and interruption-safety assertions"
      contains: "preserv"
    - path: "app/src-tauri/tests/differential.rs"
      provides: "Python-app-opens-Tauri-save oracle test (headless-or-recorded-gate)"
      contains: "JWLManager.py"
  key_links:
    - from: "app/src-tauri/src/archive/save.rs"
      to: "session::ArchiveSession"
      via: "reads db_path + entry inventory + target path from managed state"
      pattern: "ArchiveSession"
    - from: "app/src-tauri/src/archive/save.rs"
      to: "archive::manifest::compute_hash"
      via: "hash computed last, before zip"
      pattern: "compute_hash"
    - from: "app/src-tauri/tests/differential.rs"
      to: "python3 JWLManager.py"
      via: "subprocess reopen assertion"
      pattern: "python3"
---

<objective>
Deliver the persistence slice on top of the durable session: save (same-directory temp + OS-correct atomic replace, full-inventory zip rebuild, manifest hash-last), save-as (chosen path, original untouched, session target follows new path), and new empty v16 archive seeded from res/blank — then prove ARCH-02 by having the Python app reopen what the Tauri app saved (the differential oracle from D-01).

Purpose: Save is where Core Value is won or lost. Rebuilding from the session's FULL entry inventory (not just db+manifest) means loose media and unknown keys can never be silently destroyed; an OS-correct atomic replace (never delete-then-rename) means a crash can never leave a truncated archive. The Python-app cross-check is ARCH-02's real proof.
Output: `archive/save.rs`, `archive/new.rs`, save/save-as/new Tauri commands (consuming ArchiveSession), preservation + atomicity tests + the differential test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/research/FUNCTIONALITY-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic save from the session (same-dir temp + OS-correct replace, NO delete-then-rename), full-inventory zip rebuild, update_manifest hash-last</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (archive/save.rs — save_file/save_as_file JWLManager.py:1121-1150; update_manifest hash-last 1152-1170; Shared Patterns "Manifest hash-last ordering")
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Code Examples atomic save write-temp-then-rename D-04, same-filesystem caveat; Pitfall 1 hash timing)
    - .planning/research/FUNCTIONALITY-SPEC.md (save sequence; LastModified UPDATE before hash; NO trim_db in Phase 1 — ARCH-04 is Phase 2)
    - app/src-tauri/src/session.rs (01-07 — ArchiveSession: db_path, entries inventory, target_path; read from managed state, do not re-extract)
    - app/src-tauri/src/archive/manifest.rs (01-02 — compute_hash + Manifest struct this plan calls; do not reimplement)
  </read_first>
  <behavior>
    - Saving reads the ArchiveSession, writes to a same-directory temp file in the target's directory, flush+sync, then atomically replaces the target with the OS-correct primitive.
    - The rebuilt zip contains EVERY original entry from the session inventory (loose media + unknown keys byte-identical); only userData.db and manifest.json are regenerated.
    - update_manifest touches LastModified, reads schemaVersion via PRAGMA user_version, then computes hash LAST over the final DB bytes.
    - Re-opening a saved archive with the Tauri core yields the same normalized Notes (semantic round-trip, not byte).
    - After a save over an existing file, exactly one complete file exists (old or new) — never a truncated one.
  </behavior>
  <action>
    Create `archive/save.rs` implementing `save_archive(session, target_path)`: read the `ArchiveSession` from managed state (db_path, full entry inventory, target). Run the manifest update sequence in the correct order — write DB mutations (LastModified UPDATE), close/flush the rusqlite handle, read final userData.db bytes, `compute_hash` (sha2, from 01-02's manifest.rs) LAST, serialize the compact manifest. Rebuild the zip into a SAME-DIRECTORY temp file (`<target>.tmp-<rand>` in the target's own directory so the replace is on one filesystem) by streaming EVERY entry from the session inventory: replace `userData.db` (new bytes) and `manifest.json` (new bytes), and copy ALL OTHER entries (media, thumbnails, unknown/forward-compat files) through UNCHANGED (finding 4). Flush + `sync_all` the temp file, then ATOMICALLY REPLACE the target using the OS-correct primitive (finding 3): on Windows use `ReplaceFileW` (or `std::fs::rename` only where atomic-replace-over-existing semantics are verified for the target Windows version); on Unix `std::fs::rename` (atomic replace on same fs). Document in code that DELETE-THEN-RENAME IS FORBIDDEN — it opens a window where neither file is complete. Do NOT implement trim_db/VACUUM (ARCH-04 is Phase 2). Add `tests/save_tests.rs` asserting: (a) save produces a re-openable archive with semantically equal Notes; (b) a fixture opened with a loose-media entry + an unknown entry (from 01-01's generator) still contains BOTH byte-identically after save+reopen (preservation); (c) the temp file is gone and only the target remains; (d) hash equals sha256 of the on-disk userData.db after mutation; (e) an interruption-safety test: simulate/inject a failure before the replace step and assert the original target is still a complete, openable archive (old-or-new invariant).
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo test --test save_tests 2>&1 | grep -q "test result: ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test save_tests` passes: saved archive reopens with semantically equal normalized Note tables.
    - Preservation test passes: loose-media + unknown entries survive save byte-identically (rebuilt from the session inventory).
    - Interruption-safety test passes: a failure before replace leaves the original complete/openable (never truncated).
    - hash test passes: manifest hash == sha256 of final on-disk DB bytes.
    - Save uses same-dir temp + OS-correct atomic replace; no in-place source write; delete-then-rename absent from the code.
  </acceptance_criteria>
  <done>Save is atomic (OS-correct replace, no delete-then-rename), hash-last, and full-inventory preserving; a re-open round-trips semantically with media + unknown entries intact.</done>
</task>

<task type="auto">
  <name>Task 2: New empty v16 archive seeded from res/blank + save-as (original untouched, session target follows new path)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (archive/save.rs save_as_file D-05 session-follows-new-path; fixture_gen res/blank shape; new_file JWLManager.py:965-991)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-03 source read-only, D-04 atomic rename, D-05 save-as follows new file, A4 res/blank as v16 seed)
    - res/blank (extract + inspect — it is a zip of userData.db (PRAGMA user_version = 16) + default_thumbnail.png, NO manifest.json inside; the app writes manifest.json on save. Seed new archives by copying this userData.db + thumbnail, NOT by hand-building a schema.)
    - app/src-tauri/src/session.rs (01-07 — update target_path on save-as; keep source read-only)
  </read_first>
  <action>
    Create `archive/new.rs` implementing `new_archive()`: build a fresh empty v16 archive by SEEDING FROM res/blank (finding 5) — copy res/blank's `userData.db` (already PRAGMA user_version = 16) and `default_thumbnail.png` into a fresh session temp dir, then write a compact `manifest.json` via the `Manifest` struct (name=APP, version 1, type 0, schemaVersion 16, empty hash until first save). Populate a new `ArchiveSession` (no source file yet; entry inventory = the seeded entries). Do NOT hand-build a minimal DB — res/blank is the real JW Library empty seed and the authoritative v16 shape (this closes the "false oracle / circular spec" risk). Implement `save_as(target_path)`: write to the chosen path via the atomic full-inventory save from Task 1, leave the original source file's bytes unchanged (source was opened read-only into a temp dir per D-03), and update the session's `target_path` to the NEW path (D-05). Add tests: new archive opens with zero notes and a valid v16 manifest; save-as to a new path leaves an original fixture's bytes byte-identical (assert original file hash unchanged) while the new file opens cleanly and the session target follows.
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo test new_empty_archive save_as_preserves_original 2>&1 | grep -q "test result: ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test new_empty_archive` passes: a new archive (seeded from res/blank) opens with a valid v16 manifest and zero Notes.
    - `cargo test save_as_preserves_original` passes: original file bytes unchanged after save-as; new file opens cleanly; session target_path updated to new.
    - new_archive seeds from res/blank (no hand-built schema).
  </acceptance_criteria>
  <done>User can create a new empty archive seeded from the real v16 blank and save-as without mutating the original working copy; session target follows the new path.</done>
</task>

<task type="auto">
  <name>Task 3: Register save/new/save-as commands + differential oracle (Python app opens Tauri save; headless-or-recorded-gate)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-VALIDATION.md (ARCH-02 row: differential + manual; Manual-Only Verifications JW Library + owner real archive)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Phase Requirements Test Map ARCH-02 differential.rs shelling to python3 JWLManager.py, assert clean exit / no crash_box)
    - JWLManager.py (load_file / check_validity — what a clean open looks like; the app is the oracle per D-01. Prefer importing check_validity headless over launching the GUI.)
    - CLAUDE.md (repo root — Python app run instructions; res/requirements.txt)
    - app/src-tauri/src/main.rs (01-07 registered open_archive; 01-03 registered check_jwlcore — append save/new/save-as to the same invoke_handler; all return Result<T, ErrorDto>)
  </read_first>
  <action>
    Register `save_archive`, `save_as`, and `new_archive` as Tauri commands in `main.rs`/`lib.rs` (append to the existing invoke_handler alongside `open_archive` from 01-07 and `check_jwlcore` from 01-03; all return `Result<_, ErrorDto>`). Add `tests/differential.rs`: generate a fixture, save it through the Tauri save path, then run the D-01 oracle for ARCH-02 as a REAL check — invoke the Python app HEADLESS by importing/calling `check_validity` (or a `--check <path>` non-GUI entrypoint) via `python3 -c`, asserting it opens without raising / without the crash_box path. The oracle MUST NOT silently skip on missing PySide6 in a way that fakes a pass (finding 10): if the Python environment is genuinely unavailable in CI, the test FAILS-or-is-explicitly-`#[ignore]`d AND the phase requires a RECORDED MANUAL GATE — a checked box / logged evidence in the SUMMARY that a human ran the Python-open check locally — before Phase 1 is marked complete. Never a green report on a silent skip. Gate an additional env-var test (`JWLM_REAL_ARCHIVE`, D-07) that round-trips the owner's real archive locally and skips when unset (never in CI). Document the manual JW Library open step (cannot run in CI) in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo test --test differential 2>&1 | grep -qE "test result: ok|ignored"'</automated>
    <human-check>RECORDED GATE: run `python3 -c "import JWLManager; JWLManager.check_validity(<saved-path>)"` (or the headless entrypoint) locally against a Tauri-saved archive AND open it in JW Library on a real device; confirm no error and data intact; log the evidence in the SUMMARY (this gate must be satisfied before Phase 1 is complete when CI cannot run the Python oracle).</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test --test differential` passes via a REAL headless Python `check_validity` call; if the Python env is unavailable it is explicitly `#[ignore]`d with the reason AND a recorded manual gate is required (never a silent green skip).
    - save/save-as/new commands registered and callable over IPC, returning ErrorDto on failure.
    - Env-var real-archive test present and skipped when `JWLM_REAL_ARCHIVE` unset.
    - Manual JW Library open step + Python-oracle gate documented in the SUMMARY.
  </acceptance_criteria>
  <done>Python app reopens Tauri-saved archives (ARCH-02 oracle, headless or recorded-gate — never faked); save/new/save-as exposed to the frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust core -> target file | Save writes/replaces a user file on disk |
| session inventory -> rebuilt zip | Every original entry must survive; only db+manifest are regenerated |
| webview -> IPC | Save-as path selected via native dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | archive/save.rs | mitigate | Same-dir temp + OS-correct atomic replace (Windows ReplaceFileW / Unix rename); delete-then-rename FORBIDDEN — a partial write never replaces a good archive; interruption-safety test enforces old-or-new (Core Value) |
| T-05-02 | Tampering | session entry inventory | mitigate | Full-inventory rebuild — loose media + unknown keys copied through byte-identical; preservation test proves no silent data loss (Core Value) |
| T-05-03 | Tampering | source archive | mitigate | Source opened read-only into temp dir (D-03); save-as asserts original bytes unchanged |
| T-05-04 | Tampering | save-as path arg | mitigate | Native file dialog for path selection; narrow tauri.conf fs capability, not arbitrary JS path strings |
| T-05-05 | Denial of Service | manifest hash timing | mitigate | Hash computed last over final bytes; wrong-order regression caught by `hash_last` test |
| T-05-06 | Repudiation | ARCH-02 oracle faked | mitigate | Python oracle is a real headless check or an explicit recorded manual gate; a silent skip never reports green (finding 10) |
</threat_model>

<verification>
- `bash -c 'cd app/src-tauri && cargo clippy -- -D warnings && cargo test'` green (save, new, save-as, preservation, interruption-safety, differential).
- Manual/recorded gate: Python `check_validity` opens a Tauri-saved archive; JW Library opens it; owner real-archive round-trip locally (env-gated).
</verification>

<success_criteria>
Save/save-as/new work atomically (OS-correct replace, no delete-then-rename) without mutating the source and preserving every original entry; the Python app (headless or recorded gate) and JW Library (manually) reopen what the Tauri app saved.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
