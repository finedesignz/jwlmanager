---
phase: 01-open-view-save-foundation-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/vite.config.ts
  - app/vitest.config.ts
  - app/tsconfig.json
  - app/index.html
  - app/src/main.tsx
  - app/src/App.tsx
  - app/src/setupTests.ts
  - app/src-tauri/Cargo.toml
  - app/src-tauri/Cargo.lock
  - app/src-tauri/tauri.conf.json
  - app/src-tauri/build.rs
  - app/src-tauri/src/main.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/common/mod.rs
  - app/src-tauri/tests/fixtures.rs
  - app/src-tauri/tests/open_archive_tests.rs
autonomous: true
# SPLIT NOTE: This plan is the Walking Skeleton SCAFFOLD + Wave-0 harness only.
# The security-sensitive core primitives (error.rs, category.rs, archive/extract.rs
# zip-slip, db/notes.rs, open_archive command, NotesList render) live in 01-07 (wave 2).
# This plan owns the FULL Cargo.toml dependency list and the FULL tauri.conf.json
# (capabilities + all bundle resources incl. libs/jwlCore* and res/resources.db) so
# downstream plans (01-03 jwlcore, 01-04 resources.db) only CONSUME declared resources.
# main.rs/lib.rs here are a BARE Tauri builder (empty invoke_handler); 01-07 registers
# open_archive, 01-03 registers check_jwlcore, 01-05 registers save/new/save-as.
# REVIEWS NOTE (codex HIGH): this plan now (a) pins `zip` to an EXACT compatible version and
# commits BOTH lockfiles (finding 13); (b) establishes vitest + config + deps + committed
# package-lock BEFORE 01-02's CI runs `npm test` (finding 9); (c) seeds the synthetic fixture
# from res/blank — the real JW Library v16 empty seed — not a hand-built DB (finding 5);
# (d) makes the RED open_archive test compile-safe via `#[ignore]` until 01-07 (finding 7);
# (e) the fixture carries a loose-media entry + an unknown zip entry for the 01-05 preservation
# test (finding 4); (f) the zip-slip fixture generator covers 6 variants (finding 11);
# (g) all verify commands are portable single-filter / bash-shelled forms (finding 8).
requirements: [QA-01]
must_haves:
  truths:
    - "Running `npm run tauri dev` boots the app window (empty 'No archive open' shell) without crashing"
    - "The synthetic v16 fixture generator seeds from res/blank and produces a valid `.jwlibrary` in a temp dir with userData.db + manifest.json + a loose-media entry + an unknown entry (no real/scrubbed archive ever committed)"
    - "A repo-scan test fails if any `*.jwlibrary` file is tracked in git"
    - "An `#[ignore]`d RED end-to-end `open_archive` integration test exists (compiles clean, does not break the test crate) and is un-ignored + turned green by 01-07"
    - "`npm test` runs a committed vitest config against a committed package-lock.json (so 01-02 CI does not fail on a missing script/dep)"
  artifacts:
    - path: "app/src-tauri/Cargo.toml"
      provides: "full pinned dependency list for the whole phase (tauri, rusqlite bundled, EXACT zip pin, libloading, thiserror, serde/serde_json, sha2, tempfile, ts-rs)"
      contains: "zip"
    - path: "app/src-tauri/Cargo.lock"
      provides: "committed Rust lockfile (reproducible builds; CVE-safe zip resolution frozen)"
    - path: "app/package-lock.json"
      provides: "committed npm lockfile so `npm ci` in CI is reproducible and `npm test` deps exist"
    - path: "app/vitest.config.ts"
      provides: "vitest + jsdom config and `test` npm script established before 01-02 CI runs `npm test`"
    - path: "app/src-tauri/tauri.conf.json"
      provides: "base capabilities (dialog) + bundle resources declaring libs/jwlCore* and res/resources.db"
    - path: "app/src-tauri/src/main.rs"
      provides: "bare Tauri builder, empty invoke_handler (commands registered by later plans)"
    - path: "app/src-tauri/tests/fixtures.rs"
      provides: "res/blank-seeded synthetic v16 fixture generator + 6-variant zip-slip fixture (no real archive committed)"
    - path: "app/src-tauri/tests/common/mod.rs"
      provides: "temp-dir extraction + normalized semantic table-diff helpers"
  key_links:
    - from: "app/src/main.tsx"
      to: "app/src/App.tsx"
      via: "React root render of empty-state shell"
      pattern: "App"
    - from: "app/src-tauri/tests/open_archive_tests.rs"
      to: "open_archive command (implemented in 01-07)"
      via: "#[ignore]d RED integration test asserting fixture opens"
      pattern: "open_archive"
---

<objective>
Walking Skeleton scaffold + Wave-0 test harness. Stand up the Tauri v2 + Vite + React/TS app in `app/` as a runnable (empty-shell) `npm run tauri dev` window, pin the full phase dependency list (with an EXACT `zip` pin + committed lockfiles), establish the frontend test tooling (vitest + config + committed package-lock so 01-02's CI `npm test` step cannot fail on a missing script/dep), declare the complete `tauri.conf.json` (capabilities + all bundle resources), and build the Wave-0 fixtures/harness: a res/blank-seeded synthetic v16 fixture generator (carrying a loose-media + unknown entry for the 01-05 preservation test), a 6-variant zip-slip fixture generator, the shared test harness, and a compile-safe `#[ignore]`d RED end-to-end `open_archive` test. No security primitives and no real end-to-end open path here — those are 01-07 (wave 2).

Purpose: Separate low-risk scaffold boilerplate + Wave-0 harness (this plan) from the security-sensitive core primitives (01-07), so each is reviewable in isolation, per the plan-checker split. Establishes the runnable shell and the fixtures/harness every later plan (01-02..01-07) depends on.
Output: A booting `app/` shell via `npm run tauri dev`; full `Cargo.toml`/`tauri.conf.json` + committed `Cargo.lock`/`package-lock.json`; vitest tooling; res/blank-seeded synthetic v16 fixture generator + 6-variant zip-slip fixture + shared harness + compile-safe RED e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-open-view-save-foundation-slice/SKELETON.md
@.planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri v2 + Vite + React/TS shell (full Cargo.toml + exact zip pin + committed lockfiles + vitest tooling + tauri.conf.json, bare invoke_handler, empty-state App)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Standard Stack table + Installation; Recommended Project Structure; Package Legitimacy Audit — verify each crate/pkg version before pinning)
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (project structure; Shared Patterns "Temp-dir working copy")
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Copywriting Contract empty state; shadcn init New York / neutral / CSS variables; tokens)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-15 unwrap ban; D-16 CI matrix targets; D-17 no signing)
    - ~/.claude/design-preferences.md (dark-first tokens, spacing multiples of 4)
  </read_first>
  <action>
    Scaffold `app/` with Tauri v2 + Vite + React + TypeScript. Before pinning, verify each dependency exists at the intended version via `cargo search` / `npm view` (package legitimacy substitute per RESEARCH — all are long-established: tauri, rusqlite bundled, zip, libloading, thiserror, serde/serde_json, sha2, tempfile, ts-rs, @tanstack/react-virtual). Write the FULL `Cargo.toml` dependency list for the whole phase (so 01-07/01-02/01-04 do not re-edit it). Pin `zip` to an EXACT compatible version — the newest published `2.x` that is `>= 2.3` (CVE-2025-29787 fix) written as a caret-free exact requirement (e.g. `zip = "=2.3.0"` or the current 2.x), NOT an open `zip = ">=2.3"` that would admit a future major (finding 13). Run `cargo generate-lockfile` (or a `cargo check`) and COMMIT `Cargo.lock` (finding 13). Write the FULL `tauri.conf.json`: base capabilities (dialog plugin for native file-open), and `bundle.resources` declaring `libs/jwlCore*` (consumed by 01-03) and `res/resources.db` (consumed by 01-04) up front so those plans do not re-edit this file. Add crate-level `#![deny(clippy::unwrap_used, clippy::expect_used)]` scaffolding (D-15). Create a BARE `main.rs`/`lib.rs` Tauri builder with an EMPTY `invoke_handler` (commands are registered by 01-07/01-03/01-05). Run `npx shadcn init` (New York, neutral, CSS variables). Establish the FRONTEND TEST TOOLING now (finding 9): add `vitest` + `@testing-library/react` + `jsdom` (or happy-dom) to devDependencies, add `app/vitest.config.ts` (jsdom environment, `setupFiles: ['src/setupTests.ts']`), add `app/src/setupTests.ts` (jest-dom matchers), add a `"test": "vitest run"` npm script, run `npm install`, and COMMIT `package-lock.json` — so 01-02's CI `npm ci && npm test` cannot fail on a missing script/dep. Add one trivial passing smoke test (e.g. `App.test.tsx` asserting the empty-state heading renders) so `npm test` exits 0. Build `app/src/App.tsx` as the empty-state shell only ("No archive open" + "Open Archive"/"New Archive" CTAs per UI-SPEC, dark-first tokens) — no IPC wiring yet (that is 01-07). Do NOT run a full `tauri build` bundle anywhere.
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo check 2>&1 | grep -q "error\[" && exit 1; cd .. && npm run build 2>&1 | grep -qi "error TS" && exit 1; npm test 2>&1 | grep -qiE "pass|no test|✓|Test Files" && test -f package-lock.json && test -f src-tauri/Cargo.lock'</automated>
    <human-check>Run `npm run tauri dev`; confirm the window boots and shows the "No archive open" empty state without crashing.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo check` passes (no `error[` lines; Rust scaffold compiles; empty invoke_handler).
    - `npm run build` type-checks with no `error TS` lines.
    - `npm test` runs the committed vitest config and exits 0 (at least the smoke test passes); `package-lock.json` and `Cargo.lock` are committed.
    - `Cargo.toml` pins `zip` to an EXACT `2.x` (`>= 2.3`, no open lower bound / no caret-to-next-major); `tauri.conf.json` declares dialog capability + bundle resources for `libs/jwlCore*` and `res/resources.db`.
    - No `tauri build`/full-bundle step in any verify (feedback-latency budget; bundle is CI/manual only).
  </acceptance_criteria>
  <done>App scaffolds and boots an empty-state shell via `tauri dev`; full deps + exact zip pin + committed lockfiles + vitest tooling + tauri.conf declared; invoke_handler empty for later plans.</done>
</task>

<task type="auto">
  <name>Task 2: res/blank-seeded synthetic v16 fixture (+ loose-media/unknown entry) + 6-variant zip-slip fixture + shared harness + compile-safe RED open_archive e2e test</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (fixture_gen.rs section — res/blank shape: userData.db + default_thumbnail.png, manifest.json written separately; D-06 forbids committing real/scrubbed archives)
    - .planning/phases/01-open-view-save-foundation-slice/01-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map rows QA-01, ARCH-01)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Wave 0 Gaps; "Pitfall 2: independent-notes query" for what a good fixture must contain)
    - res/blank (INSPECT — the Python app's real empty v16 seed: a zip of `userData.db` (PRAGMA user_version = 16) + `default_thumbnail.png`, NO manifest.json inside; the app writes manifest.json separately. Seed the fixture DB by copying this userData.db, then INSERT test rows — do NOT hand-build a schema.)
  </read_first>
  <action>
    Create the shared test harness `app/src-tauri/tests/common/mod.rs` (temp-dir extraction helper, and a normalized semantic table-diff helper that reads a table into sorted rows for later semantic-equivalence comparisons — never byte comparison) and `app/src-tauri/tests/fixtures.rs`. The synthetic v16 fixture generator seeds from `res/blank` (finding 5): copy `res/blank`'s `userData.db` into a temp dir (it is already PRAGMA user_version = 16 — verified), then INSERT the minimum test rows into the existing v16 tables (Location, Note, TagMap, Tag, UserMark, LastModified) so the fixture contains at least one located Note AND at least one independent Note (LocationId IS NULL, BlockType 0). Rebuild the `.jwlibrary` zip with: that userData.db, a compact `manifest.json`, the `default_thumbnail.png` from res/blank, PLUS a loose-media entry (e.g. `media/test.png`) AND an unknown/forward-compat entry (e.g. `future_unknown.dat`) so 01-05's preservation test (finding 4) has both to round-trip. No real or scrubbed archive is ever written to the repo; if the fixture needs a v-other-than-16 seed for a later test, document how it reaches v16 (it does not — res/blank is v16). Add a SEPARATE `generate_zip_slip_fixture(variant)` covering SIX malicious entry variants for 01-02 to test against (finding 11): (1) `../` unix traversal, (2) absolute unix path (`/etc/...`), (3) absolute Windows path (`C:\...`), (4) backslash traversal (`..\..\`), (5) a duplicate-entry archive, (6) a symlink-chain overwrite; also note a bounded oversized/zip-bomb guard entry. Add `tests/open_archive_tests.rs` with an `#[ignore]`d `open_archive` integration test that asserts the fixture opens and lists >= 1 Note — marked `#[ignore]` (NOT a call to a missing symbol) so the integration-test crate COMPILES clean before 01-07 lands; 01-07 removes the `#[ignore]` and turns it green (finding 7). Add a repo-scan unit test asserting no `.jwlibrary` file is tracked in git (glob the repo, fail if any found).
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo test --test fixtures 2>&1 | grep -q "test result: ok" && cargo test --test open_archive_tests -- --include-ignored 2>&1 | grep -qE "ignored|test result"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test fixtures` passes: generator produces a zip that is a valid archive with manifest.json + userData.db + default_thumbnail.png + a loose-media entry + an unknown entry, all seeded from res/blank.
    - The repo-scan test passes: zero `*.jwlibrary` files are git-tracked.
    - The generated fixture contains at least one located and one independent Note (queryable by 01-07's test).
    - `open_archive` integration test EXISTS, is `#[ignore]`d, and the test crate COMPILES (does not reference a not-yet-existing `open_archive` symbol in a compile-breaking way) — un-ignored and green in 01-07.
    - The 6-variant zip-slip fixture generator exists for 01-02 to consume.
  </acceptance_criteria>
  <done>res/blank-seeded synthetic fixture (with media + unknown entry) + 6-variant zip-slip fixture + harness committed; no real archive in repo; compile-safe `#[ignore]`d RED e2e test present for 01-07 to un-ignore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registries -> build | crates.io / npm dependencies enter the trusted build |
| test fixtures -> repo | Fixture generator must never persist real user archives into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | tests/fixtures.rs | mitigate | Synthetic-only fixtures seeded from res/blank (D-06); repo-scan test asserts no `.jwlibrary` tracked (GDPR Art. 9) |
| T-01-02 | Tampering | scaffold main.rs/lib.rs | mitigate | Bare builder, empty invoke_handler; crate-level unwrap/expect deny scaffolded (D-15) — no untrusted-input path exists yet (that lands in 01-07) |
| T-01-SC | Tampering | cargo/npm installs | mitigate | Verify each package version on registry before pinning; all named deps long-established (no `[SLOP]`/`[SUS]`); `zip` pinned to an EXACT `2.x >= 2.3` (CVE-2025-29787 safe); `Cargo.lock` + `package-lock.json` committed for reproducibility |
</threat_model>

<verification>
- `bash -c 'cd app/src-tauri && cargo check && cargo test --test fixtures && cargo test --test open_archive_tests -- --include-ignored'` green.
- `cd app && npm run build` type-checks clean; `npm test` exits 0; `npm run tauri dev` boots the empty-state shell.
- `Cargo.lock` + `package-lock.json` committed; no `.jwlibrary` file tracked in git.
</verification>

<success_criteria>
The Tauri app scaffolds and boots an empty-state shell via `tauri dev`; frontend test tooling + committed lockfiles + exact zip pin are in place; the res/blank-seeded synthetic v16 fixture generator (with media + unknown entry) + 6-variant zip-slip fixture + shared harness are committed with a compile-safe `#[ignore]`d RED `open_archive` e2e test; no real archive is in the repo.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
