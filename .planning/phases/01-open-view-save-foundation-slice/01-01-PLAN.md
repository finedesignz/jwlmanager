---
phase: 01-open-view-save-foundation-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/vite.config.ts
  - app/tsconfig.json
  - app/index.html
  - app/src/main.tsx
  - app/src/App.tsx
  - app/src-tauri/Cargo.toml
  - app/src-tauri/tauri.conf.json
  - app/src-tauri/build.rs
  - app/src-tauri/src/main.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/common/mod.rs
  - app/src-tauri/tests/fixtures.rs
  - app/src-tauri/tests/open_archive_tests.rs
autonomous: true
# SPLIT NOTE: This plan is the Walking Skeleton SCAFFOLD + Wave-0 harness only.
# The security-sensitive core primitives (error.rs, category.rs, archive/extract.rs
# zip-slip, db/notes.rs, open_archive command, NotesList render) live in 01-07 (wave 2).
# This plan owns the FULL Cargo.toml dependency list and the FULL tauri.conf.json
# (capabilities + all bundle resources incl. libs/jwlCore* and res/resources.db) so
# downstream plans (01-03 jwlcore, 01-04 resources.db) only CONSUME declared resources.
# main.rs/lib.rs here are a BARE Tauri builder (empty invoke_handler); 01-07 registers
# open_archive, 01-03 registers check_jwlcore, 01-05 registers save/new/save-as.
requirements: [QA-01]
must_haves:
  truths:
    - "Running `npm run tauri dev` boots the app window (empty 'No archive open' shell) without crashing"
    - "The synthetic v16 fixture generator produces a valid `.jwlibrary` in a temp dir with userData.db + manifest.json (no real/scrubbed archive ever committed)"
    - "A repo-scan test fails if any `*.jwlibrary` file is tracked in git"
    - "A RED end-to-end `open_archive` integration test exists and fails until 01-07 implements the command"
  artifacts:
    - path: "app/src-tauri/Cargo.toml"
      provides: "full pinned dependency list for the whole phase (tauri, rusqlite bundled, zip>=2.3, libloading, thiserror, serde/serde_json, sha2, tempfile, ts-rs)"
      contains: "zip"
    - path: "app/src-tauri/tauri.conf.json"
      provides: "base capabilities (dialog) + bundle resources declaring libs/jwlCore* and res/resources.db"
    - path: "app/src-tauri/src/main.rs"
      provides: "bare Tauri builder, empty invoke_handler (commands registered by later plans)"
    - path: "app/src-tauri/tests/fixtures.rs"
      provides: "synthetic v16 fixture generator (no real archive committed)"
    - path: "app/src-tauri/tests/common/mod.rs"
      provides: "temp-dir extraction + normalized semantic table-diff helpers"
  key_links:
    - from: "app/src/main.tsx"
      to: "app/src/App.tsx"
      via: "React root render of empty-state shell"
      pattern: "App"
    - from: "app/src-tauri/tests/open_archive_tests.rs"
      to: "open_archive command (implemented in 01-07)"
      via: "RED integration test asserting fixture opens"
      pattern: "open_archive"
---

<objective>
Walking Skeleton scaffold + Wave-0 test harness. Stand up the Tauri v2 + Vite + React/TS app in `app/` as a runnable (empty-shell) `npm run tauri dev` window, pin the full phase dependency list, declare the complete `tauri.conf.json` (capabilities + all bundle resources), and build the Wave-0 fixtures/harness: a committed synthetic v16 fixture generator, the shared test harness, and a RED end-to-end `open_archive` test. No security primitives and no real end-to-end open path here — those are 01-07 (wave 2).

Purpose: Separate low-risk scaffold boilerplate + Wave-0 harness (this plan) from the security-sensitive core primitives (01-07), so each is reviewable in isolation, per the plan-checker split. Establishes the runnable shell and the fixtures/harness every later plan (01-02..01-07) depends on.
Output: A booting `app/` shell via `npm run tauri dev`; full `Cargo.toml`/`tauri.conf.json`; synthetic v16 fixture generator + shared harness + RED e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-open-view-save-foundation-slice/SKELETON.md
@.planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri v2 + Vite + React/TS shell (full Cargo.toml + tauri.conf.json, bare invoke_handler, empty-state App)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Standard Stack table + Installation; Recommended Project Structure; Package Legitimacy Audit — verify each crate/pkg version before pinning)
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (project structure; Shared Patterns "Temp-dir working copy")
    - .planning/phases/01-open-view-save-foundation-slice/01-UI-SPEC.md (Copywriting Contract empty state; shadcn init New York / neutral / CSS variables; tokens)
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-15 unwrap ban; D-16 CI matrix targets; D-17 no signing)
    - ~/.claude/design-preferences.md (dark-first tokens, spacing multiples of 4)
  </read_first>
  <action>
    Scaffold `app/` with Tauri v2 + Vite + React + TypeScript. Before pinning, verify each dependency exists at the intended version via `cargo search` / `npm view` (package legitimacy substitute per RESEARCH — all are long-established: tauri, rusqlite bundled, zip ^2.3, libloading, thiserror, serde/serde_json, sha2, tempfile, ts-rs, @tanstack/react-virtual). Write the FULL `Cargo.toml` dependency list for the whole phase (so 01-07/01-02/01-04 do not re-edit it), including the `zip = ">=2.3"` CVE-safe pin. Write the FULL `tauri.conf.json`: base capabilities (dialog plugin for native file-open), and `bundle.resources` declaring `libs/jwlCore*` (consumed by 01-03) and `res/resources.db` (consumed by 01-04) up front so those plans do not re-edit this file. Add crate-level `#![deny(clippy::unwrap_used, clippy::expect_used)]` scaffolding (D-15). Create a BARE `main.rs`/`lib.rs` Tauri builder with an EMPTY `invoke_handler` (commands are registered by 01-07/01-03/01-05). Run `npx shadcn init` (New York, neutral, CSS variables). Build `app/src/App.tsx` as the empty-state shell only ("No archive open" + "Open Archive"/"New Archive" CTAs per UI-SPEC, dark-first tokens) — no IPC wiring yet (that is 01-07). Do NOT run a full `tauri build` bundle anywhere.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo check 2>&1 | grep -qv "error\[" && cd ../ && npm run build 2>&1 | grep -qiv "error TS"</automated>
    <human-check>Run `npm run tauri dev`; confirm the window boots and shows the "No archive open" empty state without crashing.</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo check` passes (Rust scaffold compiles; empty invoke_handler).
    - `npm run build` type-checks with no TypeScript errors.
    - `Cargo.toml` pins the full phase dependency set incl. `zip >= 2.3`; `tauri.conf.json` declares dialog capability + bundle resources for `libs/jwlCore*` and `res/resources.db`.
    - No `tauri build`/full-bundle step in any verify (feedback-latency budget; bundle is CI/manual only).
  </acceptance_criteria>
  <done>App scaffolds and boots an empty-state shell via `tauri dev`; full deps + tauri.conf declared; invoke_handler empty for later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic v16 fixture generator + shared test harness + RED open_archive e2e test</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (fixture_gen.rs section — res/blank shape: userData.db + default_thumbnail.png, manifest.json written separately; D-06 forbids committing real/scrubbed archives)
    - .planning/phases/01-open-view-save-foundation-slice/01-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map rows QA-01, ARCH-01)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Wave 0 Gaps; "Pitfall 2: independent-notes query" for what a good fixture must contain)
    - JWLManager.py lines 1023-1070 (upgrade_schema Location_new DDL — v16 schema reference for the synthetic userData.db)
  </read_first>
  <action>
    Create the shared test harness `app/src-tauri/tests/common/mod.rs` (temp-dir extraction helper, and a normalized semantic table-diff helper that reads a table into sorted rows for later semantic-equivalence comparisons — never byte comparison) and `app/src-tauri/tests/fixtures.rs`, a committed generator that programmatically builds a valid synthetic v16 `.jwlibrary` in a temp dir: an in-memory-then-serialized `userData.db` seeded with the minimum v16 tables (Location, Note, TagMap, Tag, UserMark, LastModified) containing at least one located Note AND at least one independent Note (LocationId IS NULL, BlockType 0), plus a compact `manifest.json` and a `default_thumbnail.png`. No real or scrubbed archive is ever written to the repo. Add `tests/open_archive_tests.rs` with a currently-failing `open_archive` integration test asserting the fixture opens and lists >= 1 Note (RED until 01-07 implements the command). Add a repo-scan unit test asserting no `.jwlibrary` file is tracked in git (glob the repo, fail if any found).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test fixtures_generate no_real_archive_committed 2>&1 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test fixtures_generate` passes: generator produces a zip that is a valid archive with manifest.json + userData.db.
    - `cargo test no_real_archive_committed` passes: asserts zero `*.jwlibrary` files are git-tracked.
    - The generated fixture contains at least one located and one independent Note (queryable by 01-07's test).
    - `open_archive` integration test exists and initially FAILS (RED) until 01-07.
  </acceptance_criteria>
  <done>Synthetic fixture generator + harness committed; no real archive in repo; RED e2e test present for 01-07 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registries -> build | crates.io / npm dependencies enter the trusted build |
| test fixtures -> repo | Fixture generator must never persist real user archives into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | tests/fixtures.rs | mitigate | Synthetic-only fixtures (D-06); repo-scan test asserts no `.jwlibrary` tracked (GDPR Art. 9) |
| T-01-02 | Tampering | scaffold main.rs/lib.rs | mitigate | Bare builder, empty invoke_handler; crate-level unwrap/expect deny scaffolded (D-15) — no untrusted-input path exists yet (that lands in 01-07) |
| T-01-SC | Tampering | cargo/npm installs | mitigate | Verify each package version on registry before pinning; all named deps long-established (no `[SLOP]`/`[SUS]`); `zip` pinned >= 2.3 (CVE-2025-29787 safe) |
</threat_model>

<verification>
- `cd app/src-tauri && cargo check && cargo test fixtures_generate no_real_archive_committed` green.
- `cd app && npm run build` type-checks clean; `npm run tauri dev` boots the empty-state shell.
- No `.jwlibrary` file tracked in git.
</verification>

<success_criteria>
The Tauri app scaffolds and boots an empty-state shell via `tauri dev`; the synthetic v16 fixture generator + shared harness are committed with a RED `open_archive` e2e test; no real archive is in the repo.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
