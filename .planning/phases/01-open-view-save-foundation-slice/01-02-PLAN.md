---
phase: 01-open-view-save-foundation-slice
plan: 02
type: execute
wave: 3
depends_on: ["01-01", "01-07"]
files_modified:
  - app/src-tauri/src/archive/manifest.rs
  - app/src-tauri/tests/manifest_tests.rs
  - app/src-tauri/tests/archive_tests.rs
  - app/src-tauri/tests/fixtures.rs
  - app/src-tauri/clippy.toml
  - .github/workflows/app-ci.yml
autonomous: true
# SPLIT NOTE (plan-checker BLOCKER 1): the old wave-1 01-01 was split; the core primitives
# (archive/extract.rs, minimal v16-only validity in archive/mod.rs) now live in 01-07 (wave 2). This
# plan CREATES the full byte-compat archive/manifest.rs (the skeleton's minimal validity is in 01-07's
# archive/mod.rs — no manifest.rs overlap). The zip-slip work here is the crafted MULTI-VARIANT
# rejection TEST against 01-07's archive::extract, consuming 01-01's 6-variant zip-slip fixture
# generator (this plan does NOT modify extract.rs; the exact CVE-safe `zip` pin + Cargo.lock live in
# 01-01's Cargo.toml). Depends on 01-07 for extract -> wave 3.
# REVIEWS NOTE (codex): (a) check_validity keeps 01-07's v16-ONLY gate (schemaVersion == 16 &&
# PRAGMA user_version == 16), rejecting v12-15 (finding 2); (b) zip-slip test covers 6 variants
# from 01-01's generator (finding 11); (c) every verify command is a portable single-`--test`
# filter; CI shell is set explicitly to bash for `grep`/`test`-based steps so windows runners
# don't fail (finding 8); (d) CI uses `npm ci` against 01-01's committed package-lock (finding 9).
requirements: [ARCH-03, ARCH-05, QA-01, QA-03, PLAT-01, SAFE-05]
must_haves:
  truths:
    - "A saved archive is byte-compatible enough that JW Library / the Python app accept its manifest.json without error (ordered fields, compact separators, hash = sha256 of the final DB bytes)"
    - "All six zip-slip variants (../, absolute unix, absolute Windows C:\\, backslash ..\\, duplicate entries, symlink-chain) are rejected, not extracted; a bounded oversized/zip-bomb guard is noted"
    - "check_validity accepts v16 ONLY (schemaVersion == 16 && PRAGMA user_version == 16) and rejects v12-15"
    - "CI builds + tests on windows-latest, windows-11-arm, ubuntu-latest, macos-latest on every push"
  artifacts:
    - path: "app/src-tauri/src/archive/manifest.rs"
      provides: "ordered Manifest struct + compact serialize + hash-last helper + v16-only check_validity"
      contains: "separators"
    - path: "app/src-tauri/tests/manifest_tests.rs"
      provides: "byte-compat + field-order + v16-only assertions"
    - path: "app/src-tauri/tests/archive_tests.rs"
      provides: "6-variant zip-slip rejection test"
      contains: "zip_slip"
    - path: ".github/workflows/app-ci.yml"
      provides: "four-leg matrix: build + fmt + clippy + test, explicit bash shell for grep/test steps"
      contains: "windows-11-arm"
    - path: "app/src-tauri/clippy.toml"
      provides: "unwrap/expect ban config"
  key_links:
    - from: ".github/workflows/app-ci.yml"
      to: "cargo clippy -- -D warnings"
      via: "CI step"
      pattern: "clippy"
    - from: "app/src-tauri/tests/archive_tests.rs"
      to: "archive::extract"
      via: "zip_slip_rejected test"
      pattern: "zip_slip"
---

<objective>
Harden the archive envelope the skeleton opened: create the full `manifest.json` byte-compatible with the Python app (ordered struct, compact separators, hash-last) with a v16-only `check_validity`, prove all six zip-slip variants are rejected against 01-01's crafted fixtures (testing 01-07's `archive::extract`), and stand up the four-leg CI matrix with fmt + clippy (unwrap ban) + test on every push (explicit bash shell so grep/test steps pass on Windows runners).

Purpose: ARCH-03 byte-compatibility and ARCH-05 security are the correctness+safety refinements the skeleton (01-07) deferred; CI (QA-03) makes "test as we go" a habit from day one.
Output: Byte-compatible `archive/manifest.rs` + v16-only tests; 6-variant zip-slip rejection test; `.github/workflows/app-ci.yml`; clippy config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md
@.planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md
@.planning/research/FUNCTIONALITY-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Byte-compatible manifest serialization (ordered struct, compact separators, hash-last) + v16-only check_validity</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (manifest.rs section — new_file shape, update_manifest hash-last ordering, check_validity gate; JWLManager.py:979-991, 1152-1170, 994-1008 excerpts)
    - .planning/research/FUNCTIONALITY-SPEC.md (§2.2 manifest shape/field order; hash = sha256 of final DB bytes; schemaVersion from PRAGMA user_version; separators=(',',':'))
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Pattern 1 ordered struct + serde_json::to_string; Anti-Patterns "Serializing via Value/HashMap"; Pitfall 1 hash timing)
    - app/src-tauri/src/archive/mod.rs (01-07's minimal v16-only validity check — this plan's full check_validity supersedes/wraps it, KEEPING the v16-only gate; do not duplicate the ArchiveError enum)
  </read_first>
  <behavior>
    - Serializing a known manifest produces exactly the Python field order with no whitespace (matches separators=(',',':')).
    - Unknown top-level and userDataBackup keys survive a read->write round-trip (forward compat).
    - The hash helper computes sha256 over the final on-disk userData.db bytes as the last DB-touching step.
    - check_validity accepts schemaVersion == 16 (and PRAGMA user_version == 16) only; a v14 manifest is rejected as UnsupportedSchema (finding 2).
  </behavior>
  <action>
    Create `archive/manifest.rs`: a `Manifest` struct with fields declared in exact Python order (name, creationDate, version, type, userDataBackup) and a nested `UserDataBackup` (lastModifiedDate, deviceName, databaseName, hash, schemaVersion), using `#[serde(rename=...)]` for camelCase and `#[serde(flatten)]` catch-all maps to preserve unknown keys (enable serde_json `preserve_order` for the flatten map). Serialize via `serde_json::to_string` (compact, non-pretty). Add a `compute_hash(db_path)` helper using `sha2` over the file bytes, and document (in code + test names) that it must be called last, after any DB mutation. Add a strict `check_validity` returning typed rejections reusing 01-07's `ArchiveError` (NotAZip / MissingManifest / MissingUserDataBackup / UnsupportedSchema { version }) — KEEP the v16-ONLY gate from 01-07 (`schemaVersion == 16` && PRAGMA user_version == 16; reject v12-15 with the Phase-3 message); do NOT loosen it to `> 11`, do not redefine the enum. Add `tests/manifest_tests.rs` asserting exact serialized bytes for a fixed manifest, field order, unknown-key preservation, that a type-confused schemaVersion (string) is a parse error (reject) not a coercion, and that a v14 manifest is rejected while a v16 manifest passes.
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo test --test manifest_tests 2>&1 | grep -q "test result: ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test manifest_tests` passes: exact compact byte string for a known manifest (no whitespace, Python field order); unknown-key round-trip preservation; malformed schemaVersion type -> typed parse error.
    - v16-only assertion passes: v14 rejected as UnsupportedSchema, v16 accepted.
    - A struct (never HashMap/Value) backs the manifest; hash helper uses `sha2`.
  </acceptance_criteria>
  <done>Manifest serializes byte-compatibly with the Python app; hash-last discipline encoded; check_validity is v16-only and tested.</done>
</task>

<task type="auto">
  <name>Task 2: 6-variant zip-slip rejection test (against 01-07's archive::extract, using 01-01's fixture generator)</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-PATTERNS.md (extract.rs anti-pattern-to-fix; Shared Patterns "Zip-slip validation via crate"; fixture_gen zip-slip note D-08)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (Security Domain zip-slip row; State of the Art CVE-2025-29787; "Don't Hand-Roll" zip path traversal)
    - .planning/phases/01-open-view-save-foundation-slice/01-VALIDATION.md (ARCH-05 row)
    - app/src-tauri/tests/fixtures.rs (01-01 — the `generate_zip_slip_fixture(variant)` 6-variant generator this test consumes; do not re-author it)
    - app/src-tauri/src/archive/extract.rs (01-07 — the extraction under test; this plan does NOT modify it)
  </read_first>
  <action>
    Add `tests/archive_tests.rs::zip_slip_rejected` driving 01-01's `generate_zip_slip_fixture` across ALL SIX malicious variants (finding 11): (1) `../` unix traversal, (2) absolute unix path, (3) absolute Windows path (`C:\`), (4) backslash traversal (`..\`), (5) duplicate entries, (6) symlink-chain overwrite. Assert `archive::extract` (from 01-07) refuses each (returns `ArchiveError::ZipSlipRejected` or the zip crate's enclosed_name rejection) and that NO file is written outside the temp extraction root (scan the parent dir before/after). Add a note/assertion for a bounded oversized/zip-bomb guard (a size cap that rejects an absurdly-large declared entry). Confirm `Cargo.toml` (owned by 01-01) already pins `zip` to an exact `2.x >= 2.3` with a committed `Cargo.lock`; if the pin is missing, record it as a blocker (do not edit Cargo.toml here — it is 01-01's file).
  </action>
  <verify>
    <automated>bash -c 'cd app/src-tauri && cargo test --test archive_tests 2>&1 | grep -q "test result: ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test archive_tests` passes: all six malicious variants rejected; no file escapes the extraction root (assert by scanning the parent dir before/after).
    - Bounded oversized/zip-bomb guard asserted or explicitly noted.
    - `Cargo.toml` shows an exact `zip` `2.x >= 2.3` pin with committed `Cargo.lock` (verified, owned by 01-01).
  </acceptance_criteria>
  <done>All six zip-slip variants are provably rejected against 01-01's crafted fixtures, not silently extracted.</done>
</task>

<task type="auto">
  <name>Task 3: Four-leg CI matrix (build + fmt + clippy + test) + clippy unwrap-ban config, explicit bash shell for grep/test steps</name>
  <read_first>
    - .planning/phases/01-open-view-save-foundation-slice/01-CONTEXT.md (D-16 CI matrix; D-17 no signing; D-13a windows-11-arm asserts typed no-binary error, not jwlCore load; D-15 unwrap ban)
    - .planning/phases/01-open-view-save-foundation-slice/01-RESEARCH.md (State of the Art windows-11-arm GA 2025-08-07, public-repo only — verify repo visibility; A2 macOS dylib arch)
    - .github/workflows/build_linux.yml (existing runner selection to borrow; Python/PyInstaller steps NOT reused)
    - .planning/phases/01-open-view-save-foundation-slice/01-VALIDATION.md (QA-03, PLAT-01 rows; full suite command)
    - app/package-lock.json + app/vitest.config.ts (01-01 — committed lockfile + vitest config so `npm ci && npm test` works in CI; finding 9)
  </read_first>
  <action>
    Add `.github/workflows/app-ci.yml` with a matrix over `windows-latest`, `windows-11-arm`, `ubuntu-latest`, `macos-latest`, each running: rust toolchain setup, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, and a frontend `npm ci && npm run build && npm test` (vitest, against 01-01's committed package-lock + config). For any step that uses Unix shell tooling (`grep`, `test`, bash conditionals), set `shell: bash` EXPLICITLY on that step (finding 8) so `windows-latest`/`windows-11-arm` (whose default shell is PowerShell) do not fail on `grep`/`test`. Install the Linux system deps Tauri needs (webkit2gtk) on ubuntu. Do NOT add code signing (Phase 11) and do NOT run a full `tauri build` bundle in CI legs beyond what fits the runner budget. Add `app/src-tauri/clippy.toml` and/or crate lint attributes disallowing `unwrap`/`expect` on archive-data paths (D-15). Confirm this repo's visibility supports the free-tier `windows-11-arm` runner; if the repo is private, note it in the SUMMARY as a blocker for that leg. The `windows-11-arm` job's jwlCore expectation is deferred to 01-03 (typed no-binary error), so this workflow only needs build+test green there.
  </action>
  <verify>
    <automated>bash -c 'test -f .github/workflows/app-ci.yml && grep -q "windows-11-arm" .github/workflows/app-ci.yml && grep -q "clippy -- -D warnings" .github/workflows/app-ci.yml && grep -q "shell: bash" .github/workflows/app-ci.yml'</automated>
    <human-check>Push the branch and confirm all four matrix legs go green in GitHub Actions.</human-check>
  </verify>
  <acceptance_criteria>
    - Workflow file exists with all four runner labels and fmt+clippy+test+build steps.
    - Steps using grep/test/bash conditionals set `shell: bash` explicitly (Windows-runner safe).
    - clippy unwrap/expect ban is enforced in CI (`-D warnings`); `npm ci` uses the committed lockfile.
    - No signing steps present (deferred to Phase 11).
    - CI run is green on all four legs (or private-repo arm64 caveat recorded).
  </acceptance_criteria>
  <done>Every push builds + tests on the full four-platform matrix with fmt/clippy gates and Windows-safe shell selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file -> Rust core | Crafted `.jwlibrary` (zip-slip, type-confused manifest) crosses into extract/parse |
| package registries -> build | crates.io / npm deps enter the trusted build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | archive/extract.rs (01-07) | mitigate | Exact `zip` `2.x >= 2.3` pin + 6-variant crafted zip-slip fixture proving rejection of ../, absolute unix, absolute Windows, backslash, duplicate, symlink-chain |
| T-02-02 | Tampering | archive/manifest.rs | mitigate | Strict serde struct parse; type-confused schemaVersion rejected; v16-only gate rejects un-upgraded v12-15 |
| T-02-03 | Repudiation | CI provenance | accept | No artifact signing in Phase 1 (PLAT-02 is Phase 11); CI only builds+tests |
| T-02-04 | Information Disclosure | tests/fixtures.rs | mitigate | Zip-slip fixture is synthetic/crafted, contains no user data (D-06) |
| T-02-SC | Tampering | cargo/npm installs in CI | mitigate | `npm ci` against 01-01's committed lockfile; exact-pinned crates + committed Cargo.lock; long-established deps only |
</threat_model>

<verification>
- `bash -c 'cd app/src-tauri && cargo fmt --check && cargo clippy -- -D warnings && cargo test'` green including manifest (v16-only) + 6-variant zip-slip tests.
- CI green on windows-latest, windows-11-arm, ubuntu-latest, macos-latest.
</verification>

<success_criteria>
Manifest is byte-compatible, hash-last, and v16-only; all six zip-slip variants are provably rejected; CI enforces build+fmt+clippy+test on all four platforms every push with Windows-safe shell selection.
</success_criteria>

<output>
Create `.planning/phases/01-open-view-save-foundation-slice/01-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
