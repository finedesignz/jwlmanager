---
phase: 04-schema-downgrade
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/src-tauri/src/db/delete.rs
  - app/src-tauri/src/archive/downgrade.rs
  - app/src-tauri/src/archive/save.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/downgrade_orchestration_tests.rs
autonomous: true
requirements: [SCHEMA-03, SCHEMA-05]
must_haves:
  truths:
    - "The user's working session stays at v16 after a v14-compatible save (PRAGMA user_version + manifest.schema_version both 16, session.db_path bytes unchanged)"
    - "The v14 bytes are written to the user's chosen output via the atomic-save path, reading from the throwaway copy — never re-trimming or touching the live session"
    - "A dry-run preview matches the actual save: same trim-first order (same survivor), exact repointed FK-row counts, and dedup-DELETED study rows surfaced as data loss"
  artifacts:
    - path: "app/src-tauri/src/archive/downgrade.rs"
      provides: "dry_run_downgrade (trim-first, exact repoint + dedup-delete counts) + save_v14_copy throwaway-copy orchestration"
      contains: "pub fn save_v14_copy"
    - path: "app/src-tauri/src/archive/save.rs"
      provides: "lower-level write-from-db-source helper reused by save_v14_copy without touching the session"
      contains: "db_source"
    - path: "app/src-tauri/src/lib.rs"
      provides: "downgrade_dry_run + save_v14_copy Tauri commands, registered"
      contains: "save_v14_copy"
  key_links:
    - from: "app/src-tauri/src/archive/downgrade.rs"
      to: "app/src-tauri/src/archive/save.rs"
      via: "v14 copy bytes written through a session-untouching write helper"
      pattern: "db_source|rebuild_zip"
    - from: "app/src-tauri/src/archive/downgrade.rs"
      to: "std::fs::copy"
      via: "throwaway copy of the working DB, never the live session"
      pattern: "fs::copy"
---

<objective>
Add the throwaway-copy orchestration that makes a v14 save non-destructive to the live session (SCHEMA-05, D4-06), the dry-run preview reusing Phase 2's `DryRunReport` (D4-08) that FAITHFULLY matches the save, and the two Tauri commands the frontend (Wave 3) calls. Downgrade is lossy (merges Locations, deletes dedup rows) — it MUST run on a throwaway copy so the user keeps editing at v16, and the preview MUST show the real effect.

Purpose: SCHEMA-03 (explicit opt-in save path) + SCHEMA-05 (session stays v16).
Output: `dry_run_downgrade`, `save_v14_copy`, a session-untouching write helper, commands `downgrade_dry_run`/`save_v14_copy`, orchestration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-schema-downgrade/04-CONTEXT.md
@.planning/phases/04-schema-downgrade/04-RESEARCH.md
@app/src-tauri/src/db/delete.rs
@app/src-tauri/src/archive/save.rs
@app/src-tauri/src/session.rs
@app/src-tauri/src/archive/downgrade.rs

<interfaces>
From app/src-tauri/src/db/delete.rs (REUSE):
```rust
pub struct DryRunReport {
    pub added: BTreeMap<String, usize>,
    pub overwritten: BTreeMap<String, usize>,
    pub deleted: BTreeMap<String, usize>,   // dedup-DELETED study rows go HERE (HIGH-3), not overwritten
    pub total_deleted: usize,
}
// currently PRIVATE — this plan makes them pub(crate):
fn snapshot_all(tx) -> Result<BTreeMap<String,HashSet<i64>>, ArchiveError>
fn diff_snapshots(before, after) -> DryRunReport
const TRACKED_TABLES: &[(&str,&str)]
```

From app/src-tauri/src/db/trim.rs (REUSE):
```rust
pub fn trim_sweep(tx: &Transaction) -> Result<(), ArchiveError>;  // VACUUM-free, rollback-safe (dry-run)
pub fn trim_db(conn: &mut Connection) -> Result<(), ArchiveError>; // trim + VACUUM (real save path)
```

From app/src-tauri/src/archive/save.rs (MED-5 — the CURRENT save path trims session.db_path and reads bytes from it; save_v14_copy must NOT reuse it as-is):
```rust
// save_archive_to trims session.db_path (line ~207), reads userData.db bytes from session.db_path (rebuild_zip ~108),
// and writes manifest.json into session.temp_dir (~223). ALL THREE would mutate the live session.
pub fn save_archive_to(session, target_path, app_name, device_name, now_iso8601) -> Result<Manifest, ArchiveError>;
fn rebuild_zip(session, manifest_bytes, temp_zip_path) -> Result<(), ArchiveError>; // reads session.db_path
fn update_manifest(db_path, existing, app_name, device_name, now_iso8601) -> Result<Manifest, ArchiveError>;
// same_dir_temp_path + atomic_replace — reuse the atomic guarantee
```

From app/src-tauri/src/session.rs:
```rust
pub struct ArchiveSession { pub temp_dir: TempDir, pub source_path, pub target_path,
    pub db_path: PathBuf, pub manifest: ManifestMeta, pub entries: Vec<ZipEntryMeta>, pub dirty: bool }
```
</interfaces>

<constraints>
- D4-06 / MED-5: NEVER trim, mutate, or read-manifest-into `session.db_path` / `session.temp_dir`. The downgrade + all writes operate on a `std::fs::copy` throwaway and an output-side temp manifest.
- Trim BEFORE downgrade (Python :1245). BOTH the real save and the dry-run put trim FIRST (HIGH-2): real save `trim_db`(copy) then `downgrade_to_v14`(copy); dry-run `trim_sweep`(tx) then group/remap/rebuild — identical order so the survivor selection matches.
- Write v14 bytes through the atomic-save path (same-dir temp + atomic rename), never hand-rolled, never delete-then-rename. Non-userData.db entries stream from `session.temp_dir` read-only.
- Output manifest schema_version = 14 (reads the copy's PRAGMA). SESSION manifest stays 16.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dry_run_downgrade — trim-FIRST, exact repoint counts, dedup-delete surfaced</name>
  <files>app/src-tauri/src/db/delete.rs, app/src-tauri/src/archive/downgrade.rs</files>
  <behavior>
    - On the Wave-1 collision fixture: `deleted["Location"] == 2` (merged-away); for each remap target the EXACT repointed-row count; dedup-DELETED rows appear in `deleted[<table>]` (data loss), NOT hidden in `overwritten`; the working DB is unchanged afterward (uv=16, row counts intact).
    - Survivor shown in preview == survivor the real save produces, even when the lowest-id group member is a trim-eligible unused Location (HIGH-2).
  </behavior>
  <action>
Make `snapshot_all`/`diff_snapshots`/snapshot helper `pub(crate)` in delete.rs (refactor `snapshot_all` to delegate to `pub(crate) fn snapshot_tables(tx, tables)`). Do NOT copy-paste the diff logic.

Implement `pub fn dry_run_downgrade(conn: &mut Connection) -> Result<DryRunReport, ArchiveError>` mirroring `dry_run_delete_notes` (delete.rs:206): `PragmaGuard`, FK off + sweep pragmas, `unchecked_transaction`. **Order inside the tx (HIGH-2 — trim FIRST, identical to `save_v14_copy`):**
1. `trim_sweep(&tx)` FIRST (VACUUM-free; matches the real save's `trim_db`-before-downgrade order so an unused lowest-id Location is removed BEFORE grouping and cannot be wrongly chosen as survivor).
2. Snapshot `Location` (+ trim/dedup-affected single-PK tables: Bookmark, InputField, TagMap, PlaylistItemLocationMap, Note, UserMark, Tag) PKs as `before`.
3. **Exact repoint counts (W1):** compute the merged old-id set the SAME way `run_downgrade_ddl` groups (post-trim), then per remap target `SELECT COUNT(*) FROM <table> WHERE <col> IN (<old ids>)` (separate count for Bookmark.PublicationLocationId). Record these as the repointed total per target.
4. **Dedup-delete counts (HIGH-3):** BEFORE running the remap, for each of the four composite-key targets compute the rows the dedup DELETE will remove: `SELECT COUNT(*) FROM <table> WHERE <col>=<old> AND <secondarykey> IN (SELECT <secondarykey> FROM <table> WHERE <col>=<keep>)` summed over merged groups. These are DELETED study rows, real data loss.
5. Run the REAL `run_downgrade_ddl` (group/remap/preflight/rebuild) on the tx.
6. Re-snapshot as `after`; `diff_snapshots` gives genuine `deleted`/`added` (incl. trim orphans + the dedup deletions, since those PKs vanish, AND the merged-away Location rows).
7. **Assemble the report:** `deleted[table]` from the diff already includes dedup-deleted + trim-deleted rows (correct — data loss surfaced). Set `overwritten[<target>]` = (repointed count from step 3) MINUS (dedup-deleted from step 4) so overwritten reflects rows that MOVED (kept), and `deleted[<target>]` reflects rows LOST — never double-count. Assert in tests that dedup-deleted rows land in `deleted`, not `overwritten`. Then DROP the tx (rollback) + guard. Never VACUUM.

Document: dry-run uses `trim_sweep`; real save uses `trim_db` (VACUUM) — identical net rows + identical order, only file-defrag differs.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test schema_downgrade_tests dry_run && cargo test --lib delete</automated>
  </verify>
  <done>dry_run_downgrade: trim runs FIRST; deleted["Location"]==2; exact per-target repoint counts asserted by value; dedup-deleted rows surfaced in `deleted[table]`; DB byte-unchanged after; delete.rs unit tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: session-untouching write helper + save_v14_copy</name>
  <files>app/src-tauri/src/archive/save.rs, app/src-tauri/src/archive/downgrade.rs</files>
  <behavior>
    - After `save_v14_copy(session, target)`: OUTPUT at `target` is a valid v14 archive (uv=14, Specialty/Edition gone, Locations merged); SESSION `session.db_path` PRAGMA uv still 16, `session.manifest.schema_version` still 16, AND `session.db_path` file bytes are byte-identical to before the call (MED-5); `session.temp_dir/manifest.json` unchanged.
  </behavior>
  <action>
**MED-5 — extract a session-untouching writer in save.rs.** Add `pub(crate) fn write_archive_from_db_source(session: &ArchiveSession, db_source: &Path, target_path: &Path, app_name, device_name, now_iso8601) -> Result<Manifest, ArchiveError>` that:
  (a) reads userData.db bytes from `db_source` (the throwaway copy), NOT `session.db_path` — parameterize `rebuild_zip` (or a copy of it) on a `db_source: &Path`;
  (b) does NOT run `trim_db` (the copy is already trimmed by save_v14_copy);
  (c) builds the manifest from `db_source` (hash + schema_version read off the copy) WITHOUT writing manifest.json into `session.temp_dir` — hold the manifest bytes in memory and stream them straight into the output zip (the existing code's `fs::write(&manifest_path, ...)` into temp_dir MUST be skipped on this path);
  (d) streams every other entry from `session.temp_dir` READ-ONLY;
  (e) writes to a same-dir temp of `target_path` + `atomic_replace` (reuse existing helpers).
Refactor the existing `save_archive_to` to delegate to this helper for its own path (passing `session.db_path` as `db_source` AFTER its trim), to avoid divergence — but keep its trim + temp_dir manifest-sync behavior for the normal-save path only. Confirm the normal-save tests still pass.

Implement `pub fn save_v14_copy(session: &ArchiveSession, target: &Path, app_name, device_name, now_iso8601) -> Result<(), ArchiveError>`:
1. `let copy_path = session.temp_dir.path().join("userData_v14.db"); std::fs::copy(&session.db_path, &copy_path)?;` — NEVER mutate `session.db_path`.
2. Open the COPY: `trim_db(&mut conn)` THEN `downgrade_to_v14(&mut conn)` (trim-first, on the copy). A `SchemaDowngradeFailed` from an un-downgradeable archive (HIGH-1) propagates here → `target` never written, copy discarded, session untouched.
3. `write_archive_from_db_source(session, &copy_path, target, ...)`.
4. Best-effort remove `copy_path`.
5. Do NOT set `session.dirty` / `session.target_path` (D4-07 — side export).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test downgrade_orchestration_tests && cargo test --test save_tests</automated>
  </verify>
  <done>write_archive_from_db_source never trims or writes into session.temp_dir/db_path; save_v14_copy produces a v14 output; session.db_path bytes + manifest unchanged; existing save_tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: orchestration tests + Tauri commands</name>
  <files>app/src-tauri/tests/downgrade_orchestration_tests.rs, app/src-tauri/src/lib.rs</files>
  <action>
Create `tests/downgrade_orchestration_tests.rs`:
1. **Session stays v16 + untouched (D4-06, MED-5):** open a session from a v16 collision fixture, SHA-256 `session.db_path` before, call `save_v14_copy` to a scratch target → assert `session.db_path` PRAGMA uv==16, `session.manifest.schema_version==16`, and the `session.db_path` hash is UNCHANGED (bytes identical); OUTPUT userData.db uv==14, Specialty/Edition gone, Locations merged to lowest id.
2. **Dry-run matches save (HIGH-2, HIGH-3, W1, D4-08):** run `dry_run_downgrade` on a connection to `session.db_path` → assert exact `deleted["Location"]` + per-target repoint counts by value; assert dedup-deleted rows appear in `deleted[table]` (not overwritten); assert working db uv still 16 + row counts unchanged. Include a fixture where the lowest-id group member is a trim-eligible UNUSED Location → assert the preview survivor == the survivor `save_v14_copy` actually produces (trim-first order).
3. **Un-downgradeable surfaces cleanly (HIGH-1 end-to-end):** a session on the HIGH-1 un-downgradeable fixture → `save_v14_copy` returns `SchemaDowngradeFailed` (via its ErrorDto at the command), NO output file at target, session still uv=16 + bytes unchanged.
4. **Output round-trips:** `open_and_validate` the v14 output reopens (upgrades v14→v16 per Phase 3), merged-Location state preserved semantically via `normalized_table_rows`, never byte-diff.

Add two Tauri commands to lib.rs (mirror `delete_notes_dry_run` + `save_as`):
- `downgrade_dry_run(state) -> Result<DryRunReport, ErrorDto>`: opens `session.db_path`, delegates to `dry_run_downgrade`, op label `"downgrade_dry_run"`.
- `save_v14_copy(path: String, state) -> Result<(), ErrorDto>`: locks session (`as_ref` — NOT mutated), delegates to `archive::downgrade::save_v14_copy` with `APP_NAME`/`APP_DEVICE_NAME`/`now_iso8601_utc`, op label `"save_v14_copy"`, file arg = target path.
Register both in `invoke_handler![...]`.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test downgrade_orchestration_tests && cargo build</automated>
  </verify>
  <done>Commands registered + compile; orchestration tests green incl. session-untouched (byte hash), dry-run-matches-save (trim-first survivor + dedup-delete surfaced), and un-downgradeable-no-output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frontend → Tauri command | untrusted `path` string for the v14 output target |
| live session DB → throwaway copy | the boundary that keeps the session v16 + byte-untouched |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | live session mutated by downgrade/save reuse | mitigate | `fs::copy` + `write_archive_from_db_source` (no trim, no temp_dir manifest write); session-byte-hash test (MED-5) |
| T-04-06 | Denial of Service | partial/truncated v14 output on failure | mitigate | atomic-save path; un-downgradeable → no output (HIGH-1 end-to-end test) |
| T-04-07 | Information disclosure | error reason to IPC | mitigate | ErrorDto boundary, reason stays internal |
| T-04-11 | Repudiation | preview diverges from actual save | mitigate | trim-first order (HIGH-2) + exact repoint (W1) + dedup-delete surfaced (HIGH-3); dry-run-matches-save test |
| T-04-SC | Tampering | cargo installs | mitigate | no new deps |
</threat_model>

<verification>
- `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
- Full `cargo test` green (incl. existing save_tests after the rebuild_zip refactor).
</verification>

<success_criteria>
A v14-compatible archive is written to the user's chosen path via a session-untouching atomic write, colliding Locations merged, while the live session provably stays byte-identical at v16. The preview matches the save exactly: trim-first survivor, exact per-target repoint counts, and dedup-DELETED study rows surfaced as data loss. Un-downgradeable archives fail with a typed error and write nothing. No byte-diffing.
</success_criteria>

<output>
Create `.planning/phases/04-schema-downgrade/04-02-SUMMARY.md` when done.
</output>
