---
phase: 04-schema-downgrade
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - app/src/components/CommandBar.tsx
  - app/src/components/CommandBar.test.tsx
  - app/src/components/DeletePreviewDialog.tsx
  - app/src/App.tsx
  - app/src-tauri/tests/differential.rs
autonomous: true
requirements: [SCHEMA-03, SCHEMA-04]
must_haves:
  truths:
    - "A user can trigger an explicit 'Save v14-compatible copy…' action, distinct from Save/Save As, that shows a merge preview before writing"
    - "The preview reports how many Locations will be merged before any bytes are written"
    - "A Rust-downgraded archive is accepted by the Python app's check_validity"
    - "Python's own downgrade_schema on the same fixture yields semantically equivalent v14 (normalized, not literal survivor ids)"
  artifacts:
    - path: "app/src/components/CommandBar.tsx"
      provides: "Save v14-compatible copy action wired to downgrade_dry_run + save_v14_copy"
      contains: "save_v14_copy"
    - path: "app/src-tauri/tests/differential.rs"
      provides: "downgrade differential oracle (Rust-downgraded accepted by Python)"
      contains: "downgrade"
  key_links:
    - from: "app/src/components/CommandBar.tsx"
      to: "downgrade_dry_run / save_v14_copy commands"
      via: "invoke, preview-then-confirm via DeletePreviewDialog"
      pattern: "invoke.*save_v14_copy"
    - from: "app/src-tauri/tests/differential.rs"
      to: "JWLManager.Window.check_validity"
      via: "Python oracle accepts the Rust-downgraded v14 archive"
      pattern: "check_validity"
---

<objective>
Deliver the user-facing "Save v14-compatible copy…" action (SCHEMA-03, D4-07) with a merge preview reusing Phase 2's `DeletePreviewDialog`, and extend the Python differential oracle to prove a Rust-downgraded archive is accepted by the real JW Library ecosystem and is semantically equivalent to Python's own downgrade (D4-10).

Purpose: close the phase with the explicit opt-in UI + the strongest cross-implementation verification.
Output: CommandBar v14 action + preview wiring + vitest coverage; `differential.rs` downgrade oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-schema-downgrade/04-CONTEXT.md
@.planning/phases/04-schema-downgrade/04-RESEARCH.md
@app/src/components/CommandBar.tsx
@app/src/components/DeletePreviewDialog.tsx
@app/src-tauri/tests/differential.rs

<interfaces>
Backend commands available (from Plan 04-02):
- `downgrade_dry_run() -> DryRunReport` (no args; uses open session)
- `save_v14_copy(path: string) -> void`

From CommandBar.tsx (existing pattern to extend):
```tsx
type ActionName = "open" | "new" | "save" | "saveAs";  // add "saveV14"
const FILTERS = [{ name: "JW Library Backup", extensions: ["jwlibrary"] }];
// runAction(name, action): disables all buttons while pending + busyRef double-click guard
// handleSaveAs: save({filters}) → invoke("save_as", {path: target}) → onSaved()
// dismissed dialog (null target) = clean cancel, no error banner
```

From DeletePreviewDialog.tsx (REUSE — general per D2-07):
```tsx
interface DeletePreviewDialogProps {
  report: DryRunReport;         // renders report.deleted per-table counts
  onConfirm: () => Promise<void>;
  onCancel: () => void;
}
```
The dialog currently hardcodes Notes-delete copy ("Delete these Notes?"). Generalize its copy to be caller-driven (add optional `title`/`summaryPrefix`/`confirmLabel` props with the existing Notes strings as defaults) so the v14 flow can say e.g. "Save v14-compatible copy?" and frame `deleted["Location"]` as "N Locations will be merged". Do NOT fork the component.

From differential.rs (extend — mirror `python_app_opens_upgraded_v14_archive`):
```rust
fn run_python_check_validity(archive_path: &Path) -> (bool, String, String);  // reuse
// common::generate_* fixture helpers; open_and_validate; save paths
```
</interfaces>

<constraints>
- D4-07: the v14 action is SEPARATE from Save/Save As, never the default. Own confirmation (the preview dialog IS the confirmation).
- Preview-then-write: call `downgrade_dry_run` FIRST, show the dialog, and only call `save_v14_copy` on Confirm. Cancel writes nothing.
- Reuse the busyRef double-click guard + dismissed-dialog-is-clean-cancel pattern already in CommandBar.
- Differential oracle stays `#[ignore]`d in CI (Rust-only matrix, no PySide6) exactly like the existing two — never a silent pass; document as a RECORDED MANUAL GATE.
- Normalized comparison (A2): Python may pick a different survivor id; compare dependent-count-per-surviving-key, NOT literal LocationIds.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize DeletePreviewDialog copy (caller-driven)</name>
  <files>app/src/components/DeletePreviewDialog.tsx</files>
  <action>
Add optional props `title?`, `bodyRenderer?` (or `summary?` string), and `confirmLabel?` to `DeletePreviewDialogProps`, defaulting to the current Notes strings so existing usage + tests are unchanged. Render the provided title/summary when given. Keep the calm card styling, restrained-red confirm, `aria-modal`, `data-testid` hooks, and the busyRef double-click guard exactly as-is. This is a copy-generalization only — no behavior change to the delete flow.
  </action>
  <verify>
    <automated>cd app && npm test -- DeletePreviewDialog</automated>
  </verify>
  <done>Existing DeletePreviewDialog tests pass unchanged; new optional props typecheck and override copy when supplied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire "Save v14-compatible copy…" action in CommandBar</name>
  <files>app/src/components/CommandBar.tsx, app/src/components/CommandBar.test.tsx, app/src/App.tsx</files>
  <action>
Add `"saveV14"` to `ActionName`. Add a `handleSaveV14` (mirror `handleSaveAs`): via `runAction("saveV14", ...)` — `save({ filters: FILTERS, ... })` for the target path (dismissed = clean cancel), then `invoke<DryRunReport>("downgrade_dry_run")`, then surface the reused `DeletePreviewDialog` (title "Save v14-compatible copy?", summary framing `report.deleted["Location"] ?? 0` as "N Locations will be merged for v14 compatibility", confirm label "Save v14 copy"). On Confirm → `invoke("save_v14_copy", { path: target })` then `onSaved()`/a success signal; on Cancel → no-op. Disable the action (like Save/Save As) when no archive is open. Add the toolbar button labeled per 01-UI-SPEC style (secondary, not primary — it is an export, not the main save).

Lift the preview dialog state into CommandBar (or App.tsx, matching how the existing delete preview is hosted — inspect the current DeletePreviewDialog host and follow that pattern). Add CommandBar.test.tsx cases: (a) clicking Save v14 calls `downgrade_dry_run` then shows the dialog; (b) Confirm calls `save_v14_copy` with the chosen path; (c) Cancel calls neither `save_v14_copy`; (d) dismissed save dialog = no invoke, no error; (e) the action is disabled with no archive open. Mock `@tauri-apps/api/core` invoke + `@tauri-apps/plugin-dialog` save as the existing tests do.
  </action>
  <verify>
    <automated>cd app && npm test -- CommandBar && npm run build</automated>
  </verify>
  <done>Save v14 action previews-then-writes, reuses DeletePreviewDialog, honors cancel/dismiss/disabled; CommandBar tests + tsc/vite build green.</done>
</task>

<task type="auto">
  <name>Task 3: Downgrade differential oracle</name>
  <files>app/src-tauri/tests/differential.rs</files>
  <action>
Add `python_app_opens_downgraded_v14_archive` mirroring `python_app_opens_upgraded_v14_archive` (differential.rs:100), `#[ignore]`d with the same RECORDED-MANUAL-GATE reason string (CI is Rust-only, no PySide6):
1. Generate a v16 collision fixture (reuse the Wave-1 `common::generate_v16_collision_fixture`), open a session via `open_and_validate`, call `archive::downgrade::save_v14_copy` to a scratch target.
2. Assert the OUTPUT on-disk userData.db PRAGMA user_version == 14 before handing off.
3. `run_python_check_validity(&target)` → assert Python's `check_validity` ACCEPTS the Rust-downgraded archive.
4. **Normalized equivalence (A2):** in the same test (or a sibling), shell to Python to run the app's OWN `downgrade_schema` on the SAME v16 fixture (unbound call pattern like check_validity, or a small helper script), then compare NORMALIZED state — dependent-row-count grouped by the surviving Location's v14 key — between the Rust output and the Python output. Do NOT assert equal literal survivor LocationIds (Python's non-deterministic `ids[0]` may differ). If invoking Python's `downgrade_schema` headlessly is impractical (it is a nested closure inside a Qt method), document that limitation and fall back to asserting the Rust output satisfies the v14 invariants Python's check_validity enforces + explicit normalized-key dependent counts on the Rust output alone — never a silent skip.
5. Extend the env-gated `real_archive_round_trip_env_gated` note (or add a gated leg) for a real v16 archive downgraded + Python-accepted, guarded by `JWLM_REAL_ARCHIVE` + python3 availability (skip visibly, never fail, when absent).
Update the test doc comments with STATUS once run locally; if not run in this environment, mark clearly as NOT-YET-VERIFIED (never claim VERIFIED PASSING without a real run).
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test --test differential -- --list</automated>
  </verify>
  <done>Downgrade oracle compiles and is discoverable (listed), `#[ignore]`d with a clear reason; when run with PySide6 installed it asserts Python accepts the Rust-downgraded v14 archive + normalized equivalence. Manual-gate status recorded honestly in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user → v14 export action | explicit opt-in; must never be the implicit Save |
| Rust downgrade output → Python/JW Library | cross-implementation acceptance boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Repudiation | user unaware a save is lossy/merging | mitigate | preview dialog surfaces "N Locations will be merged" before any write (D4-08) |
| T-04-09 | Tampering | v14 output not actually JW-Library-valid | mitigate | Python check_validity differential oracle (RECORDED MANUAL GATE) |
| T-04-10 | Elevation of privilege | v14 action fired without archive open | mitigate | action disabled when no session; test asserts |
| T-04-SC | Tampering | npm installs | mitigate | no new deps |
</threat_model>

<verification>
- `npm test` (vitest) green incl. new CommandBar + DeletePreviewDialog cases.
- `npm run build` clean; `cargo test --test differential -- --list` shows the new oracle.
- Manual gate (run locally with res/requirements.txt + staged DLLs): `cargo test --test differential -- --ignored` accepts the Rust-downgraded archive.
</verification>

<success_criteria>
The user can explicitly export a v14-compatible copy, sees how many Locations will merge before committing, and the resulting archive is accepted by Python's own check_validity — semantically equivalent (normalized) to Python's downgrade. No byte-diffing.
</success_criteria>

<output>
Create `.planning/phases/04-schema-downgrade/04-03-SUMMARY.md` when done.
</output>
