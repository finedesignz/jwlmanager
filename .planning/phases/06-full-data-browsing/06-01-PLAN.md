---
phase: 06-full-data-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src-tauri/src/db/labels.rs
  - app/src-tauri/src/db/notes.rs
  - app/src-tauri/src/db/mod.rs
  - app/src-tauri/src/lib.rs
  - app/src/bindings/BrowseRow.ts
  - app/src/components/NotesList.tsx
  - app/src/components/NotesList.test.tsx
  - app/src/components/CommandBar.tsx
  - app/src/App.tsx
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, DATA-05, DATA-06]
must_haves:
  truths:
    - "Opening an archive still lists Notes with resources.db-synthesized labels — zero regression (the living Notes tracer stays green)."
    - "The process_code/process_detail/process_color/resolve_publication label math lives once in db/labels.rs and is imported, never duplicated per category (D6-01)."
    - "One unified BrowseRow is the row type over IPC; NotesRow no longer exists as a separate struct (D6-02)."
  artifacts:
    - "app/src-tauri/src/db/labels.rs"
    - "app/src/bindings/BrowseRow.ts"
  key_links:
    - "notes.rs imports labels::process_* (pub(crate)), not private copies."
    - "open_archive/list_notes return Vec<BrowseRow>; frontend importers reference the BrowseRow binding."
---

<objective>
Enabling refactor for the whole phase: lift the shared label-synthesis helpers out of `db/notes.rs` into a reusable `db/labels.rs` module, and generalize the Notes-only `NotesRow` into the single unified `BrowseRow` schema every category collapses to (the Python `merge_df` frame). No new user capability — this is the foundation the five category getters (06-02) and the generalized frontend (06-03/06-04) build on.

The already-shipped Notes browse+delete slice is the *living tracer* for this phase's architecture (SQL → labels → row struct → IPC → virtualized list → selection). This plan proves the refactor keeps that whole slice intact by keeping the existing Notes + frontend suites green after the extraction and rename — an architectural regression here surfaces on the first commit, not ten layers later.

Purpose: eliminate the six-divergent-copies risk (D6-01) and multiply-bound ts-rs bindings (D6-02) before any category work begins.
Output: `db/labels.rs` (pub(crate) helpers + their moved unit tests), a `BrowseRow` struct + `BrowseRow.ts` binding superseding `NotesRow`, and all Rust/TS consumers updated with the suite still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-data-browsing/06-CONTEXT.md
@.planning/phases/06-full-data-browsing/06-RESEARCH.md
@app/src-tauri/src/db/notes.rs
@app/src-tauri/src/db/mod.rs
@app/src-tauri/src/db/resources.rs
@app/src-tauri/src/lib.rs
@app/src/components/NotesList.tsx
@app/src/components/CommandBar.tsx
@app/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract db/labels.rs — shared label-synthesis module (D6-01)</name>
  <files>app/src-tauri/src/db/labels.rs, app/src-tauri/src/db/notes.rs, app/src-tauri/src/db/mod.rs</files>
  <action>
Create `app/src-tauri/src/db/labels.rs` and MOVE (not copy) from `db/notes.rs`, verbatim, per D6-01: the `CODE_YR`/`CODE_JWB` `LazyLock<Regex>` statics, the `DATED_PREFIX_EXCLUDED`/`BIBLE_APPENDIX_SYMBOLS`/`COLOR_NAMES` const arrays, and the four functions `process_code`, `process_color`, `process_detail`, `resolve_publication`. Mark each moved item `pub(crate)` so both `notes.rs` and the future `db/browse.rs` can call them. These are byte-for-byte the same math every category runs — the Python defines them ONCE as closures inside `regroup` (`JWLManager.py:578-639`) and all six getters call them; do not leave a private copy behind in `notes.rs`.

Move the label-math unit tests (`process_code_*`, `process_detail_*` — currently the `#[cfg(test)] mod tests` block in `notes.rs` that exercises the helpers) into `labels.rs` alongside their subjects; keep the `#![allow(clippy::unwrap_used, clippy::expect_used)]` test-module posture and the `dev_resources_db_path` catalog helper. Register the module in `db/mod.rs` with `pub mod labels;`.

In `notes.rs`, replace the removed definitions with `use crate::db::labels::{process_code, process_color, process_detail, resolve_publication};` and delete the now-unused `regex`/`LazyLock` imports if nothing else in `notes.rs` uses them. Do NOT change any label-synthesis behavior — this is a pure move. The crate-level `#![deny(clippy::unwrap_used, clippy::expect_used)]` still applies to `labels.rs` non-test code, so the `#[allow(clippy::expect_used)]` on the regex `expect("...")` literals must move with them.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test -p jwlmanager_lib labels && cargo test --test notes_query_tests</automated>
  </verify>
  <done>`db/labels.rs` holds the four helpers + regexes + consts as `pub(crate)`; `notes.rs` imports them with no private duplicate; the moved label-math unit tests and `notes_query_tests` pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supersede NotesRow with the unified BrowseRow (D6-02)</name>
  <files>app/src-tauri/src/db/notes.rs, app/src-tauri/src/lib.rs, app/src/bindings/BrowseRow.ts, app/src/components/NotesList.tsx, app/src/components/NotesList.test.tsx, app/src/components/CommandBar.tsx, app/src/App.tsx</files>
  <behavior>
    - query_notes still returns the same located + independent rows, now typed BrowseRow, with language/color/tags/modified as Some(...) for Notes (Notes populates every column).
    - A category that does not produce a column leaves it None/absent — the type must be able to represent that (the merge_df fill_null analog); Notes exercises the all-present case here, the absent case lands with the getters in 06-02.
    - open_archive and list_notes return Vec<BrowseRow>; the existing open_archive integration test still yields the located Genesis note with detail1 "01: Genesis" and language "English".
    - The frontend still renders Notes label + tags + modified, and the Notes delete flow is unchanged.
  </behavior>
  <action>
Rename the `NotesRow` struct to `BrowseRow` in `db/notes.rs` and generalize it into the single `merge_df` schema (D6-02): make `language`, `color`, `tags`, and `modified` each `Option<String>` (a category such as Bookmarks/Annotations/Favorites has no color/tags/modified; Playlists has no language) while keeping `year`/`detail1`/`detail2` as `Option<String>` and `short`/`full`/`type_group` as `String` (merge_df always fills these, even for Playlists) and keeping the `independent: bool` flag (only Notes sets it true). Update the `#[ts(export, export_to = "../../src/bindings/BrowseRow.ts")]` target so the generated binding is `BrowseRow.ts`; the old `NotesRow.ts` is superseded. In `query_located_notes`/`query_independent_notes`, wrap the now-optional fields in `Some(...)` (Notes always populates them) so behavior is byte-identical to today.

Update `lib.rs`: `open_archive` and `list_notes` return `Vec<BrowseRow>` (change the `use db::notes::NotesRow` import to `BrowseRow`, and the two `-> Result<Vec<...>, ErrorDto>` signatures). Do not change command bodies otherwise.

Update the frontend consumers to the regenerated binding: `NotesList.tsx`, `CommandBar.tsx`, and `App.tsx` import `BrowseRow` from `../bindings/BrowseRow` instead of `NotesRow`; the invoke generics `invoke<NotesRow[]>` become `invoke<BrowseRow[]>`. In `NotesList.tsx`, the label/tags/modified render must tolerate `null` now (render nothing when a field is null) — for Notes the fields are always present so there is no visual change; keep the fixed 44px no-wrap row contract untouched. Update `NotesList.test.tsx`'s `makeNote` factory and any type imports to the `BrowseRow` shape (nullable color/tags/modified) so the Notes suite stays green. Delete the stale `app/src/bindings/NotesRow.ts` file. Do NOT delete `NotesList.tsx` itself — it is replaced by `CategoryList` in a later plan (06-04), not here.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test && cd ../.. && npm --prefix app test</automated>
  </verify>
  <done>`BrowseRow` is the only row struct; `BrowseRow.ts` exists and `NotesRow.ts` is gone; `open_archive`/`list_notes` return `Vec<BrowseRow>`; the full Rust suite and full vitest suite are green with no `NotesRow` references remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive `userData.db` → Rust label synthesis | untrusted archive column values flow into `process_*`/catalog lookups |
| Rust → IPC → React | synthesized label strings cross to the renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `db/labels.rs` process_* over malicious archive strings | medium | mitigate | Labels are text-only; no SQL is built from them; publication/book/language resolve via `ResourceCatalog` HashMap lookups, never interpolation. Behavior is a verbatim move — unchanged from the tested Notes path. |
| T-06-02 | Denial of Service | refactored helpers on malformed archive rows | medium | mitigate | Crate-level `#![deny(clippy::unwrap_used, clippy::expect_used)]` keeps the archive-data path panic-free; lookups return `Option` with defaults. The only `expect` is on compile-time-constant regexes (moved with its `#[allow]`), not archive input. |
| T-06-SC | Tampering | npm/cargo dependency installs | high | accept | Phase 6 installs NO package (06-RESEARCH Package Legitimacy Audit: N/A). Verify no new entry appears in `app/package.json` or `app/src-tauri/Cargo.toml`. |
</threat_model>

<verification>
- `cd app/src-tauri && cargo test` — full Rust suite green (labels module tests + all integration tests, including `notes_query_tests` and `open_archive_tests`).
- `npm --prefix app test` — full vitest suite green (`NotesList.test.tsx`, `CommandBar.test.tsx`, etc.).
- No new dependency: `app/package.json` and `app/src-tauri/Cargo.toml` are unchanged (git diff shows no dependency additions).
- `app/src/bindings/NotesRow.ts` is deleted and `app/src/bindings/BrowseRow.ts` exists.
</verification>

<success_criteria>
Label helpers are shared from `db/labels.rs`, `BrowseRow` is the single unified row type end-to-end, and the shipped Notes browse+delete slice still passes its full Rust and TS suites with zero behavior change.
</success_criteria>

<output>
Create `.planning/phases/06-full-data-browsing/06-01-SUMMARY.md` when done.
</output>
