---
phase: 11-platform-polish
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - app/src/i18n/en.ts
  - app/src/lib/errors.ts
  - app/src/lib/errors.test.ts
  - app/src/components/ErrorBanner.tsx
  - app/src/components/ErrorBanner.test.tsx
  - app/src/components/JwlCoreNotice.tsx
  - app/src/components/CategorySwitcher.tsx
  - app/src/components/CategorySwitcher.test.tsx
  - app/src/components/CategoryList.tsx
  - app/src/components/CategoryList.test.tsx
  - app/src/components/ColorMenu.tsx
  - app/src/components/ColorMenu.test.tsx
  - app/src/components/UtilitiesMenu.tsx
  - app/src/components/UtilitiesMenu.test.tsx
  - app/src/settings/SettingsProvider.tsx
  - app/src/components/CommandBar.tsx
  - app/src/components/CommandBar.test.tsx
  - app/src/components/TagDialog.tsx
  - app/src/components/TagDialog.test.tsx
  - app/src/components/FavoriteAddDialog.tsx
  - app/src/components/FavoriteAddDialog.test.tsx
  - app/src/components/MediaAddDialog.tsx
  - app/src/components/MediaAddDialog.test.tsx
  - app/src/components/EditPreviewDialog.tsx
  - app/src/components/EditPreviewDialog.test.tsx
  - app/src/components/FoldMergeDialog.tsx
  - app/src/components/FoldMergeDialog.test.tsx
  - app/src/components/RecordEditor.tsx
  - app/src/components/RecordEditor.test.tsx
  - app/src/i18n/completeness.test.ts
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "PLAT-03's literal text ('all user-facing strings are localized') is satisfied across the WHOLE app, not just the two surfaces 11-03 proved the mechanism on -- every one of the 13 remaining components (CommandBar, TagDialog, FavoriteAddDialog, MediaAddDialog, EditPreviewDialog, FoldMergeDialog, RecordEditor, CategorySwitcher, CategoryList, ColorMenu, UtilitiesMenu, ErrorBanner, JwlCoreNotice) plus lib/errors.ts's describeError catalog renders exclusively through 11-03's t()."
    - "describeError becomes a pure function of (ErrorDto, t) -- it accepts the translation function as a parameter rather than importing a hook, since it is a plain lib function, not a component; every call site (ErrorBanner, SettingsProvider's own save-error banner) is updated to pass t from useI18n()."
    - "Category display text (D6-06/DATA-08) is translated for PRESENTATION ONLY -- CategorySwitcher and CategoryList continue to key control flow, onSelect payloads, and IPC arguments off the raw Category enum values exactly as before; only the rendered label goes through t()."
    - "Pluralized and value-interpolated strings (CommandBar's dry-run summaries, JwlCoreNotice's reason suffix) reuse the {token} substitution and split-around-JSX conventions 11-03 established -- no second templating mechanism is introduced."
    - "A structural completeness test scans all 13 retrofitted components plus errors.ts and fails if any of them contains a hardcoded JSX text node, aria-label/title/placeholder string literal, or native-dialog `filters[].name`/`title` string literal (the `open`/`save` dialog option objects in CommandBar, CategoryList, MediaAddDialog) outside the same narrow allowlist (product name, raw enum values used as testids, punctuation-only fragments) 11-03's completeness test already established. `defaultPath` (default save-dialog filename) literals are explicitly out of scope for both the retrofit and this test -- see task 2's scope decision -- so this test's coverage claim is 'all user-facing dialog chrome/label/title strings', not literally every string a native dialog call passes."
    - "describeError full coverage is proven exhaustive by deriving the real list of error codes from the Rust source of truth at test time (reading `src-tauri/src/error.rs`'s and `settings.rs`'s `to_dto` match arms as text and extracting every `(\"code\", \"message_key\")` tuple with a regex) -- NOT by hand-typing a duplicate code list and NOT by widening `ErrorDto.code` to a TS union, since `ErrorDto` stays `{ code: string, ... }` exactly as ts-rs already generates it."
    - "Switching language in Settings re-renders CommandBar, ErrorBanner (when an error is showing), and CategorySwitcher/CategoryList on the same interaction, proving the retrofit is live, not merely present in source."
  prohibitions:
    - "MUST NOT add any i18n npm package or Cargo dependency -- same D11-02 line as 11-03; zero new dependencies across eleven phases now."
    - "MUST NOT machine-translate de/es/fr/it/pl/pt/ru/uk -- they remain the same empty/sparse files 11-03 created; this plan only ADDS keys to en.ts, it does not touch the 8 locale files at all (Partial<Record<StringKey,string>> tolerates new keys with zero edits, by construction)."
    - "MUST NOT change any Category enum value, any IPC argument name, or any control-flow branch to read from a translated string -- DATA-08 forbids this; t() output is presentation-only, verified structurally in task 3."
    - "MUST NOT alter ErrorDto's shape, any Rust error code, or any Tauri command -- this plan is a pure frontend retrofit of already-sanitized, already-existing error copy. This includes NOT narrowing `ErrorDto[\"code\"]` from `string` to a TS union in the generated binding: `describeError full coverage` (task 3) instead derives its code list by reading `src-tauri/src/error.rs` and `settings.rs` as plain text at test time and regex-extracting every `(\"code\", \"message_key\")` tuple from their `to_dto` match arms -- genuinely exhaustive against the real Rust source of truth, zero hand-typed duplicate list, zero `ErrorDto`/binding change."
    - "MUST NOT introduce a second string-templating mechanism (ICU, ordinal plural rules, a new library) -- reuse the {token} substitution and manual plural-branch pattern already proven in 11-03 and evidenced in CommandBar's existing inline pluralization logic (e.g. `{n === 1 ? '' : 's'}`)."
    - "MUST NOT leave any of the 13 named components or lib/errors.ts partially retrofitted -- a component with even one remaining hardcoded string is a completeness-test failure, not a deferred item."
  artifacts:
    - app/src/lib/errors.ts
    - app/src/i18n/completeness.test.ts
  key_links:
    - "describeError(err, t) is called from every ErrorBanner instance in the tree; 11-03's fix moving SettingsProvider's own save-error ErrorBanner inside I18nProvider is what makes this call site reachable at all -- this plan does not re-plumb that, only consumes it."
    - "CategorySwitcher/CategoryList's t()-driven LABEL is strictly separate from the Category enum value driving onSelect/IPC payloads -- the same component renders both; the plan's acceptance criteria checks this separation explicitly, not just that a label renders."
    - "The completeness test in task 3 extends 11-03's own structural scan (same technique, same allowlist shape) to the full 13-component + errors.ts surface -- it is the single gate that proves PLAT-03 is done, not merely attempted."
---

<objective>
Finish PLAT-03: retrofit the 13 components 11-03 deliberately deferred, plus
`lib/errors.ts`'s ~37-case error copy catalog, onto the i18n architecture
11-03 already proved end to end -- so every user-facing string in the running
app, not just the App shell and Settings dialog, renders through `t()`.

Purpose: 11-03 proved the mechanism cheaply on two small surfaces. This plan
is the expansion half of the same tracer-first slice, applying the identical
API (`t()`, `{token}` substitution, the split-around-JSX convention) to the
much larger real surface RESEARCH's Pitfall 3 flagged as the actual
localization risk -- so nothing new is invented here, only extended.

Output: every component in `app/src/components/` and `describeError` render
exclusively through the catalog; a structural completeness test proves it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-platform-polish/11-CONTEXT.md
@.planning/phases/11-platform-polish/11-RESEARCH.md
@app/src/i18n/I18nContext.tsx
@app/src/i18n/strings.ts
@app/src/lib/errors.ts
@app/src/components/CommandBar.tsx
@app/src/bindings/ErrorDto.ts
@app/src-tauri/src/error.rs
@app/src-tauri/src/settings.rs
</context>

<scope_boundary>
Settled during planning; do not re-derive or re-litigate.

**Everything not covered by 11-03 belongs here.** The full component list:
`CommandBar.tsx`, `TagDialog.tsx`, `FavoriteAddDialog.tsx`, `MediaAddDialog.tsx`,
`EditPreviewDialog.tsx`, `FoldMergeDialog.tsx`, `RecordEditor.tsx`,
`CategorySwitcher.tsx`, `CategoryList.tsx`, `ColorMenu.tsx`, `UtilitiesMenu.tsx`,
`ErrorBanner.tsx`, `JwlCoreNotice.tsx`, plus `lib/errors.ts`. This list is
exhaustive of `app/src/components/*.tsx` minus `SettingsDialog.tsx` (done in
11-03). Do not treat any of these thirteen as optional or "already good
enough" -- the completeness test in task 3 checks all of them by name.

**No new templating mechanism.** 11-03 already proved `{token}` substitution
and the split-around-JSX pattern (App.tsx's empty-state sentence). CommandBar's
existing pluralization (`{n === 1 ? "" : "s"}`) stays exactly that shape --
move the literal English words into catalog templates with a `{plural}` token
the caller fills with `""` or `"s"`, do not invent ICU plural rules or a
pluralization library.

**describeError's signature changes -- decided.** It becomes
`describeError(err: ErrorDto, t: TFunction): string` (or an equivalent typed
parameter shape -- name it in the SUMMARY). It is a plain function, not a
component, so it cannot call `useI18n()` itself; every call site passes `t`
explicitly. This is the one signature change in the plan and it ripples to
every `ErrorBanner` call site -- enumerate them, do not assume there is only
one.

**Category identity stays enum-driven (DATA-08/D6-06) -- decided.**
`CategorySwitcher`/`CategoryList` translate only the rendered LABEL; the
`Category` union value itself, `onSelect` payloads, and every `list_category`/
`list_notes` IPC call keep using the raw enum exactly as today. Do not key
anything off a translated string.

**No Rust changes, no new dependency.** Same line as 11-03; this plan is
entirely `app/src/**`.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Error catalog + the six lower-complexity components</name>
  <files>app/src/i18n/en.ts, app/src/lib/errors.ts, app/src/components/ErrorBanner.tsx, app/src/components/JwlCoreNotice.tsx, app/src/components/CategorySwitcher.tsx, app/src/components/CategoryList.tsx, app/src/components/ColorMenu.tsx, app/src/components/UtilitiesMenu.tsx, app/src/settings/SettingsProvider.tsx</files>
  <read_first>
    - app/src/i18n/I18nContext.tsx, app/src/i18n/strings.ts, app/src/i18n/en.ts -- the
      API this task extends: `useI18n()`, `StringKey`, the `{token}` substitution
      convention, and 11-03's split-around-JSX example.
    - app/src/lib/errors.ts -- the full ~37-case `describeError` switch; every `return`
      is a literal English sentence, several with an interpolated suffix (e.g. the
      `default` case's `${err.operation}`).
    - app/src/components/ErrorBanner.tsx -- its two call sites of `describeError` and
      `error.safe_file_name` (the one dynamic value this component already interpolates
      outside `describeError` itself).
    - app/src/components/CategorySwitcher.tsx -- confirms the enum-value-as-label
      pattern (D6-06) this task must NOT disturb for control flow, only for display.
    - app/src/components/CategoryList.tsx, ColorMenu.tsx, UtilitiesMenu.tsx,
      JwlCoreNotice.tsx -- read each in full; grep each file separately for JSX text
      children AND for `aria-label=`/`title=`/`placeholder=` attributes (a single regex
      pass undercounts per RESEARCH Pitfall 3 -- run both passes per file).
    - app/src/components/CategoryList.tsx's native `open`/`save` dialog calls
      specifically (around lines 409-414, 450-458, 491-496 as of planning) -- these
      pass `filters: [{ name: "..." }]` and a `title: \`Select a prior ${category}
      export…\`` string that are genuinely user-facing OS dialog text but sit outside
      JSX/aria-label/title-attribute scope, so the earlier grep passes miss them; find
      and catalog them explicitly.
    - app/src/settings/SettingsProvider.tsx as left by 11-03 -- confirms its own
      `ErrorBanner` instance now sits inside `I18nProvider` and needs a `t` value passed
      through the same way `ErrorBanner.tsx` gets it.
  </read_first>
  <action>
    Extend `app/src/i18n/en.ts` (do not touch the 8 locale files -- they need no edit).
    For every `describeError` case, add one catalog key per error code (a natural
    namespace: `errors.notAZip`, `errors.missingManifest`, ... `errors.default`, this
    last one carrying an `{operation}` token). Preserve every sentence's exact existing
    English wording verbatim -- this task moves copy, it does not rewrite it. Add keys
    for CategorySwitcher's six category display labels (Notes/Bookmarks/Favorites/
    Highlights/Annotations/Playlists), CategoryList's/ColorMenu's/UtilitiesMenu's own
    strings as inventoried by reading each file, and JwlCoreNotice's notice sentence
    (with a `{reason}` token for its optional suffix) and its dismiss button's
    `aria-label`.

    Change `describeError`'s signature to accept the translation function as a second
    parameter and have every `return` resolve through `t(key)` (with `{operation}`
    substituted for the default case), never a literal. Update BOTH call sites --
    `ErrorBanner.tsx` (pulling `t` from `useI18n()`) and any other place that calls
    `describeError` directly (grep the whole `app/src` tree for `describeError(` to find
    every call site; do not assume `ErrorBanner.tsx` is the only one).

    Retrofit `JwlCoreNotice.tsx`, `CategorySwitcher.tsx`, `CategoryList.tsx`,
    `ColorMenu.tsx`, and `UtilitiesMenu.tsx` to render every string through `t()`,
    including `CategoryList`'s native `open`/`save` dialog `filters[].name` strings
    ("Text files", "JW Library playlists") and its `Select a prior {category} export…`
    dialog `title` -- pass the translated string into the dialog options object at
    call time exactly like any other `t()` call site, the `{category}` interpolation
    still supplied by the component as today. For
    `CategorySwitcher`/`CategoryList`, translate ONLY the rendered label -- the
    `Category` value passed to `onSelect`/IPC calls and used for `data-testid`s must
    remain the untranslated enum string, exactly as today (DATA-08/D6-06). Keep every
    existing `data-testid` unchanged; only rendered text and attribute VALUES that are
    genuinely user-facing (not test hooks) change.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `describeError` contains no literal English sentence in its body -- every branch
      resolves through `t(key, params?)`.
    - Every call site of `describeError` (found by a whole-tree grep, not assumed)
      passes a real `t` from `useI18n()`.
    - CategorySwitcher's and CategoryList's `onSelect`/IPC/testid values are byte-identical
      to before this task (a diff against the pre-task file shows only the rendered
      label/JSX text changing, never the enum-driven attributes) -- verified by the
      existing CategorySwitcher.test.tsx/CategoryList.test.tsx passing unmodified in
      their enum-driven assertions.
    - `git diff app/package.json app/package-lock.json app/src-tauri` is empty.
    - `npx tsc --noEmit`, `npx vitest run`, `npm run build` all clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Presentation-only string substitution across six components and one lib function; no enum, IPC contract, or persisted shape changes.</reversibility>
  <done>The error-message catalog and the six lower-complexity components render exclusively through t(), with category control flow provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The seven dialog-heavy components -- CommandBar, TagDialog, FavoriteAddDialog, MediaAddDialog, EditPreviewDialog, FoldMergeDialog, RecordEditor</name>
  <files>app/src/i18n/en.ts, app/src/components/CommandBar.tsx, app/src/components/TagDialog.tsx, app/src/components/FavoriteAddDialog.tsx, app/src/components/MediaAddDialog.tsx, app/src/components/EditPreviewDialog.tsx, app/src/components/FoldMergeDialog.tsx, app/src/components/RecordEditor.tsx</files>
  <read_first>
    - app/src/components/CommandBar.tsx in full (already read during planning -- it has
      the highest string count and the two pluralized, JSX-embedded dry-run summary
      blocks: the v14-save summary and the merge/fold-merge summaries). These summaries
      mix literal text, an interpolated count, a manually-branched plural suffix, and a
      file/archive-count value -- follow 11-03's split-around-JSX convention exactly:
      catalog templates carry `{count}`/`{plural}`/`{fileName}`/`{archiveCount}` tokens,
      the component computes the plural suffix (`n === 1 ? "" : "s"`) exactly as it does
      today and passes it as a param, it does not move the branching into the catalog.
    - app/src/components/EditPreviewDialog.tsx -- it is REUSED by CommandBar for three
      different confirmation flows (v14 save, merge, fold merge), each passing its own
      `title`/`ariaLabel`/`confirmLabel`/`confirmPendingLabel`/`summary` as props from
      CommandBar. Confirm whether those props are already caller-supplied strings (in
      which case ONLY CommandBar's call sites need retrofitting, not
      EditPreviewDialog's own body) or whether EditPreviewDialog also renders its own
      chrome strings (Cancel button, etc.) that need separate retrofitting -- read the
      file to determine which, do not assume either.
    - app/src/components/TagDialog.tsx, FavoriteAddDialog.tsx, MediaAddDialog.tsx,
      FoldMergeDialog.tsx, RecordEditor.tsx -- read each in full; grep each separately
      for JSX text children AND `aria-label=`/`title=`/`placeholder=` attributes.
    - app/src/components/CommandBar.tsx's `FILTERS` constant (`[{ name: "JW Library
      Backup", extensions: [...] }]`, around line 65 as of planning) and its `open`/
      `save` calls near lines 152, 186, 212 -- and `MediaAddDialog.tsx`'s `open` call's
      `filters: [{ name: "Images", ... }]` around line 96-99 -- these `filters[].name`
      strings are user-facing OS dialog text outside JSX/attribute scope and were
      missed by a prior narrower scan; catalog and retrofit them here.
    - Scope decision on the same three `save()` calls' `defaultPath` literals
      (`"New Archive.jwlibrary"` at line 152, `"Archive.jwlibrary"` at line 186,
      `"Archive (v14).jwlibrary"` at line 212) -- OUT OF SCOPE for this task, left as
      literal English. Rationale: unlike `filters[].name` (a descriptive dialog-chrome
      label) or `title:` (a sentence), a `defaultPath` is the OS save dialog's
      pre-filled suggested filename -- the user routinely retypes or accepts it
      regardless of locale, and it is not prose. All three are string literals, not
      templated (confirmed by reading the call sites; `"Archive (v14).jwlibrary"` is a
      hardcoded literal, not built from a version constant). Splitting each into a
      translated human-readable stem plus a forced-literal `.jwlibrary` extension (per
      the extension-must-stay-unlocalized constraint) would add templating surface for
      a string the user is expected to overwrite/confirm rather than read as UI
      chrome -- not worth the risk of a locale bug corrupting a real file extension.
      Task 3's completeness-test claim is scoped accordingly: it covers
      `filters[].name` and dialog `title` strings, NOT `defaultPath`.
    - app/src/i18n/en.ts as extended by task 1 -- the growing key namespace convention
      to continue (e.g. `commandBar.*`, `tagDialog.*`).
  </read_first>
  <action>
    Extend `app/src/i18n/en.ts` with one namespaced key per string found in these seven
    files, following task 1's established convention. For every pluralized or
    value-interpolated string, use the `{token}` substitution mechanism with the
    component continuing to compute WHICH value/plural-suffix to pass -- the catalog
    holds the template, the component holds the logic, exactly as 11-03 established.

    Retrofit each of the seven components so every button label, dialog title,
    `aria-label`, placeholder, native-dialog `filters[].name` string (CommandBar's
    `FILTERS` constant, MediaAddDialog's `open` call), and summary sentence renders
    through `t()`. Where
    `EditPreviewDialog` is invoked by CommandBar with caller-supplied prop strings,
    retrofit CommandBar's call sites (the props it passes), and separately retrofit any
    of EditPreviewDialog's OWN internal chrome strings if it has any (per the read_first
    finding above). Do not change any invoke command name, request payload shape, or
    `data-testid` value anywhere in this task -- only rendered text and genuinely
    user-facing attribute values.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All seven components contain zero hardcoded user-facing string literals outside
      `t()` calls (spot-checked here; task 3's structural test is the exhaustive proof).
    - CommandBar's two dry-run summary sentences render byte-identical output to before
      this task for the same inputs (e.g. 1 vs 3 locations, 1 vs multiple records) --
      verified by the existing CommandBar.test.tsx assertions on rendered summary text
      passing unmodified, proving the `{token}`/plural-param substitution reproduces the
      exact prior copy.
    - No `invoke(...)` call's command name or payload shape changed anywhere in this
      task's diff.
    - `npx tsc --noEmit`, `npx vitest run`, `npm run build` all clean.
  </acceptance_criteria>
  <reversibility rating="reversible">Presentation-only string substitution across seven components; no IPC contract, enum, or persisted shape changes.</reversibility>
  <done>Every dialog and the command bar render exclusively through t(), with pluralized/interpolated summaries reproducing their exact prior English output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Completeness test across all 13 retrofitted components + errors.ts, and a live multi-component language-switch test</name>
  <files>app/src/i18n/completeness.test.ts, app/src/lib/errors.test.ts</files>
  <read_first>
    - app/src/i18n/I18nContext.test.tsx (11-03) -- the exact structural-scan technique
      (source-text regex over JSX text nodes and the three attribute names, with a
      narrow explicit allowlist) this task extends from two files to the full thirteen
      plus errors.ts.
    - Every file this plan modified (tasks 1 and 2) -- confirm final state before writing
      assertions against it.
    - `app/src-tauri/src/error.rs` and `app/src-tauri/src/settings.rs` -- their `to_dto`
      match arms are the source-of-truth code list `errors.test.ts` derives from (each
      arm's tuple is `("snake_case_code", "error.category.key")`, sometimes split across
      lines); confirm the tuple shape is consistent enough for one regex to extract both
      files before writing the extractor.
    - `app/src/bindings/ErrorDto.ts` -- confirms `code: string` (not a union), which is
      why the coverage test reads Rust source rather than importing a TS type.
    - The <behavior> block below -- the test names listed there are contractual.
  </read_first>
  <behavior>
    - `completeness all components`: the structural scan from 11-03 runs over
      `CommandBar.tsx`, `TagDialog.tsx`, `FavoriteAddDialog.tsx`, `MediaAddDialog.tsx`,
      `EditPreviewDialog.tsx`, `FoldMergeDialog.tsx`, `RecordEditor.tsx`,
      `CategorySwitcher.tsx`, `CategoryList.tsx`, `ColorMenu.tsx`, `UtilitiesMenu.tsx`,
      `ErrorBanner.tsx`, and `JwlCoreNotice.tsx` (named explicitly, not a glob that could
      silently skip a renamed file) and finds zero hardcoded strings outside the same
      narrow allowlist 11-03 established (product name, raw `Category` enum values used
      as `data-testid`/props, punctuation-only fragments) -- the scan's regex now ALSO
      matches native-dialog `filters:` array `name:` string literals and `open`/`save`
      `title:` string literals (not just JSX text and `aria-label`/`title`/`placeholder`
      attributes), so CommandBar's `FILTERS` constant, CategoryList's export/import
      dialog filters and prior-export title, and MediaAddDialog's `filters` are covered
      by the same gate, not merely retrofitted by convention in tasks 1-2.
    - `describeError full coverage`: every error code the Rust source actually emits
      resolves, through `describeError`, to a non-empty, non-literal (catalog-sourced)
      string, and the `default` branch's `{operation}` substitution works. The code list
      is derived at test time, not hand-typed and not read off a TS union (`ErrorDto`'s
      `code` field is `string`, per `app/src/bindings/ErrorDto.ts`, and stays that way --
      see the scope_boundary note above): the test reads `src-tauri/src/error.rs` and
      `src-tauri/src/settings.rs` as text and regex-extracts every `("code", "message_key")`
      tuple from their `to_dto` match arms, then asserts `describeError` produces a
      distinct, non-empty, catalog-sourced string for each extracted code. A code present
      in the Rust source with no corresponding `describeError` branch fails this test.
    - `language switch, multi-component`: rendering `CommandBar` (with an archive open)
      and `ErrorBanner` (with an error present) together under `I18nProvider`, changing
      the active locale re-renders BOTH components' visible text on the same interaction,
      falling back to English since the target locale's catalog is still empty.
    - `category enum isolation (structural)`: a source-text assertion that
      `CategorySwitcher.tsx`/`CategoryList.tsx` never pass a `t()` return value into
      `onSelect`, an `invoke(...)` payload, or a `data-testid` template literal --
      guards D-08/D6-06 by construction, not by convention alone.
  </behavior>
  <action>
    Write `app/src/i18n/completeness.test.ts` extending 11-03's structural-scan
    technique (same `?raw`-style source read, same regex approach) across the thirteen
    named files, with the allowlist stated as an exported constant so it is visibly the
    single source of truth for what is permitted, not re-derived per file. Extend the
    scan's regex to also catch native-dialog `filters: [{ name: "..." }]` and
    `title: "..."`/template-literal option strings passed to the `@tauri-apps/plugin-dialog`
    `open`/`save` calls, alongside the existing JSX-text and
    `aria-label`/`title`/`placeholder` attribute matching.

    Write `app/src/lib/errors.test.ts` covering `describeError full coverage` by reading
    `src-tauri/src/error.rs` and `src-tauri/src/settings.rs` from disk at test time (Node
    `fs`, already available in the vitest environment -- no new dependency) and
    regex-extracting every `("code", "message_key")` tuple out of each file's `to_dto`
    match arms, then asserting `describeError` resolves each extracted code to a
    distinct, non-empty string via a real `t` built from the actual `en` catalog -- not a
    mock `t` that trivially echoes the key, and not a hand-typed duplicate code list
    that can drift from the Rust source (`ErrorDto["code"]` is `string`, not a finite
    union, so there is no TS union to iterate; the Rust match arms ARE the source of
    truth this test derives from instead).

    Write the multi-component language-switch test and the category-enum-isolation
    structural test using the same Testing Library + invoke-mocking conventions already
    established (see `SettingsDialog.test.tsx`/`I18nContext.test.tsx` from 11-03).

    Use `npx vitest run`, never bare `vitest`.
  </action>
  <verify>
    <automated>cd app && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four named behaviors are covered by passing tests.
    - The completeness test is proven to actually guard: temporarily reintroduce one
      hardcoded string into one of the thirteen files, confirm the test goes red for
      that specific file, revert, confirm green -- record this in the SUMMARY (mirrors
      the demonstrated red/green precedent from every prior plan's own guard test).
    - `describeError full coverage` derives its code list from `src-tauri/src/error.rs`
      and `settings.rs` at test time (regex-extracted `("code", "message_key")` tuples),
      not a manually duplicated list -- adding a new `to_dto` match arm in either Rust
      file without a matching catalog key and `describeError` branch fails this test
      automatically, and the test never touches `ErrorDto`'s TS shape to achieve this.
    - The category-enum-isolation test fails if a future edit routes a translated label
      into `onSelect`/an IPC payload/a testid -- demonstrate this too (temporarily do it,
      confirm red, revert, record in the SUMMARY).
    - Full existing vitest suite remains green -- no regression across eleven phases of
      accumulated tests.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>PLAT-03 is proven complete by test: every retrofitted component and the error catalog are provably free of stray hardcoded strings, category identity is provably still enum-driven, and a language switch visibly updates multiple components at once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ErrorDto.code`/`operation` -> catalog lookup | `describeError` keys off a closed, Rust-emitted `code` string and interpolates the free-form `operation` field into the one `default`-branch template |
| translated label -> control flow | the boundary that must NOT be crossed: a `t()` return value must never reach `onSelect`, an IPC payload, or a `data-testid` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Tampering | a translated category label accidentally driving `list_category`'s IPC argument or `onSelect` | medium | mitigate | `CategorySwitcher`/`CategoryList` keep the raw `Category` enum as the sole control-flow value; task 1's acceptance criteria and task 3's structural `category enum isolation` test both check this directly, not by inspection alone |
| T-11-16 | Information disclosure | `describeError`'s `default` branch interpolating `err.operation` (a value that could theoretically carry more detail than intended) | low | accept | `operation` already crosses the sanitized `ErrorDto` IPC boundary today (pre-existing, unrelated to this plan) -- this plan only moves the surrounding sentence into the catalog, it does not change what `operation` contains or add new interpolation surface |
| T-11-17 | Denial of service | a missing catalog key for a `describeError` branch after this retrofit | low | mitigate | `describeError full coverage` (task 3) derives the real code list at test time by regex-extracting every `("code", "message_key")` tuple from the Rust `to_dto` match arms in `src-tauri/src/error.rs` and `settings.rs` -- not a hand-maintained list and not a TS union (`ErrorDto["code"]` stays `string`) -- so a future code without a matching key fails the test rather than silently rendering `undefined` |
| T-11-SC | Tampering | npm dependency supply chain | low | accept | Zero new dependencies across this plan -- same as 11-03 |
</threat_model>

<verification>
- `cd app && npx vitest run`, `npx tsc --noEmit`, `npm run build` all clean.
- `git diff app/package.json app/package-lock.json app/src-tauri` empty -- zero new
  dependency, zero Rust touched across this entire plan.
- `cd app/src-tauri && cargo test --jobs 2` still fully green (regression check only;
  this plan should not touch any Rust file).
- Manual: launch the app with an archive open, trigger a merge dry-run preview and an
  error banner (e.g. attempt an operation that fails), open Settings, switch language,
  confirm the command bar, the open dialog, and the error banner all re-render
  legibly (falling back to English) with no reload and no blank/undefined text anywhere.
</verification>

<success_criteria>
Every component in `app/src/components/` and `lib/errors.ts`'s error catalog render
exclusively through `t()`; category identity remains provably enum-driven through both
runtime behavior and a structural test; a language switch visibly and correctly updates
multiple components in the same interaction; PLAT-03 is complete, not partial.
</success_criteria>

<artifacts_this_plan_produces>
| Kind | Name / path |
|------|-------------|
| Modified | `app/src/i18n/en.ts` (extended with every remaining component's keys), `app/src/lib/errors.ts` (`describeError(err, t)`), `app/src/settings/SettingsProvider.tsx` (its own `ErrorBanner`/`describeError` call site updated to pass `t`) |
| Test file | `app/src/lib/errors.test.ts` (new -- `describeError full coverage`, deriving codes from `src-tauri/src/error.rs`/`settings.rs`), `app/src/i18n/completeness.test.ts` (new -- extends 11-03's structural scan across all 13 components + native-dialog strings, plus `language switch, multi-component` and `category enum isolation (structural)`) |
| Modified (13 components) | `CommandBar.tsx`, `TagDialog.tsx`, `FavoriteAddDialog.tsx`, `MediaAddDialog.tsx`, `EditPreviewDialog.tsx`, `FoldMergeDialog.tsx`, `RecordEditor.tsx`, `CategorySwitcher.tsx`, `CategoryList.tsx`, `ColorMenu.tsx`, `UtilitiesMenu.tsx`, `ErrorBanner.tsx`, `JwlCoreNotice.tsx` (all under `app/src/components/`), plus their existing `*.test.tsx` files updated where rendered text assertions changed -- `JwlCoreNotice`'s own retrofit is asserted inside `ErrorBanner.test.tsx` (its existing test location, not a separate `JwlCoreNotice.test.tsx` file) |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/11-platform-polish/11-04-SUMMARY.md` when done.
</output>
